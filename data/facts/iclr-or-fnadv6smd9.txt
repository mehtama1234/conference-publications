REAL METHOD of "Non-Convex Federated Optimization under Cost-Aware Client Selection" (arXiv 2512.05327v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Within this setting, we propose a new algorithm that achieves the best-known communication and local complexities among existing federated optimization methods for non-convex optimization. This algorithm is based on the inexact composite gradient method with a carefully constructed gradient estimator and a special procedure for solving the auxiliary subproblem at each iteration. These constraints make it critical to design optimization algorithms that minimize communication costs, particularly in settings with partial client participation. This efficiency arises from the method’s ability to exploit dissimilarity ( δ) between local and global objectives. However, SARAH-based methods require peri- odic full synchronization with all clients in order to compute full gradients. Consequently, it remains unclear whether such methods are more communication-efficient than SARAH-based methods, since they rely on fundamentally different client-selection strategies with different constant dependencies. • We propose a new model formalizing the concept of federated optimization algorithms and defining information-based notions of communication and local complexities. (Section 2.1) • Within our new model, we propose a new gradient method that achieves the best communication and local complexities among existing first-order methods for non-convex optimization. This method is based on the inexact composite gradient method (I-CGM) with a carefully constructed gradient estimator and a special procedure for solving auxiliary subproblem at each iteration. 2.1 Federated Optimization Algorithms and their Complexity Federated Optimization Algorithm.We consider the standard federated optimization setting with a central server and n clients. ,Ofn, afederated optimization algorithmfor a problem class F is a procedure that proceeds acrosscommunication rounds. Then it specifies a certain method Mr i (often called alocal method) for each client i∈S r to run locally. The method Mr i starts with the initial information (¯I r i ,H r i ), and iteratively queries the oracle Ofi, obtaining a response Rr i , which is then sent back to the server. After the algorithm terminates at a certain round R≥0 , the server then constructs and outputs an approximate solution ˆxR to problem (1) based onI R using a certain rule specified by the algorithm. 1) how to select clients, 2) how to compute the information ¯I r i that is sent to each selected client, 3) which local method each selected client runs, 4) when to terminate, and 5) how to form the approximate solution. Illustration of the sequence of procedures performed by a federated optimization algorithm at each communication roundr. At each step k≥0 , this method first computes a point xr i,k based on Hr i,k, queries the oracle at this point, and then updates its local information: At the beginning of each step k, the method also performs a termination test T r i,k(Hr i,k); Once this test is satisfied at a certain step K r i , the method terminates and constructs the output Rr i from the final information Hr i,K r i . The information sets are then updated as Hr+1 i :=H r i,Kr i , and remain the same (Hr+1 i :=H r i ) for each non-selected client i /∈Sr. 1) how to compute the next point at each step, 2) when to terminate, and 3) how to form the result. We allow each of these rules to be randomized (resulting in arandomizedlocal method); if all the rules are deterministic, the local method is calleddeterministic. Note that the above definition of a distributed optimization algorithm is rather general and only constrains how the algorithm accesses information about the optimization problem. In particular, we do not impose any restrictions on the arithmetic or memory complexity of each step of the algorithm, nor on the size of the data transmitted between the server and the clients. ∥∇f1(x)− ∇f1(y)∥ ≤L1∥x−y∥.(4) 3 Inexact Composite Gradient Method 3.1 Inexact Composite Gradient Method We first introduce the Inexact Composite Gradient Method (I-CGM), which serves as the backbone of our approach. Moreover, if the approximation errors PT−1 t=0 ˆΣ2 t can also be bounded by the first two terms on the left-hand side, then the convergence of the gradient norm is guaranteed. Then by further upper-bounding the summation of PT−1 t=0 [f(x t)−f ⋆] in terms of F 0 and ˆΣ2 t , it can be shown that we need K≃ L1T λ local steps to achieve the desired accuracy condition (6). Then at each iteration t≥0 , Gt is computed first, after which the next iterate xt+1 is computed. , ncomputes ∇fi(xt) and initializes bt i and sends the result to the server; the server then aggregates these results computing ∇f(x t) to initialize Gt and bt. , ncomputes ∇fi(x0) and sends the result to the server; the server then aggregates these results, computing ∇f(x 0) to initialize G0. At each iteration t≥0 , the estimator gt ≈ ∇f(xt) is computed first and it depends only on Gt−1 and St−1. , ncomputes ∇fi(x0) and sends the result to the server; the server then aggregates these results, computing ∇f(x 0) to initialize g0. We choose this case to demonstrate that even when A-CSS and R-CSS are equally cheap, our proposed methods already outperform several commonly used algorithms. To generate Ai,j, we first sample a diagonal matrix ¯A with entries uniformly distributed in [0,110] , and then add bn diagonal noise matrices whose entries are sampled from [0,18] . We compare two proposed algorithms against SCAFFOLD[ 20], FEDAVG[ 1] (with sampling), SABER-FULL[ 12] (with PAGE), SABER-PARTIAL[ 12] (only compute full gradient once) and GD (running directly on f). For SVRG-based methods, the expected number of communication rounds at each iteration is roughly m n n+m , which is twice as large as other methods. For simplicity and to ensure fair comparisons across algorithms, we assume that all methods use the deterministic first-order oracle locally, i.e., Ofi = OFOi, for all i∈[n] . 18 We next briefly describe each method in Table 1 and discuss how the communication and local complexities are computed for each method. Then the server makes another communication round with D-CSS, sends ∇f(x t) to client 1 which then performs 1/p local steps in expectation and sends the result back to the server. xt+1 i ≈arg min x∈Rd  fi(x) + ∇fSt (xt)− ∇fi(xt),x by running momentum-based first-order methods locally for Θ( Lmax ∆max ) steps. At each iteration t≥0 , the server first uses R-CSS to select clients St, sends xt−1 and xt to the client