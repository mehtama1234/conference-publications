REAL METHOD of "Dynamic Kernel Graph Sparsifiers" (arXiv 2211.14825v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

53 2 1 Introduction Kernel methods are a fundamental tool in modern data analysis and machine learning, with exten- sive applications in computer science, from clustering, ranking and classification, to ridge regres- sion , principal-component analysis and semi-supervised learning [vL07, NJW02, Zhu05a, Zhu05b, LSZ+19b]. We can assume the first row and first column are changed with no loss of generality. If αd =O(poly(n)), then there is a randomized dynamic algorithm that receives updates of locations of points inPone at a time, and maintains a almost linear spectral sparsifier inn o(1) time with probability1−1/poly(n). [FHP05] presented an algorithm to update the list of WS pairs, but it cannot be used directly in this situation, because the [FHP05] algorithm is only able to return a list of WS pairs such that the singleton containing the inserted (or removed) point is one of the vertex subsets in these pairs. Among these steps, the WSPD update algorithm is deterministic; the resampling algo- rithm uses fresh randomness for every round of updates. Given a set of data points{x i}n i=1, and a sketching matrix Π∈R k×d defined in Definition H.1, we initialize a set of precomputed projected data pointsex i = Π·x i. Without loss of generality, we first make the assumption that, all the points are in theℓ 2 unit ball ofR d, i.e., the set{x∈R d | ∥x∥2 ≤1}. We can assume the first row and first column are changed with no loss of generality. Efficient accelerated coordinate descent methods and faster algorithms for solving linear systems. Training multi-layer over-parametrized neural network in subquadratic time.arXiv preprint arXiv:2112.07628, 2021. While the existing algorithm is very fast when the graph is sparse enough, we should focused on faster algorithms since our target graph might be dense. [ZHDK23, AS23] shows how to use kernel technique to compute attention matrix in large language models. Dynamic Algorithms used in Optimization.In addition, dynamic algorithms have been widely used in many of the optimization tasks. Usually, most optimization analysis are robust against noises and errors, such as linear programming [CLS19, JSWZ21], empirical risk mini- mization [LSZ19a, QSZZ23], semi-definite programming [HJS +21, GS22], integral optimization [JLSZ23], training neural network [BPSW21, SYZ21, SZZ21], and sum of squares method [JNW22]. Ifβ >1, then Pr[L≥ βk d ]≤β k/2 ·(1 + (1−β)k (d−k) )(d−k)/2 ≤exp( k 2 (1−β+ lnβ)) B.3.2 Useful lemmas on JL Using Lemma B.7, we can show that Lemma B.8.Letβ=n 1/k, then we havePr[L≥ βk d ]≤exp(− 1 4 kn1/k)≤exp(−log 1.9 n). Proof.We show that Pr[L≥ βk d ]≤exp( k 2 (1−β+ lnβ)) ≤exp(− k 2 β 2 ) = exp(− k 2 n1/k 2 ) ≤exp(−log 1.9 n) where the first step follows from Lemma B.7, the second step follows fromβ/2≥1 + lnβ, the third step follows fromβ=n 1/k, and the last step follows fromn 1/k ≥log 1.9 n. Definition B.17(Compressed quad tree).Given a quad treeT, for each a path of degree one nodes, we replace it with the first and last nodes on the path, with one edge between them. 24 Algorithm 1Finding well separated pairs 1:procedureWSPD(Q, u, v)▷ Qis a compressed quad tree,u, vare tree nodes onQ, Lemma B.21 2:ifuandvare well separatedthen 3:return(u, v) 4:else 5:ifl max(u)> lmax(v)then 6:Letu 1, . 15:procedureComputeWSPD(Q)▷ Qis a compressed quad tree 16:r←root ofQ 17:returnWSPD(Q, r, r) 18:end procedure Lemma B.19(Theorem 2.2.3 in [HP11]).Given ad-dimensional point setPof sizen, a com- pressed quad tree ofPcan be constructed inO(dnlogn)time. The procedureComputeWSPD(Algorithm 1) generates the WSPD from the tree Q, and runs in time O(2d ·nlogn). 25 B.6 Leverage Score and Effective Resistance Definition B.23.Given a matrixA∈R m×n, we defineσ∈R m to denote the leverage score ofA, i.e., σi =a ⊤ i (A⊤A)ai,∀i∈[m]. LetR∈R m×n be a Sparse Embedding matrix as defined in Definition C.1 withm= O(ϵ−2 ·log(1/δ))ands= Ω(ϵ −1 ·log(1/δ))non-zero entries of each column, then it satisfies(ϵ, δ)- AMP ofAandB, andA ⊤R⊤RBcan be computed in time s·nnz(A) +s·nnz(B) +T mat(dA, m, dB). It takess·nnz(A) time to computeA ⊤R⊤,s·nnz(B) time to computeRB, andT mat(dA, m, dB) to computeA ⊤R⊤RB. D Algorithm Here in this section, we give our main algorithm as follows. 28:procedureUpdateB(∆b∈R n)▷∆bis sparse 29:∆ eb←Φ·∆b 30:ez←ez+ eL† ·∆ eb 31:end procedure 32: In Section E.5, we first propose a (slow) resampling algorithm takesO(n) time to resamplen o(1) edges. In Section E.6, we then explain how to improve the running time of (slow) resampling algorithm. In Section E.8, we apply a black box reduction to our update algorithm to obtain a fully dynamic update algorithm. Lemma E.8.Given a compressed quad treeTof aO(k)-dimensional point setP, a WSPDP computed fromT, a pointp∈Pand another pointp ′, in the output of Algorithm 6,T new is a quad treeT new ofP\{p} ∪ {p′},P new is a WSPD ofP\{p} ∪ {p′}andSis a collection of tuples (A, B, A′, B′).P new can be obtained by doing the following: E.5 Linear Time Resampling Algorithm Here in this section, we state our linear time resampling algorithm. E.6 Efficient Sublinear Time Resampling Algorithm Algorithm 7 returns a set of pairs that is with high probability close to the input setE. •An initialization procedure which runs in timet initialize; •An update procedure which runs in timet update. Then we have the result that, for any parameterξsuch thatξ≤min{ζ,log 6 (w/2)}, there exists a fully dynamic data structure consists of a size-O(2 ξ)set of data structuresD. Without loss of generality, we can assume the first row and first column are changed. F.3 Algorithms F.3.1 Modification to DynamicGeoSpar For the applications in Section F and G, we add a memberdiffand methodsgetDiffand getLaplaciantoDynamicGeoSparand change methodsInitand Update to initialize and updatediff(Algorithm 10). F.3.2 Dynamic Sketch Algorithm Here we propose the dynamic sketch algorithm as follows. ▷Content in Algorithm 4 18:diff← {} 19:end procedure 20:procedureUpdate(P) 21:. ▷Content in Algorithm 9 22:foreachEdgesupdate pair (E, E new)do 23:foreach e inE\E new do 24:Add−eto diff 25:end for 26:foreach e inE new\Edo 27:Addeto diff 28:end for 29:end for 30:end procedure Running timeBy Theorem E.3, line 19 takesO(dk+n o(1) logα) time. 25:procedureUpdateV(∆v∈R n)▷∆vis sparse 26:∆ev←Ψ·∆v 27:ez←ez+ eL·∆ev 28:end procedure 29: 30:procedureQuery 31:returnez 32:end procedure is the updated sketch ofL H x. There is a data structure (Algorithm 12) for the Online Approximate Dynamic Ultra-