REAL METHOD of "JAPAN: Joint Adaptive Prediction Areas with Normalising Flow" (arXiv 2505.23196v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

In this work, we proposeJAPAN (Joint Adaptive Prediction Areas for Normalising- flows), a method that uses the (conditional) density estimates as the conformity score computed byflow-based models. • We proposeJAPAN, a conformal prediction method that uses log-densities as conformity scores, enabling efficient, adaptive, and potentially multimodal prediction areas. This dataset is partitioned into atraining set Dtrain = {(xi, yi)}m i=1 and acalibration set Dcal = {(xi, yi)}n i=m+1, where n = m + l. X → Yis fitted onDtrain, and aconformity score αi is computed on each calibration point(xi, yi). This includes discrete flows as well as continuous-time flows such asscore-based diffusion models (bijective mapping exists through the probability flow ODE) (Song et al., 2021, 2020), continuous normalising flows (CNFs)(Grathwohl et al., 2018) trained with or without Flow-Matching (Lipman et al., 2022), orstochastic interpolants objectives (Albergo and Vanden-Eijnden, 2022). After training the NF on the training set, we compute conformity scores on the held-out calibration set. We summarise the temporal context into a representationc(i) T , computed using a recurrent or attention-based encoder such as an RNN or Transformer Vaswani et al. Then the area of the prediction setΓϵ(x) can be computed as [Area(Γϵ(x)) = 1 N NX i=1 1  ˆp(h−1(zi, x) | x) ≥ τϵ · exp(ϕ(z, x)) pZ(zi) . Training setDtrain = {(xi, yi)}n i=1, calibration setDcal = {(xj, yj)}l j=1, significance level ϵ ∈ (0, 1) Ensure: Train a conditional normalising flow to estimateˆp(y | x) on Dtrain: As a result, low-density regions in the data space that correspond to high-density latent samples are mistakenly included in the conformal region.PCP also allows for disjoint regions and avoids global geometric constraints, but its local ball-based area construction introduces soft constraints that fail on densities with ring-like modes. We bold the methods for each datasets that have a mean coverage of 0.90±0.01 and has the lowest area for the dataset. Computed areas per dataset (Checkerboard, Moons, Circles, Spiral) for all the methods computed over25 random data splits. Each bar’s length signifies the standard deviations, with methods sorted by average area for each dataset. JAPAN is the only method that can provide disjoint regions, is context- adaptive, and has no constraint on the geometry, whereas PCP and CONTRA have soft constraints on the geometry. This is due to these methods using half of the calibration set to train an auxiliary copula or residual predictor model, and their final prediction intervals can become numerically unstable. We bold the methods for each datasets that have a mean coverage of 0.90±0.01 and has the lowest area for the dataset. Prediction areas per dataset (Energy, RF2D, RF4D, SCM) for the top 5 methods computed over25 random data splits. Each bar’s length signifies the standard deviations, with methods sorted by average area for each dataset. We bold the methods for each datasets that have a mean area of 0.90±0.01 and has the lowest area for the dataset. Prediction areas per dataset (COVID, Particle-1, Drone, Pedestrian) for the top 5 methods computed over25 random data splits. Each bar’s length signifies the standard deviations, with methods sorted by average area for each dataset. This can be viewed as using afixed context encoder, whose representation is learned through an external or unrelated training objective, or could be trained under a single objective. for each calibration example(x, y), we evaluate the log-densitylog ˆp ˆY (y) under the learned marginal model (rather than conditioning onx) and use this as the conformity score (see Algorithm 2). Nonetheless, we find that the method can produce surprisingly compact and valid prediction regions, especially when compared to baselines that rely on fixed geometric constraints. Then, unlike the unconditional case, we train a conditional normalising flow to model P(Y | ˆY ). Training setDtrain = {(xi, yi)}n i=1, calibration setDcal = {(xj, yj)}m j=1, significance level ϵ ∈ (0, 1) Ensure: Even though the flow is trained separately from the base model, its ability to correct for systematic biases in ˆy enables significantly more efficient conformal sets than any competing method. Training setDtrain = {(xi, yi)}n i=1, calibration setDcal = {(xj, yj)}m j=1, significance level ϵ ∈ (0, 1) Ensure: Train a base modelµ on Dtrain to predict ˆyi = µ(xi) 2: Training setDtrain = {(xi, yi)}n i=1, calibration setDcal = {(xj, yj)}m j=1, significance level ϵ ∈ (0, 1) Ensure: Option B (prediction posterior):Train a predictive modelµ, then fit ˆp(µ(x) | y) using {(µ(xi), yi)}n i=1 3: Training setDtrain = {(xi, yi)}n i=1, calibration setDcal = {(xj, yj)}m j=1, significance level ϵ ∈ (0, 1) Ensure: Further details on the algorithm are in Algorithm 6. Algorithm 7 shows the full algorithm for this proposal. Training setDtrain = {(xi, yi)}n i=1, calibration setDcal = {(xj, yj)}m j=1, significance level ϵ ∈ (0, 1) Ensure: Training setDtrain = {(xi, yi)}n i=1, calibration setDcal = {(xj, yj)}m j=1, significance level ϵ ∈ (0, 1) Ensure: Joint Adaptive Prediction Areas with Normalising-Flows our approach can be extended to use predictions from another model, posterior densities, or an adaptive threshold, each step introduces additional assumptions and potential estimation errors. zl+1 = f l(zl) +context encoder(x), where f l is the Transformer autoregressive block,l is the layer index, andcontext encoder(x) denotes the conditioning signal projected to match the forecast window. The attention mechanism ensures each timestepzl i can only attend to past and current positions. exact likelihoods via tractable Jacobians, causal modelling of temporal dependencies, and a parallelisable attention-based architecture. We first validated the hyperparameters on a simulated dataset and then reused them across all real-world experiments. The attention-based flow model includes 10 autoregressive flow layers. Within each attention block, we use 2 attention heads, with 32-dimensional head size and 64-dimensional time-step embeddings (via expansion). For the density model, we use a normalising flow architecture composed of 4 coupling layers, each with 2 residual blocks and hidden layer dimensionality of 32.