REAL METHOD of "Thought Branches: Interpreting LLM Reasoning Requires Resampling" (arXiv 2510.27484v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

First, when a model states a reason for its action, does that reason actually cause the action? In “agentic misalignment” scenarios, we find that self-preservation sentences have small causal impact, suggesting they do not meaningfully drive blackmail. Third, how do we understand the effect of removing a reasoning step when the model may repeat it post-edit? We introduce a resilience metric that repeatedly resamples to prevent similar content from reappearing downstream. Fourth, since CoT is sometimes “unfaithful”, can our methods teach us anything in these settings? Adapting causal mediation analysis, we find that hints that causally affect the output without being explicitly mentioned exert a subtle and cumulative influence on the CoT that persists even if the hint is removed. For each sentence, we measure resilience(# resamples to eliminate its content) andeffect(causal impact on the output when its content is fully absent). evaluating which reasoning steps actually cause the final answer (Fayyaz et al., 2024; Arcuschin et al., 2025; Chen et al., 2025). Most interpretability methods either ignore this or average over a handful of seeds, which is insufficient for causal claims. Our approach is to approximate this distribution via on-policy resampling, regenerating the subsequent CoT from different points and contrasting the downstream trajectories. We focus on decision-making scenarios, first targeting “agentic misalignment” (Lynch et al., 2025) where models appear to blackmail others in self-preservation. Models may utter a statement later if it is omitted or they can negate earlier steps. (ii) Methods.We introduce methods to measure the resilience and counterfactual importance of reasoning steps, robust to the model later repeating a removed step.(iii) Empirical.Across models and domains, we identify critical decision points, show self-preservation statements have negligible causal impact in blackmail, and reveal global structure in CoT distributions (e.g., influential justification clusters in resume screening). For each model, we generate 20 base CoT traces leading to blackmail and perform counterfactual resampling with 100 rollouts per sentence. 2.1 METHODOLOGY 2.1.1 COUNTERFACTUALIMPORTANCEBASELINE Quantifying the importance or effect of a reasoning step with respect to the final response produced by the model is a reasonable prerequisite for CoT interpretability (Bogdan et al., 2025). For each sentence Si in a CoT, we generate a distribution over outcomes by resampling from positionionward, keepingS 1, . To address the limitation of counterfactual importance, we introduce counterfactual++ importance, which measures the causal impact when a sentence’s semantic content is completely absent from the entire trace. Baseline counterfactual importance (Appendix C.1.1, Figure 7) shows relatively uniform importance across all categories, precluding claims about which reasoning steps truly drive blackmailing. 3.1 METHODOLOGY We investigate blackmail and whistleblowing scenarios, where models threaten exposure of private information. For each CoT, we first intervened on the 5 most impactful sentences positions per baseline counterfactual importance (Section 2), although similar results emerge in just examining randomly selected positions (Appendix C.2.3). On-policy resampling achieves substantive plan shifts more often and has no effect less often than off-policy methods. 4.1.1 TRANSPLANT RESAMPLING Our analyses generate a CoT for a question while a hint is presented and then transplant portions of said CoT into a model prompted with the same question but without any hint. For each question, we manually identified one unfaithful CoT, where the model gave the hinted answer but did not mention the hint. For each unfaithful CoT consisting of N sentences, we performed the following procedure, which allows measuring the cumulative effect of the firstisentences of an unfaithful CoT on the model’s answer: 1.Truncation:For each sentenceS i wherei∈[1, N], we truncated the CoT up to and includingS i. First, appending sentences from an unfaithful CoT onto an unhinted prompt reliably increases the rate at which a model outputs the hinted answer. The hint motivates many biased reasoning steps throughout the CoT to elicit the hinted answer. This issue has seen considerable attention in the context of how LLMs evaluate job candidates and resumes (Sorokovikova et al., 2025; Karvonen & Marks, 2025; Wilson & Caliskan, 2024; An et al., 2025). 4.2.1 METHODOLOGY We examined how Qwen3-8B evaluates a resume for a mid-level information technology (IT) position. Running the model 100 times for each prompt in the dataset, we identified eight prompts displaying variable answers (Myes = 46.3%, SDyes = 14.6%), which we used for the resampling analysis. For each resume and variant, we generated 500 base CoTs and responses. For each CoT, we then resampled the modeloncestarting from each sentence. For each resume, we organized its sentences into 32 clusters based on sentence embeddings; names and pronouns were replaced with neutral words prior to clustering. Whereas Section 2 and Section 3 accurately evaluated a sentence’s effect by intensively resampling its CoT, here we minimally resample a large number of CoTs and then cluster across CoTs to allow reliably estimating a sentence cluster’s effect. Appendix Section E provides the complete results, showing the four sentences for each resume encouraging yes or no responses most. We next tested a mediation model for each resume, as a regression predicting each base CoT decisions (y= 1 or 0), with x as a dummy variable for Black female versus White male and with 16 mediators, one for each sentence cluster (bi = 1if a sentence i appeared and 0 otherwise). The resampling approach then quantifies the each statement’s impact which can be used for principled interpretation. Attribution methods such as ablations or statistical 1Examining how sentences correlate with a final answer does not reveal these patterns – e.g., “Therefore, the answer is no” is linked to a 99% no rate, but this statement is determined by the upstream CoT and its causal effect is minuscule. 8 Published as a conference paper at ICLR 2026 scoring aim to identify which steps matter (Bogdan et al., 2025; Fayyaz et al., 2024), but they frequently miss cases where models regenerate omitted content or silently bias their reasoning. Our work unifies these threads by providing a causal resampling framework 