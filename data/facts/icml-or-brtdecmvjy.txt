REAL METHOD of "Enhancing LLMs for Graph Tasks via Graph-aware LoRA Generation" (arXiv 2606.22429v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

How- ever, existing methods struggle to encode whole- graph information, leading to potential informa- tion loss and suboptimal graph understanding. In this work, we propose a novel weight-level infor- mation injection paradigm for adapting LMs to graph tasks. Instantiating this paradigm following low-rank adaptation (LoRA), we introduce GaRA, a Graph-aware LoRA generation model. GaRA constructs low-rank weight updates conditioned on the original graph structures and constrains the norm of the generated updates, thus injecting whole-graph information and avoiding the opti- mization bias in the weight generation. (b) Input- level injection paradigm transforms input graphs into subgraph sequences, overlooking higher-level information in the original graphs and leading to potential information loss. (d) In practice, weight-level injection is complementary to the input-level injection, alleviating potential information loss. However, the parameterization of GNNs is inherently tied to the number of input feature dimensions and output targets (e.g., labels or attributes) in the training data (Zhao et al., 2024). As graphs from different datasets exhibit heterogeneous feature spaces or target sets (Chen et al., 2024b), pre-trained GNNs cannot be directly transferred to downstream graphs without modifying model parameters or architectures (Zhao et al., 2024). However, similar to traditional GNNs, these methods remain tightly coupled to task-specific target sets and thus exhibit limited transferability across different downstream tasks. This limitation overlooks higher-level information encoded in the original graph and potentially leads to information loss and biased graph understanding. We then implement this weight-level paradigm with low- rank adaptation (LoRA), proposing theGraph-aware LoRA generation method, named GaRA, including a LoRA-style weight generation module and a norm-rescaling module. • We propose a graph-aware weight generation method, GaRA, under the weight-level paradigm, which allevi- ates potential information loss and tackles the architec- tural optimization bias problem. Output-level Injection In these methods, LMs typically serve as text encoders, while GNNs further enhance the representations and make final predictions (Liu et al., 2023; Chen et al., 2024b). However, to ensure computational efficiency and training stability, these methods commonly freeze the GNN during LM tuning (Wang et al., 2024; Li et al., 2024a). In consequence, the resulting graph- dependent signals cannot be optimized for downstream objectives, which may lead to suboptimal representations and exacerbate distribution shifts between pre-training and downstream data (Wang et al., 2025). To address this prob- lem, GOFA (Kong et al., 2024) interleaves GNN layers into a frozen pre-trained LLM, adjusting the prompt sig- nal accordingly but also incurring intensive computational requirement. Beyond the characteristics discussed above, due to con- straints on model complexity and the goal of task unification, both GNN-based methods and graph compression strategies reduce all tasks to the graph level by extracting an n-hop subgraph centered on the target node or edge. This process overlooks higher-level information encoded in the original graph, potentially leading to information loss in downstream tasks. Different from these methods that solely operate at the input level, we propose a weight-level paradigm, injecting whole-graph information via weight generation. First, graph datasets typically contain much less training data than vision datasets, making weight generation 1ZeroG also applies message passing after the LM. Without loss of general- ity, given a weight matrix ˆW∈R p×q for a linear trans- formation Rp 7→R q, the general adaptation process can be formulated as an additive modification to the original weights (General)W= ˆW+ ∆W,(1) where ∆W denotes the weight update. To alleviate information loss, we propose a novel weight-level paradigm, injecting whole-graph information via weight generation and complementary to the input-level subgraph information. Each task is then represented by a tuple (G,G s, ttask, tlabel), where Gs ⊆ Gis the sub- graph associated with the task,ttask is a textual task descrip- tion specifying the prediction objective, and tlabel is the corresponding textual label. Consequently, when- ever increasing the norm of ∆W decreases the training loss, gradient descent will try to amplify ∥∆W∥. During the training process, we observe that while LoRA exhibits a stable training dynamics, the primary LoRA-style generation method leads to continuous norm amplification on the generated low-rank matrix. Therefore, the op- timizer can solely rely on the generated weight updates to reduce the training loss, making it easier to fit the fine-tuning dataset and thus preferred during optimization. Constraining Norm Amplification.To tackle the uncon- strained norm amplification bias during optimization, we propose a simple yet effective solution by fixing the average column norm of the generated low-rank matrix G to match that of the corresponding left matrix L. Model Training Strategy Following the common practice in graph compression strat- egy (Chen et al., 2024a; Wang et al., 2025), we adopt in- struction tuning during the model training. Baseline models include the output-level injection method OFA (Liu et al., 2023), input-level injection methods (GraphGPT (Tang et al., 2024), LLaGA (Chen et al., 2024a), TEA-GLM (Wang et al., 2024), GOFA (Kong et al., 2024), and UniGTE (Wang et al., 2025)), and a pre-trained LLM Vicuna-7B (Zheng et al., 2023). For node classifica- tion and link prediction tasks, methods that solely depend on the input-level injection can only encode subgraph infor- mation, where the whole-graph information is discarded. %).“Partial" denotes employing arXiv, BookChildren, AmazonComputer, and FB15K237 for tuning, where the first three datasets only involve node classification tasks. “Full" denotes employing arXiv, BookChildren, AmazonComputer, FB15K237, and ChEMBL for tuning, where the first three datasets involve both node classification and link prediction tasks. Results are averaged over the 50 randomly selected test samples, where the selection is refreshed every 50 training steps. Model variants are fine-tuned on arXiv, BookChildren, AmazonComputer, and FB15K237, where arXiv, BookChildren, and AmazonComputer contain only node classification tasks. To study the