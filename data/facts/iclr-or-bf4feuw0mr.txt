REAL METHOD of "DemoGrasp: Universal Dexterous Grasping from a Single Demonstration" (arXiv 2509.22149v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We propose DemoGrasp, a simple yet effective method for learning universal dexterous grasping. We formulate this trajectory editing as a single-step Markov Decision Process (MDP) and use RL to optimize a universal policy across hundreds of objects in parallel in simulation, with a simple reward consisting of a binary success term and a robot–table collision penalty. In simulation, DemoGrasp achieves a 95% success rate on DexGraspNet objects using the Shadow Hand, outperforming previous state-of-the-art methods. It also shows strong transferability, achieving an average success rate of 84.6% across diverse dexterous hand embodiments on six unseen object datasets, while being trained on only 175 objects. Recent studies have extensively investigated the use of RL for training universal dexterous grasping policies. However, many of these approaches train on hands without robot arms [35, 41], use privileged contact information as observations [16, 35], and face a trade-off between collision penalties and other complex reward terms [16, 41], limiting their potential for deployment on real robots. In addition, their reliance on complicated observation design, reward shaping, and multi-stage pipelines increases the barrier to extending these methods to new embodiments and task settings. In this research, we proposeDemoGrasp, a simple yet powerful framework for universal dexterous grasping that addresses these challenges. In our method, the RL policy explores how to edit the demonstration along these two axes, rather than exploring in the low-level robot action space as in prior methods, resulting in more efficient trial-and-error. For each trial, the Demo Editor policy takes observations at the first timestep and outputs an end-effector transformation and a delta hand pose. Demonstrations can also be used to augment RL, serving as reward signals [7, 22, 53], auxiliary losses [30], and training data in the replay buffer [25]. We define the initial object frame as a static coordinate frame obtained by translating the world frame to the object’s geometric center at the first timestep of the demonstration. Intuitively,{q∗hand t } forms a hand-pose sequence from open to close, and{p∗ee-obj t } is an end-effector trajectory that first approaches the object center (the origin of the initial object frame) and then lifts. (2) Here, Tlift denotes the first timestep at which the object’sz-position increases (i.e., it begins to be lifted) in the demonstration.∆ z is a constant vector in thez direction that lifts the object vertically afterTlift. 3.3 Single-Step Reinforcement Learning MDP reformulation.Given the grasp exploration scheme via demonstration editing, we reformulate the task as a single-step MDP: 3.4 Vision-Based Sim-to-Real After training the RL policy, we train a vision-based policy on its successful rollouts to enable sim-to-real transfer. We then train a Flow-Matching [23] policy with action chunking for imitation learning, modeling the multi-modal action distribution with high 6 quality. To close the visual sim-to-real gap, we perform domain randomization of colors, textures, lighting conditions, camera extrinsics, and table positions during data collection, and we finetune a pre-trained ViT [12] encoder for the visual representation. (1) the performance and scalability of our method through large-scale simulation with diverse object datasets and dexterous hand embodiments (Sections 4.2 and 4.3); (2) the sim- to-real performance of our method through real-world experiments with a wide variety of objects (Section 4.4); and (3) an analysis of the components of the proposed method (Section 4.5). For evaluations on DexGraspNet [36] with the Shadow Hand (Section 4.2), we train on 3,200 objects from the DexGraspNet training set to align with baseline settings. In all remaining sections, unless otherwise specified, we randomly sample 175 objects from the YCB dataset [4] and the DexGraspNet training set for training, and test on unseen objects from other datasets. We follow the set- tings of previous state-of-the-art methods—UniDexGrasp [41], UniDexGrasp++ [35], and UniGraspTrans- former [37]—training the 18-DoF Shadow Hand with a 6-DoF floating wrist on the training set of 3,200 objects. DemoGrasp surpasses the best baseline by 5% in state-based settings and 4% in vision-based settings on both training and test sets, and exhibits a minimal generalization gap of 1% between training and unseen objects, demonstrating strong learning and generalization performance. Notably, the baseline methods do not randomize object initial positions, whereas our method is trained and tested with a large reset region of50cm× 50cm, posing a challenge for spatial generalization. Benefiting from the translation invariance of our demonstration-replay mechanism (i.e., replaying a demonstration for the same object at different initial locations leads to the same grasp outcome), spatial randomization does not hinder RL exploration in our method, yielding strong spatial generalization. In addition, while baselines rely on complex reward designs (e.g., hand–object distance, object-lift, and hand-lift terms) to facilitate RL, our method uses a simple binary reward, highlighting the simplicity and effectiveness of our approach. We train the policy on 175 objects—75 randomly sampled from YCB [4] and 100 randomly sampled from the DexGraspNet training set [36]—and test on five out-of-distribution datasets: For Omni6DPose and ModelNet40, which consist of larger objects, 7 Table 1:Success rates on DexGraspNet with the Shadow Hand in simulation.Results are reported for both state-based and vision-based settings on 3,200 training objects (Train.), 141 unseen objects from seen categories (Test Seen Cat.), and 100 unseen objects from unseen categories (Test Unseen Cat.). Although trained on different object datasets, the test sets are unseen for both methods and thus form a fair comparison, since both aim at universal grasping over arbitrary objects. All multi-fingered hands achieve > 90%success on the 175 training objects and gen- eralize to unseen datasets with an average success rate of84.6%, indicating that our method extends easily to different hands rather than overfitting to a particular hand. Given the demonstration-editing scheme, we sample in the editing-parameter space for each object and position, execute the edited rollout in simulation, and train a behavior cloning (BC) policy on the successful rollouts. Table 5 