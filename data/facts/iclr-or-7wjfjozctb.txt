REAL METHOD of "No Prior, No Leakage: Revisiting Reconstruction Attacks in Trained Neural Networks" (arXiv 2509.21296v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Revisiting Reconstruction Attacks in Trained Neural Networks Yehonathan Refael1* Guy Smorodinsky2* Ofir Lindenbaum3 Itay Safran2 1Tel Aviv University 2Ben-Gurion University of the Negev 3Bar-Ilan University September 26, 2025 Abstract The memorization of training data by neural networks raises pressing concerns for privacy and security. Recent work has demonstrated that portions of the training data can be reconstructed directly from the parameters of a trained model, even without access to gradients or queries [15, 5, 21]. Unlike some of the previous methods that produced generic reconstructions resembling class prototypes or averages [7, 6], the new techniques generate highly accurate and specific reproductions of the original training data. Intuitively, when optimization succeeds under certain assumptions, such networks trained using standard techniques do not merely converge to any solution that fits the training set; instead, they converge to specific solutions that satisfy additional constraints. [15] constructs an objective function that is minimized when these constraints are satisfied and carries out their attack by optimizing it. To what extent do the constraints imposed by the implicit bias of trained neural networks leak information about the training data? In this paper, we address the above question by rigorously studying properties of the objective function used to fuel the reconstruction attack introduced by Haim et al. We propose a method that, given the original training data, enables the construction of multiple other global minima for the objective function used in the attack. Lastly, we empirically demonstrate that if an attacker initializes the attack far from the true training data, the reconstruction obtained contains instances that are significantly farther away from the training data as well. [15]) is crucial for conducting successful reconstruction attacks in this setting, and that the constraints imposed by the implicit bias alone do not necessarily leak information about the training data. Moreover, we demonstrate that under the assumption that the training data does not span the entire domain, there exist infinitely many such alternative training sets whose distance from the true training set can be arbitrarily large (Theorem 4). • In Subsection 3.2, we consider the more realistic setting in which the trained model only approximately satisfies the implicit bias constraints, and present merging and splitting techniques analogous to those mentioned in the previous bullet point (Lemmas 6 and 7). Finally, we demonstrate that even if the attacker possesses additional information about the training procedure, under the assumptions of structured data and a well-trained model, it remains possible to split the points in a way that preserves data confidentiality (Theorem 9). 2 Additional related work Reconstruction-based privacy attacks.An emerging line of work studies the possibility of extracting training data directly from trained models, raising serious privacy concerns. Their approach frames data recovery as minimizing a suitably defined objective whose solution can align with portions of the true training set. In practice, mechanisms enforce differential privacy by injecting carefully calibrated noise into data, queries, or training procedures [11, 1]. Preliminaries.The following well-established result, which has recently attracted significant attention, shows that homogeneous neural networks trained with logistic or exponential loss exhibit an implicit bias: Then, gradient flow converges in direction3 to a first order stationary point (KKT point [30]) of the following maximum-margin problem: It simultaneously adjusts the candidate training data X′ and their associated Lagrange multipliers λ to minimize a composite loss function. However, in practice, the margin width γ(θ) may take on a different scale, which, for later use, we denote by γ(θ) =p >0 .4 We remark that this method aims to reconstruct training data instances that are on the margin (namely, instances whose prediction value ispor−p), since otherwise, by Equation 4, they do not factor in the constructed objective. Including a prior would confound the evaluation of the method’s performance, as success would become dependent on the specificity of the external knowledge, making it difficult to disentangle the contribution of the mathematical constraints imposed by Equations 1-4. Framework.An attacker, motivated to reconstruct the training set S, is given a full access to the neural network (binary classifier) architecture Φ(θ;·), as well as complete knowledge of the weights θ.5 However, the attacker has no 3We say that gradient flowconverges in directionto ˆθiflim t→∞ θ(t) ∥θ(t)∥ = ˆθ ∥ ˆθ∥ . With this information, the attacker then optimizes the objective in Equation 5 to reconstruct the training set, yielding X′ = (x′ 1, . Following the definition of our loss function, we specify the corresponding set of examples that satisfy the conditions of Theorem 1, which the minimization of the objective in Equation 6 aims to find. While these technical results provide means to explore the loss surface of the objective in Equation 6, they do not provide a quantifiable assessment of the extent to which KKT sets can be manipulated by merging and splitting. ,xn} ⊂Rd be the training set, and let Φ(θ;x) a 2-layer neural network that was trained on S and reached a KKT point. ,xn} ⊂Rd be a KKT set, let Φ(θ;x) be a 2-layer, width-k neural network that was trained on S and reached a KKT point, and suppose that γ >0 and ν∈R d satisfy ⟨ν,x i⟩ ≤γfor all i. ,xn} ⊂Rd be an ε-KKT set for some ε >0, let Φ(θ;x) be a 2-layer, width-k neural network that was trained onS and reached an(ε, δ)-KKT point, and suppose thatγ >0 and ν∈R d satisfy ⟨ν,x i⟩ ≤γ for alli. in practice, some information about δ may leak, or the attacker may be able to deduce an upper bound on its value from the training dynamics.6 Because our splitting technique can degrade the value of δ, this upper bound introduces an additional constraint: we cannot split the training set beyond the attacker’s bound, which limits the allowable budget for altering training instances. ,xn} ⊂Rd be an (ε, δ)-KKT set for some ε, δ >0, and let Φ(θ;x) be a 2-layer, width-k neural network that was trained on S and reached an (ε, δ)-KKT point. For example, when the data domain consists of natural images, this corresponds to prior knowledge that the training set must lie with