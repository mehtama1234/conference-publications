REAL METHOD of "UltraMemV2: Memory Networks Scaling to 120B Parameters with Superior Long-Context Learning" (arXiv 2508.18756v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We present UltraMemV2, a redesigned memory-layer architecture that closes this performance gap. Our work brings memory-layer architectures to performance parity with state-of-the-art MoE models, presenting a compelling alternative for efficient sparse computation. Memory-layer architectures[2, 18, 26] offer an alternative sparse model with significantly less memory access. The Over-tokenized Transformer [16] can also be viewed as a memory-layer architecture, where an n-gram router activates embeddings from a memory table that are subsequently added to the word embeddings. We introduce UltraMemV2, a redesigned memory-layer architecture that bridges the performance divide between embedding-based and expert-based sparse models. tighter coupling between memory layers and Transformer blocks with memory layers in every block; (2)simplified value expansion: We present the first memory-layer architecture competitive with state-of-the-art 8-expert MoE models, closing a significant performance gap in sparse model research. We demonstrate UltraMemV2’s effectiveness at scale and establish design principles for activation density versus parameter count trade-offs, providing guidance for future memory-layer architectures. 2 Related Work MoE Architecture The concept of MoE was first introduced by Shazeer et al.[34]. Memory Layer Architecture The idea of a memory layer was first explored by Lample et al.[26] with the introduction of the Product Key Memory (PKM). Memory+[2] also made some improvements to the memory layer architecture. However, most of these memory layer architectures have only managed to match the performance of MoE models withone activated experts. The UltraMem[18] was an attempt to address the limitations of existing memory layer architectures. By introducing several innovative improvements, UltraMemV2 can achieve comparable performance to MoE models with eight activated experts, filling the gap in the current research on memory layer architectures. Given a hidden statex ∈ RDin, a gate with parametersK ∈ RN ×Din computes routing scores forN experts. The input hidden statex is first projected to a queryq via linear layerqrow and qcol. This query is then used to compute scores against multiple, smaller sets of factorized keys, row keys Krow ∈ RN ×Dk and column keysKcol ∈ RN ×Dk to retrieve values fromN 2 value pool by Product Quantizaion (PQ)[20]. Given a hidden statex ∈ RDi, scores for activated values are computed by Tucker Decomposed Query-Key Retrieval (TDQKR): (a) MoE layer; (b) Product Key Memory (PKM) layer; (c) UltraMemV2 layer. Then weighted sum pooling with Implicit Value Expansion (IVE) is conducted to generate the final output of memory layer: 1) Every transformer block contains an FFN layer and an UltraMem-V2 layer. 4 2) The multiple linear layers in Implicit Value Expansion (IVE) are removed and use only a single linear layer. Considering that IVE adds a linear layer for each head to remap, but mapping different embeddings to corresponding linear layers will increase additional non-computational operations, affecting the inference speed. 3.5 Improved initialization When using a normal distribution to initialize the UltraMem-V2 layer, the standard deviation must be carefully designed; otherwise, the training process is very prone to divergence. (1) After initialization, the variance of the output activation of the memory layer does not diverge with the increase in the number of layers; (2) The variance of the output activation should not be too large. 5 3.6 Auxiliary losses In this section, we introduce two auxiliary losses which areNOT used in UltraMemV2. Recall that the balance loss of MoE[38] is a constraint on the result of the router, while UltraMem can be thought of as a MoE with row/column routers, so we propose the balance loss of UltraMem following MoE. Given a batch B with T tokens,Count n is the number of times then-th row/column key is activated, then fn = Count n/T, p n = 1 T TX t Pn row/col, (20) where pn represents the average probability of then-th row/column key being selected. The proprietary training corpus comprises 3.9T tokens for PreTraining (PT) and 500B high-quality long context tokens for continued training (CT). 8 15 20 30 50 70 100 150 200 Consumed tokens (B) 2.0 2.2 2.4 2.6 2.8 3.0 3.2 3.4Loss Diff=0.02 Training Loss Curve (Smoothed) PEER Baseline 150 200 300 Consumed tokens (B) 0.20 0.22 0.24 0.26 0.28 0.30Accuracy Diff=0.03 Open-Benchmark PEER Baseline Figure 2 PEER ablation study on UltraMemV2-430M/5B. Ultimately, the gain in retrieval accuracy from the increased dimensionality of the retrieval vector marginally outweighs the negative impact of the larger candidate pool for retrieval. Although there is no obvious gain in the validation set loss after adding a certain number of layers, the model with more UltraMemV2 layers performs better in downstream tasks. Specifically, we pretrain the model with 1.6T tokens under constant learning rate, then continue training with 250B tokens under cosine decay learning rate. Tucker core penalty loss In contrast to the approach in UltraMem [18], which emphasizes the necessity of constraining non-maximum eigenvalues of the Tucker core to mitigate approximation errors, our empirical findings suggest that such a constraint may be superfluous during training. The model trained without the penalty loss maintains comparable accuracy to the baseline in the early training stages. it doubles with rankr = 2, and again doubles for each layer if, for instance, one value table is shared across four layers. Training loss (left) and downstream accuracy on Open-Benchmark (right) for UltraMemV2-430M/5B with and without balance loss (β = 0.001). Optimal Number of Shared Layers We then investigate the impact of the number of shared layersg within the Ring topology, specifically comparing S4-Ring, S9-Ring, and S16-Ring. Training loss difference (smoothed) showing NoRing topology achieves lower training loss. Training loss difference (smoothed) relative to baseline showing S4-Ring, S9-Ring, and S16-Ring all achieve lower training loss, with benefits saturating around 9 shared layers. We evaluate S6-Block, as withL = 24 layers, this creates 4 blocks of 6 layers, offering a comparable degree of value table accessibility to S9-Ring (where each layer accesses 9 tables, but tables are re-used across layers). Training loss difference (smoothed) showing S6-Block achieves comparable training loss improvements to S9-Ring while 