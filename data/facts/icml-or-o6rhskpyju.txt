REAL METHOD of "Spectral Collapse Drives Loss of Plasticity in Deep Continual Learning" (arXiv 2509.22335v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Analyzing a linearized ReLU network, we derive explicit ϵ-rank conditions for successful training and prove that the loss-weighted Gram matrix is spectrally equivalent to the Generalized Gauss- Newton approximation, thereby relating NTK dy- namics to Hessian curvature. Recent work has begun to connect plasticity loss to second-order properties, such as the stable rank of the Hessian or layer-wise spectral condi- tioning (Lewandowski et al., 2024b;a). We define Hessian spectral collapse, and identify it as a central mechanism underlying plasticity loss. Related Work Causes of Plasticity LossSeveral pathologies have been proposed to explain loss of plasticity in continual learn- ing, both supervised and reinforcement (Lyle et al., 2023; Klein et al., 2024). Mitigating Plasticity LossExisting methods for mitigat- ing loss of plasticity target one or more of the aforemen- tioned pathologies (Klein et al., 2024). Approximate Second-Order MethodsOur work con- tributes to a recent line of inquiry that connects loss of plasticity to second-order information. Deep Neural NetworksA multi-layered-perceptron (MLP), also called a feedforward neural network, is con- structed by composing L functions, i.e., F L ◦ · · · ◦F1, one per layer. The dimension at layer l indicates the number of neurons at that layer. Given such a task τ, a learning algorithm seeks parameter values that minimize the expected loss Lτ (θ) .= Edτ [ℓτ (zτ ,y τ )]on the task, wherez τ =F θτ (xτ ). The gradient of the network is the vector of first deriva- tives of the loss function w.r.t. Together, the Jacobian and the Hessian cap- ture up to second-order changes in the loss, i.e., when the parameters change by a vector δ∈R P, the loss can be ap- proximated as Lτ (θ+δ)≈ Lτ (θ)+∇ θ L⊤ τ δ+ 1 2 δ⊤∇2 θ Lτ δ. 3 Spectral Collapse Drives Loss of Plasticity Neural Tangent Kernel (NTK)Given the the complex- ity of deep neural networks with non-linearities, the neural tangent kernel (NTK) has been proposed as an abstraction to characterize the training dynamics of (stochastic) gra- dient descent in the infinite-width limit (i.e., M→ ∞) (Jacot et al., 2018). Since evaluation metrics may be non-differentiable, we fur- ther assume a (possibly regularized) surrogate expected loss function Lτ :R P →R , for each task τ, which is intended to maximize the evaluation metric, but is generally better be- haved. A solution to the continual learn- ing problem is generated by an algorithm A:R P →R P, which outputs parameters for the current task τ that opti- mize τ’s surrogate loss function Lτ, given the parameters learned during the previous task, i.e., the parameters for task τ+ 1 are initialized as the learned parameters of task τ: Definition 4.1(Successful Training).In a continual learn- ing problem, given finite update budget K and a small error tolerance υ, we write I(ℓτ (θ(K) τ )≤υ) to indicatesuccess- ful trainingon task τ. Denote the first-layer weights by W[1] = [w [1] 1 , . We fix the output weights and the ReLU gates, so the only trainable parameters in this linearized model are the first-layer weights. We fix W[1](0) to be a frozen reference copy of the ini- tialized first-layer matrix W[1]. We define the squared-error loss as ℓτ (θ) .= 1 2 ˆFθ (Xτ )−Y τ 2 . In continual learning, where each task is trained for a finite number of steps K, θ(0) τ+1 .=θ (K) τ . 4 Spectral Collapse Drives Loss of Plasticity Next, we present two lemmas that enable us to define suc- cessful training based on ϵ-rank. If a component of the initial residual lies in the slow eigenspace, then after K gradient steps at least a γ fraction of that component remains in that slow subspace. Lemma 4.7(Necessary fast-projection condition).Using the fast and slow projections, ϵK,γ , and the initial loss residual r(0) τ , a necessary condition to achieve successful training on taskτisγ PS τ r(0) τ ≤ √2υ. This implies the expected MSE loss and frozen-gates Gram matrix is task-invariant under fresh pixel permutations, up to finite-width fluctuations, which means that loss of plasticity isnotcaused by permutations. Given a specific parameter, [W[l]]i,j in layer l and another parameter, [W[l′]]i′,j′, in l′ ̸=l) , in practice ∂2L/∂[W[l]]i,j ∂[W [l′ ]]i′ ,j′ is approximately 0 (Becker et al., 1988), where L is the loss. Empirically, however, the loss-weighted Gram matrix, GL Wdoes undergo spec- tral collapse, and its ϵ-rank tracks the loss of plasticity due to the loss-weighted geometry that governs classification dynamics. For larger models, selected-layer regularization, bottleneck-layer regulariza- tion, randomized or truncated SVD, and sketching methods may be needed. We do not yet have language-model results; extending L2-ER to such models will likely require apply- ing the penalty only to selected representations, adapters, or other low-dimensional modules, and attention layers may require a more specialized curvature approximation. Successτ+1 .=  ℓτ (θ(K) τ+1 )≤υ  =  ∥r(K) τ+1 ∥≤ √ 2υ  .(54) 19 Spectral Collapse Drives Loss of Plasticity Lemma 4.7(Necessary fast-projection condition).Using the fast and slow projections, ϵK,γ , and the initial loss residual r(0) τ , a necessary condition to achieve successful training on taskτisγ PS τ r(0) τ ≤ √2υ. In addition to BP, we collect the output features of each dense layer (excluding convolutional layers) over a fixed number of steps (er-batch in Table 1). From these stacked features, we compute the effective rank (Roy & Vetterli, 2007) for each layer and sum across all layers. Every L steps, these stacked features are used to compute the effective rank (Roy & Vetterli, 2007) of the representation at each layer. Note that a gradient descent step is taken on this loss only every L steps. 26 Spectral Collapse Drives Loss of Plasticity Algorithm 1Continual Learning with Effective-Rank Regularization (L2-ER) Input:Task datasets {{(xτ,i ,y τ,i )}N−1 i=0 }T−1 τ=0 ; model Fθ ; learning rates α (task), β (ER); weight decay λ; ER update intervalU(in steps) State:Per-layer feature buffers{B ℓ}L ℓ=1 with capacityU forτ= 0toT−1do fori= 0, . Network Architecture We employ a standard multilayer perceptron (MLP) with three hidden layers of hidden size 1000 each followed by a ReLU activation. From top to bottom, the algorithms are ordered by increasing accuracy.Top:Algorithms that lose plasticity (ER, BP).Bottom:Algorithms that preserve plasticity (L2, CBP,L2-ER). 38 Spectral Collapse Drives Loss of Plasticity Algorithms thatlose plasti