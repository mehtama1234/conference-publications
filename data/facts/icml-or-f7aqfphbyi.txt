REAL METHOD of "Batch Normalization for Neural Networks on Complex Domains" (arXiv 2605.00467v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Among those, Riemannian batch normalization (BN) layers have shown to enhance training sta- bility and improve accuracy. In this paper, we propose BN layers for neural networks on com- plex domains. We conduct experi- ments on radar clutter classification, node classifi- cation, and action recognition demonstrating the efficacy of our method. • We propose a general BN layer for neural networks on complex domains. While such spaces are the focus of our work, this layer is also applicable to Riemannian manifolds widely used in machine learning tasks, and is closely related to existing Riemannian BN layers. • We provide experimental evaluations showing that the proposed method yields systematic improvements with respect to existing methods. Proposed Approach In this section, we propose a BN algorithm on complex domains. We then compute an almost geodesic between 0M andϕ x(y)by solving the following equation: Algorithm 1BN on Complex Domains Training Phase Input:Batch of k points {xj}k j=1 on M, running mean mr, biasg, momentumη. Connection with Existing BN Layers Our method can be seamlessly adapted to the Riemannian and Lie group settings considered in previous works (Brooks et al., 2019; Chakraborty, 2020; Lou et al., 2020; Kobler et al., 2022a;b; Chen et al., 2024; 2025). In this section, we first show the connection of the proposed BN layer with some existing BN layers. Then, we point out that our method can also be used in the framework of (2025b) to build a BN layer (RSSBN) for neural networks on Rieman- nian symmetric spaces (RSS). In particular, our BN algorithm shares the same operations as these algorithms in three steps: Proposition 4.2 shows that when the scaling operations are removed from the BN algorithms in (2020; 2024), they share the same operations as our algorithm in the three steps. In particular, if for any x∈D and t∈[0,1] , the automorphism satisfies the property that ϕ0D (tx) = tϕ0D (x), then the curveα x,y can be given as αx,y(t) = 1 q(ϕx(y)) (1 +q(ϕ x(y)))t −(1−q(ϕ x(y)))t (1 +q(ϕ x(y)))t + (1−q(ϕ x(y)))t . However, the authors of (2025a) only developed two building blocks for those networks, i.e., fully-connected (FC) layer and multinomial logistic regression (MLR) layers. Here we build the first BN layer for Siegel neural networks. The work in (2019) was among the first which introduced BN layers for SPD neural networks. Since these layers only use the sample mean to normalize data, the sample variance was then added to the normalization operation to provide better control on the sample statis- tics (Kobler et al., 2022a;b; Chakraborty, 2020). Our proposed BN layer is used as the first layer. The last layer is the QMLRSym+ n ×SHr−1 n layer proposed in (2025a) for classification. This shows the effectiveness of our proposed BN layer compared to FC layers in (2025a). Since many building blocks for hyperbolic neural networks (HNNs) have been developed (Shimizu et al., 2021; Bdeir et al., 2024), the focus of this experiment is to compare our proposed BN layer against BN layers for HNNs, and we do not aim to build a state-of-the-art HNN architecture for the considered task. The encoder of HNN-GyroBN-H has a number of blocks, each of which is composed of a linear layer (HypLinear), a BN layer (GBN), and an activa- tion layer (HypAct) built on the Poincar´e ball. Our network CBallNetBN is constructed by replacing the GBN layer with our BN layer. Specifically, CBallNetBN converts the output of the HypLinear layer to Bn using the Discrete Fourier Transform (DFT) (Xiao et al., 2022), then applies our BN layer, and finally converts its output to the Poincar´e ball us- ing the inverse DFT. 2 show that our BN layer outperforms both the GBN and RBN layers by large mar- gins. Ablation Study Impact of the BN layer in SiegelNetBNWe remove the BN layer from SiegelNetBN and evaluate the performance of the resulting network SiegelNetNoBN on the task of radar clutter classification. As can be observed, our BN layer exhibits benefit similar to BN layers Table 3.Impact of the BN layer in SiegelNetBN. Method D1 D2 D3 D4 SiegelNetNoBN29.37±0.4838.93±0.5136.56±0.4562.53±0.54 SiegelNetBN 33.83±0.3443.29±0.4041.38±0.3765.93±0.32 Table 4.Impact of the BN layer in CBallNetBN. Impact of the BN layer in CBallNetBNWe conduct the same ablation study for CBallNetBN by removing the BN layer from it and evaluate the performance of the resulting network CBallNetNoBN on the task of node classification. (6) now becomes (a, b) = arg min a,b∈Rn(n+1)/2 kX j=1 d2 SDn (xj, φ(φ1(a) +iφ 2(b))).(10) We use the methods in (2021; 2025a) to compute the eigenvalues and inverses of points on Siegel spaces. For Kernel-Siegel which is based on kernel density estimation (KDE), we use the method in (2017) and the one in (2016) for KDEs on Sym+ n and SDn, and then combine them to obtain a KDE on the product space Sym+ n ×SDr−1 n . Our networks are implemented in the Pytorch framework and trained using cross-entropy loss and Adadelta optimizer for 2000 epochs. 1 of the main paper along with those of SiegelNetKobayashiBN, which is trained by replacing the K ¨ahler distance dSDn (·,·) with the Kobayashi distance dK SDn (·,·) for the computation of the Fr´echet mean in the BN layer of SiegelNetBN. Method D1 D2 D3 D4 D5 D6 SiegelNetNoBN29.37±0.4838.93±0.5136.56±0.4562.53±0.5440.18±0.1932.68±0.42 SiegelNetBN 33.83±0.3443.29±0.4041.38±0.3765.93±0.3245.61±0.1237.10±0.26 Table 10.Results (mean accuracy ± standard deviation) computed over 10 runs for node classification using 8-dimensional and 32-dimensional node embeddings. DimensionMethod Airport Pubmed Cora 8 HNN-RBN-H (Chen et al., 2025)62.34±0.6862.50±2.3629.63±7.55 HNN-GyroBN-H (Chen et al., 2025)70.38±2.4061.46±3.6341.33±2.94 CBallNetBN 74.42±3.4371.22±0.7658.46±1.11 32 HNN-RBN-H (Chen et al., 2025)60.49±2.9067.81±2.3141.29±4.80 HNN-GyroBN-H (Chen et al., 2025)78.79±2.0167.19±2.3343.43±1.99 CBallNetBN 80.24±1.3573.16±0.4661.08±1.17 splits for Airport dataset and standard splits (Kipf & Welling, 2017) with 20 train examples per class for Pubmed and Cora datasets. The networks are implemented in the Pytorch framework and trained using cross-entropy loss and Adam optimizer. Action Recognition To further demonstrate the applicability of our method, we conduct action recognition experiments on NTU60 (Shahroudy et al., 2016) and NTU120 (Liu et al., 2019) datasets. We use the method in Section B.1.2 to compute the input data of our networks. All networks are trained usi