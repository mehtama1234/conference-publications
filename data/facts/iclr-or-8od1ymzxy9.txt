REAL METHOD of "Alignment through Meta-Weighted Online Sampling: Bridging the Gap between Data Generation and Preference Optimization" (arXiv 2509.23371v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

To bridge this gap, we propose Meta-Weighted Adaptive Preference Optimization (MetaAPO), a novel framework that dynamically couples data generation with model training. This guides targeted online generation and assigns sample-wise meta-weights to the optimization objective, dynamically balancing the quality and distribution of online and offline data. These methods reparameterize the RLHF objective to directly optimize the policy using pre-collected offline preference datasets, simplifying the training process and improving efficiency. To improve the quality and distribution of training data, existing methods for mitigating distribution shifts generally fall into two categories. Some methods improve the reliability of offline data by filtering out low-quality or OOD samples using manually designed metrics, such as average ∗Corresponding author 1 arXiv:2509.23371v2 [cs.CL] 27 Feb 2026 Published as a conference paper at ICLR 2026 Offline DatasetLLM ResponsesGenerate LLMOffline & Online Dataset Generation Train in g + Meta-Learner Adaptive Sample WeightedTrain Hi,… Figure 1:Left:Overview of MetaAPO. The meta-learner adaptively assigns weights by evaluating offline data, guiding both targeted online sample generation and training on the weighted combination of offline and online samples.Right:Performance (left y-axis, line plots) and online generation and annotation ratio relative to Online DPO (right y-axis, bar plots) across training iterations for different methods. the current policy first generates responses, which are then annotated by a reward model to collect preference data for direct preference alignment. In addition, the meta-learner adjusts the training process by assigning sample-wise meta-weights to a hybrid loss function, adaptively balancing the contributions of offline data and online samples according to their respective quality and distribution advantages. • We propose MetaAPO, an extensible alignment framework compatible with various preference optimization methods. 2 Published as a conference paper at ICLR 2026 • Our method employs a meta-learner that adaptively assigns sample-wise weights to data. guiding online data sampling based on the model’s training needs to mitigate distribution mismatches and adjusting the training by emphasizing beneficial samples. Consequently, various data selection methods aim to identify the most informative and helpful samples from general-purpose datasets to improve training effectiveness (Wang et al., 2025; Khaki et al., 2024; Badrinath et al., 2024). DICE (Chen et al., 2024) and SeRA (Ko et al., 2024) combine data augmentation with filtering methods, guided by length regularization and reward margins to enhance training data. The reward model rψ(x,y) is trained using the Bradley–Terry (BT) model (Bradley & Terry, 1952), 3 Published as a conference paper at ICLR 2026 by minimizing a negative log-likelihood loss that encourages larger reward gaps between preferred and dispreferred responses: The policy is then fine- tuned on the hybrid dataset using the meta-weighted objective to obtain the updated policy πθ(t) (Section 4.2). Given a prompt x and a pair of preferred and dispreferred responses (yw,y l), the instance-level preference score is computed as: Suppose we consider M objectives (e.g.,helpfulness, harmlessness, and honesty), each inducing its own partial preference relation and corresponding offline preference loss ℓoff i (·), where i∈ {1, ..., M}. We then apply objective-wise label enhancement (Xu et al., 2019; 2023) by assigning an adaptive continuous label degree for each objective: The overall objective is to minimize the meta-weighted preference loss: L(θ) =−E (x,yoff pair,yon pair)∼Daug h w·ℓ θ(x,y off w ,y off l )| {z } adaptive offline loss + (1−w)·ℓ θ(x,y on w ,y on l )| {z } adaptive online loss i , (5) where ℓθ(·) denotes a standard preference alignment objective (e.g.,DPO in Eq. To demonstrate the general effectiveness of our method, we experiment with two supervised fine-tuned models from different-sized base model: Evaluation Metric.We evaluate our method using three widely adopted automatic evaluation benchmarks for LLMs: Baselines.We evaluate the following baseline methods relevant to the setting of our method: For each prompt, we generate K= 8 diverse responses, which are then scored by a reward model to construct preference pairs. Overall performance of our proposed MetaAPO method with Llama-3.1-8B and Qwen2.5- 7B, compared with offline, online and hybrid baseline methods on AlpacaEval 2, Arena-Hard and MT-Bench. While online and hybrid methods significantly outperform purely offline baselines by leveraging on-policy feedback, they are often constrained by the quality and diversity of their own model genera- tions. We also compare performance across different reward models, preference optimization methods and datasets in Appendix C.2, demonstrating the robustness of our approach. 5.3 FURTHERANALYSES Training dynamics reveal an explore-integrate behavior.To examine the dynamic interplay during training, we tracked the offline preference scoreℓoff and generation reward on a held-out test set across iterations. 5 with auniform coefficientof 0.5 for all augmented samples, assigning equal importance to offline and online preference pairs during training. 6 CONCLUSION In this paper, we propose MetaAPO, a novel framework that bridges the gap between data generation and model training in LLM alignment. To achieve this, we introduce the techniques ofadaptive online sampling,meta-weighted optimizationandmeta-learner update, which jointly enable dynamic integration of data and training. The gradient update will then tend to adjust ϕ such that hϕ(ℓoff(·)) (the weight assigned to the offline sample) decreases. First, we establish a uniform convergence bound between the empirical and true risk of the meta-learner, using the Rademacher complexity of the meta-loss hypothesis class. A.2.1 PRELIMINARIES Definition 1(Meta-Loss Function).For a meta-sample (x,y off pair,y on pair) and a meta-learner hϕ, the meta-loss is defined as: A.3 METAAPOWITHSIMPO OBJECTIVE The MetaAPO framework is designed to be largely agnostic to the choice of the underlying prefer- ence optimization loss, enabling it to be applied in conjunction with various alignment algorithms. The original objective of SimPO involves minimizing the following loss function: allenai/Llama-3.1-Tulu-3-8B-SFT based on the Llama architecture, and FuseAI/FuseChat-Qwen-