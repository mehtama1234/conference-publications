REAL METHOD of "Delving into Non-Exchangeability for Conformal Prediction in Graph-Structured Multivariate Time Series" (arXiv 2605.04957v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Experi- mental results on real-world traffic datasets show that SCALE not only achieves valid coverage but also consistently improves the coverage-efficiency trade-off over the state-of-the-art CP methods. To address the non-exchangeability problem, recent CP methods, such as Conformal Prediction for Time Series (CPTS) (Xu & Xie, 2023a) and Sequential Predictive Conformal Inference (SPCI) (Xu & Xie, 2023b), propose to design sequential or adaptive calibration techniques. These procedures typically rely on the identification of (approximate) exchangeability, which is further leveraged to recover prediction errors or nonconformity scores (Wu et al., 2025). 1 arXiv:2605.04957v1 [cs.LG] 6 May 2026 Delving into Non-Exchangeability for Conformal Prediction in Graph-Structured Multivariate Time Series The non-exchangeability challenge becomes even more se- vere forgraph-structuredmultivariate time series (MTS), where observations at each time step form a coupled random vector and cross-node interactions can induce system-level dependence. Based on this insight, we introduce the concept of Spectral Graph Conditional Exchangeability (SGCE), which formal- izes that the high-frequency components can be approxi- mately exchangeable when conditioned on low-frequency ones. Here, the conditioning preserves the global trend in the evolution of time series, promoting a more precise pre- diction for each individual series. Building upon SGCE, we propose a practical implementation named Spectral Confor- mal prediction via wAveLEt transform (SCALE). It then conformalizes high-frequency residual scores via adaptive gating on low- frequency components to produce prediction intervals. Com- pared with prior CP approaches (Cini et al., 2025) that pur- sue exchangeability via cross-variable encoding under the assumption of sparse dependence, our approach models joint exchangeability through spectral conditioning, providing a new paradigm for CP in graph-structured MTS. • Algorithmically, we develop SCALE, a graph-wavelet conformal prediction framework that adapts conformal quantiles to calibrate high-frequency components condi- tioned on low-frequency ones. At each time step, the corresponding snapshot is decomposed into multiple frequency bands (right panel), where components with higher frequencies tend to be more exchangeable due to weaker cross-node interactions. • Empirically, we demonstrate an improved coverage– efficiency trade-off on real-world traffic datasets when compared with state-of-the-art baseline methods for con- formal prediction in graph-structured MTS forecasting. Prior work on STGNNs explores heterogeneous and time-varying dependencies via adaptive/dynamic adjacency, attention- based spatial mixing, and multi-component spatio-temporal blocks (Guo et al., 2019; Zheng et al., 2020; Wu et al., 2020). This motivates sequential, online, and adaptive conformal procedures (Xu & Xie, 2023a;b; Wu et al., 2025; Li & Rodr ´ıguez, 2025). The model Fθ is typically trained to minimize a deterministic loss function ℓ(Xt:t+T , bXt:t+T ), e.g., the mean squared error (MSE) loss or mean absolute error (MAE) loss (Lim & Zohren, 2021). Given a pre-trained predictor Fθ, our goal is to perform post-hoc uncertainty quantification. At time step t, SCP computes the residual error ri t =x i,cal t −bxi,cal t , where bxi,cal t is the output of the predictor Fθ. In view of this, we resort to spectral graph theory to address this limitation, and the critical techniques used in our method are introduced in the next section. Methodology In this section, we present the motivation for our study and describe the proposed SCALE method in detail. (c) Low-Frequency Channel:A lightweight learnable encoder maps the low-frequency trends into a structural conditioning embedding Ct.(d) High-Frequency Channel:A parameter-free extractor computes temporal statistics Mt, avoiding the reintroduction of spatial couplings. The frequency components subsequently serve as inputs in our method. The entire network is trained end-to-end to minimize the pinball loss, i.e., the quantile regression loss (Koenker & Bassett Jr., 1978) ℓα(y,bq) = max{α(y−bq),(α−1)(y−bq)} , which asymmetrically penalizes under- and over-estimation at miscoverage rate α. For ∀α∈(0,1) , let bCX,α t+1:t+K denote the prediction interval of our SCALE method, and then it holds that P  Xt+1:t+K ∈ bCX,α t+1:t+K  ≥1−α.(14) Theorem 5.1 guarantees that our framework maintains finite- sample validity regardless of the distribution shifts in the low-frequency trends Lt, provided that the high-frequency components Ht remain conditionally exchangeable. Coverage, PI-Width, and Winkler are reported as mean±std across seeds for trainable methods. In the first stage, we adopt STGNN (Cini et al., 2025) for align- ment, and we report results with the alternative backbones in Appendix E.1 to demonstrate the generality of our method. For the multi-step analysis, we additionally includeConForME(Galv ˜ao Lopes et al., 2024), a multi-horizon conditional conformal method. Since our method jointly performs SGWT decom- position and low-frequency gating, it maintains reliable coverage while avoiding overly conservative intervals. These methods output full-horizon quantiles in one model, enabling a clean per-horizon comparison, and single-step baselines are omitted to avoid extra design choices. CoREL is especially flat because it relies on a single latent state that is taken from the last encoder step and uses a shared linear readout for all horizons, which homoge- nizes per-step behavior. Building on SGCE, we propose a method named Spectral Conformal prediction via wAveLEt transform (SCALE). 10:ifτis giventhen 11:Setkto the first index with smoothed correlation> τ. We define δdep as the TV distance between the joint distribution of the actual residuals ΠP (˜r)and the closest exchangeable product measureΠ Q(˜r), namely δdep ≜d TV(ΠP (˜r),ΠQ(˜r)).(C.15) Step 4: All interval methods operate on the same residual tensors produced by the first stage backbone and share the same calibration/test split. The first stage Backbone and Residual Generation Backbone architecture.The point forecaster uses a GRU + DiffConv template with one GRU-based temporal layer and two graph message-passing layers, using hidden size 32 and node embedding size 16 with elu activation. The architecture consists of a linear input encoder, a GRU temporal encoder, stacked DiffConv layers, and a linear readout. For trainabl