REAL METHOD of "When Flatness Does (Not) Guarantee Adversarial Robustness" (arXiv 2510.14231v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

To arrive at this result, we first derive a closed-form expression for relative flatness in the penultimate layer, and then show we can use this to constrain the variation of the loss in input space. We then show that to maintain robustness beyond a local neighborhood, the loss needs to curvesharplyaway from the data manifold. One property that has long attracted attention is the flatness of the loss surface. At first glance, this appears to be disconnected, since adversarial examples concern the change of the loss with respect to the input, while flatness quantifies the change with respect to the weights. ℓ(f(w(x+δx)), y) =ℓ(f(wx+wδx), y) =ℓ(f((w+wδ)x), y) =ℓ(f( ˆwx), y)∼= ℓ(f(wx), y) The last step follows from the loss surface being flat. Intuitively, we show that under theory-friendly conditions, flatness in feature space constrains how much the loss can change in input space. To this end, we derive a closed-form expression for relative flatness in the penultimate layer for the cross-entropy loss. By bounding the third derivative of the loss, we show that flatness at the penultimate layer induces a lower bound on the radius within which the loss remains approximately constant in feature space. (2017), who showed that small-batch training tends to converge to flatter minima with lower test error, while large-batch train- ing leads to sharper, less generalizable solutions. These insights inspired a range of optimization techniques aimed at biasing training towards flatter regions of the loss surface. Methods like Entropy-SGD (Chaudhari et al., 2019), Stochastic Weight Averaging (SW A)(Izmailov et al., 2018), SW AD (Cha et al., 2021), and Sharpness-Aware Minimization (SAM) (Foret et al., 2021) explicitly or implicitly minimize sharpness and have been shown to improve generalization across various architectures and datasets. 3.2 LOSS-BASEDDEFINITIONS OFADVERSARIALEXAMPLES ANDROBUSTNESS In this work, we connect the geometry of thelosssurface with adversarial robustness. The classical definition of adversarial examples (Definition 1) only incorporates the loss implicitly through the prediction flip, making it unsuitable for directly analyzing how robustness relates to the structure of the loss landscape. Analogous to Definition 2, we define robustness against adversarial loss changes. Definition 5(Loss-change robustness).Letf:X → Ybe a classifier,l:Y × Y →R + a loss function, and(x, y)∈ X × Ya sample. Definition 6(Loss-change dataset robustness).Given a datasetS⊆ X × Y, a classifierfis(δ, ϵ)- robust overSif it is(δ, ϵ)-robust at every(x, y)∈Saccording to Definition 5. This loss-based formulation is strictly stronger, though, as it requires small loss under perturbation even for high-confidence predictions—enforcing a stricter, uniform standard across the dataset. Analytical methods quantify curvature, using the trace or eigenvalue spectrum of the Hessian of the loss (Hochreiter & Schmidhuber, 1994). Importantly, they informally argue that it is sufficient to compute this quantity at thepenultimate layer, avoiding the need to analyze the full network. Formally, given a network decomposed into a feature extrac- torϕ:X →R d mapping to the penultimate layerw∈R k×d and a classifiergapplying softmax, i.e.,f(x) =g(wϕ(x))relative sharpness is defined as, κT r(w) :=∥w∥ 2T r(H(w, S)),(1) whereT rdenotes the trace, andHis shorthand for the Hessian of the loss computed onSwrtw: Although this derivation explicitly applies to the penultimate layer, we will demonstrate that this geometry propagates to earlier layers. Why the last layer is sufficientFor any recitified affine neural network composed of linear, convolutional and BatchNorm layers, curvature propagates cleanly through the architecture. This structure reveals that all curvature in hidden layers is inherited from the output layer. As a result, any sharpness measure based on the Hessian is strongly influenced by the final layer and its immediate input, justifying our focus on the penultimate layer in what follows. To achieve this, we first connect input perturbations to changes in the output, then relate these changes in feature space to parameter perturbations, i.e., relative sharpness. Since we can expressϕ(ξ)asϕ(x) + ∆Aϕ(x), we can then use the linearity of the representation layer to relate the perturbation of input to a perturbation in weights. Bounding loss increaseNext, we bound the loss difference betweenξandx. With this we can bound the difference between the loss suffered on an adversarial exampleξand the loss on a clean examplex for a converged model as follows. Proposition 9 precisely reveals how relative sharpness constrains the extent to which the loss can change under input perturba- tions. by scaling the penultimate layer weightswby a factorsi.e.w s =sw, we can directly control the loss surface curvature without retraining. This method allows us to precisely control sharpness in a systematic and reproducible manner, in contrast to SAM-based training approaches. First, we examine whether the curvature properties at the penultimate layer propagate throughout the network, validating our derivations in Section 4 and our focus on this layer as representative of the global loss landscape. 6.1 SHARPNESSPREDICTSLOSSSENSITIVITYUNDERADVERSARIALPERTURBATIONS We now empirically test our core claim that relative sharpness governs local loss sensitivity and shapes adversarial robustness. Setup.To this end, we generate adversarial examples using a weaker attack PGD-ℓ 2 (25 steps, ϵ= 0.025, step sizeα= 0.001). Loss Increase Analysis.Figure 2a shows the histogram of loss increases between clean examplesx0 and final adversarial iteratesx 25. For small scaling factors (e.g.,s= 0.5), the loss increases steadily from the first iteration, while for large factors (e.g.,s= 50), the loss stays almost constant throughout the entire trajectory until a sharptake-off pointwhere it suddenly strongly increases, which marks the boundary of the basin. On the PGD-trajectory, sharpness quickly peaks and then decreases to zero, while loss keeps increasing. In the previous experiments, we validated our theoretical derivations showing that relative sharpness governs local loss sensitivity, with flatter networks exhibiting limited loss vari- ation under perturbations. Traditional prediction-flip definitions of adversarial examples proved insufficient for this analysis, so we introduced a loss-based formulation (Definition 4) that enables direct analysis of ho