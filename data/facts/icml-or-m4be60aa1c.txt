REAL METHOD of "TN-SHAP-G: Graph-Structured Tensor Network Surrogates for Shapley Values and Interactions" (arXiv 2606.01540v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We propose TN-SHAP- G, a framework that exploits structure in graph- structured inputs to compute Shapley values and higher-order interaction indices efficiently. Once trained from a small number of oracle queries, the surrogate enables deterministic recovery of first- and higher-order Shapley indices via the multilinear extension, without additional model queries or Monte Carlo variance. Architecture-specific methods (e.g., GNNEx- 1Universit´e de Montr ´eal, Montr ´eal, Quebec, Canada 2Mila, Quebec AI Institute, Montreal, Quebec, Canada 3CIFAR AI Chair. Once trained from a small number of model queries, the surrogate enables deterministic recovery of Shapley values and interaction indices on the learned surrogate via closed- form integration, without imposing architectural constraints on the predictor. A single trained surrogate amortizes computation of all first- and higher-order Shap- ley quantities. For any coalition S⊆V , define the masked feature matrixX S ∈R n×d by (XS)v := ( xv, v∈S, x0 v, v /∈S, (1) and the induced cooperative game ν(S) :=f(G, XS).(2) Although we focus on node-level explanations (players v∈ V ), the same construction applies when players correspond to edges or subgraphs by redefining the masking operator accordingly. By uniqueness of the multilinear extension (Ap- pendix A.1), a surrogate that fits all coalition values yields exact Shapley quantities; in practice we fit from M≪2 n samples (Theorem 3.6) and then compute attributions deter- ministically from the surrogate (Section 3.3). For each node u∈V , we introduce a core tensor A(u) ∈R 2×χ×···×χ,(9) of order |N(u)|+ 1 with onephysicalindex of dimension 2 encoding coalition membership su ∈ {0,1}and |N(u)| bondindices of dimension χ, one per neighbor v∈ N(u). The contraction sums over all bond indices and returns a scalar for each coalition assignment s∈ {0,1} n, defin- ing an implicit approximation bT of the coalition tensor. If each edge e∈ δ(A, B)has bond dimensionχ e, then rankA|B(T)≤ Y e∈δ(A,B) χe.(11) In particular, if a single bond of dimension χ separates A andB, thenχ≥rank A|B(T). Then any tensor-train representation of T generically requires TT-ranksΩ(χ2), and approximation with smaller ranks incurs error lower-bounded by the truncated singular value tail of the corresponding matricization. The training dataset S= {Sj}M j=1 consists of coalitions S⊆V labeled with teacher evaluations yj =ν(S j). Notably, the same surrogate supportsallfirst- and higher- order quantities, whereas sampling-based estimators typi- cally require separate Monte Carlo budgets for each order. COMPUTATIONAL COMPLEXITY Given a trained surrogate, the cost of computing Shapley values and interaction indices is dominated by tensor net- work contraction and polynomial interpolation. The graph- aligned structure of the surrogate allows both first- and second-order attributions to be computed efficiently by ex- ploiting locality in the interaction graph. (i) all first-order derivatives{∂zu ˆν(t1)}u∈V can be com- puted inO(|V|χ τ+1 )total time; (ii) all edge mixed derivatives{∂zu ∂zv ˆν(t1)}(u,v)∈E can be computed inO(|V|χ τ+1 )total time. TN-SHAP-G scales efficiently to larger graphs (the coarsening architecture; Appendix F.7), with practical training and deterministic attribution times (Fig. 100 200 300 400 600 Number of Nodes 102 103 Training Time (s) T ~ n^2.30 (a)Training time for learning the graph-aligned TN surrogate as a function of graph size. 100 200 300400 600 Number of Nodes 100 3 × 10 1 4 × 10 1 6 × 10 1 Shapley Time (s) T_Shapley ~ n^0.57 (b)Time to compute all node Shapley values from the trained surrogate via TN-SHAP-G. Limitations and Future Work The current implementation assumes a fixed baseline masking scheme and focuses on node-based players, though the framework extends to edge- and motif-level players. introduction to methodology and encoding rules.Journal of chemical information and computer sciences, 28(1):31–36, 1988. We then derive the multilinear-extension contraction identity that underpins our deterministic Shapley computation, and show how partial derivatives are computed efficiently via local gate replacement. Tensor trains (TT).A tensor train (TT) is a TN topology in whichTis factorized as Ti1,...,in = X α1,...,αn−1 G(1) i1,α1 G(2) α1,i2,α2 · · ·G(n) αn−1,in ,(18) where G(k) are 3-way cores (except at the ends) and αk are bond indices of dimension χk. For completeness, a step-by-step derivation of the contraction identity is given in Appendix A.3. Step-by-step derivation of the contraction identity We derive Equation (8) starting from the Bernoulli-mixture definition of the multilinear extension in Equation (4). Graph-Structured Tensor Network Surrogates for Shapley Values and Interactions Step 1 (Start from the multilinear extension).By definition, eν(z) = X S⊆V ν(S) Y v∈S zv Y v /∈S (1−z v).(19) Step 2 (Re-index the sum by binary indicators).Each subset S⊆V is in bijection with its indicator vector s∈ {0,1}n given by sv =1{v∈S} . Substituting this into (20) yields eν(z) = X s∈{0,1}n Ts nY v=1 zsv v (1−z v)1−sv .(21) Step 4 (Define selector vectors).For eachv∈Vdefine bv(zv) := 1−z v zv  ∈R 2. bϕi :=L i(beν), bIT :=L T (beν), so asinglesurrogate beνsuffices to compute all desired first-order Shapley values and higher-order interaction indices, without retraining separate surrogates per index. Finally, we avoid the exponential coalition tensor by learning a structured surrogate ˆνfrom a limited query budget, and then compute all required Shapley quantitiesdeterministicallyfrom this single surrogate without further teacher evaluations. Graph-Structured Tensor Network Surrogates for Shapley Values and Interactions Algorithm 2 TN-SHAP-G(deterministic O2 edge interactions from a single surrogate) Require:trained surrogateˆν(·; Θ)onG= (V, E), probe points{t j}m j=1 Ensure:edge interaction indices{ ˆϕuv}(u,v)∈E 1:forj= 1, . first differentiate with respect to zi (replacing bi 7→b ′ i), then differentiate with respect toz j (replacingb j 7→b ′ j). Finally, if degmax(G)≤∆ and dim(e)≤χ , then for each v, Q e∈Ev dim(e)≤χ deg(v) ≤ χ∆, henceN G ≤ P v∈V χ∆ =|V|χ ∆. Since each desired attribution quantity is a linear functional of thesameunique eν, any surrogate ˆνthat approximates eν (e.g., a trained tensor-network surrogate) can be plugged into these linear functionals to obtain corresponding approximations to all first-order Shapley values and higher-order interactions, without retraining a separate sur