REAL METHOD of "Diffusion Differentiable Resampling" (arXiv 2512.10401v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Drawing on reparametri- sation, we propose a new resampling method that is informative and instantly differentiable, based on a training-free diffusion model sur- rogate. We theoretically prove that our diffu- sion resampling method provides a consistent re- sampling distribution, and we show empirically that it outperforms the state-of-the-art differen- tiable resampling methods on multiple filtering and parameter estimation benchmarks. Various methods have thus been proposed to make the re- sampling step differentiable in the context of SMC and SSMs (see recent surveys in Chen & Li, 2025; Brady et al., 2025). • We empirically validate our method through both ab- lation and comparison experiments. The results show that our method consistently outperforms the com- monly used differentiable resampling methods for both filtering and parameter estimation problems. Notably, our method is computationally efficient and stable, al- lowing for practical and robust usage in applications. More- over, recent advances provide methods for propagating gra- dients through SDE solvers (see, e.g., Bartosh et al., 2025; Li et al., 2020; Kidger et al., 2021) and these methods have been well implemented in common automatic differentiation libraries (Kidger, 2021). To combine with the SMC in Algorithm 2, we compute µN and ΣN based on  (wj−1,i, Zj−1,i) N i=1, see Appendix A for details. Experiments In this section we empirically validate our method in both synthetic and real settings. First, we show that the diffusion resampling, regardless of the differentiability, is a useful resampling method and is at least as good as commonly used resampling methods (Sec- tion 4.1). We use the prior as the proposal to generate importance samples, and then apply the resam- pling methods to obtain resampled particles. For each experiment, we generate a measurement sequence with 128 time steps and apply a Kalman filter to compute the true filtering so- lution and log-likelihood L(θ1, θ2) evaluated at Cartesian Θ = [θ1−0.1, θ1+0.1]×[θ2−0.1, θ2+0.1]. Therefore, in the next sections we will focus on comparison using first-order gradient-based methods. We gen- erate data at time t∈[0,3] discretised by Milstein’s method with 256 steps. 0 250 500 750 1000 Training iteration 103 9× 102 1.1× 103 1.2× 103 1.3× 103 1.4× 103 1.5× 103 1.6× 103 MLE loss Diﬀusion T = 1, K = 4 OT 0.3 Gumbel 0.3 Soft 0.5 Stopped Figure 4.Loss traces (median over all runs) for training the prey- predator model. The first figure shows that the trained model using the diffusion resampling achieves the lowest prediction error and is significantly more stable than the baselines. The second figure aligns with the results from the first figure, showing that the training loss with diffusion resampling consistently lower bounds that of the other methods, and is more stable. the first uses adaptive resampling under a tempered observation likeli- hood, and the second is a more challenging setting with resampling at nearly every filtering step. In ad- dition, their method introduces a further challenge in that the resampling gradient should also be propagated through the diffusion training. In contrast, our method focuses on a statistically grounded diffusion resampling scheme without training, and is more informative. Our experiments verify that the proposed method outperforms the state-of-the-art dif- ferentiable resampling methods for filtering and parameter estimation of state-space models. An ensemble score filter for tracking high-dimensional nonlinear dynamical systems.Computer Methods in Applied Mechanics and Engineering, 432:117447, 2024. Sequential Monte Carlo methods to train neural network models.Neural Computation, 12(4):955–993, 2000. Improved rate of first order algorithms for entropic optimal transport. For each i, draw N independent samples {ui,j ∼Uniform[0,1]} N j=1 and computeg i,j =−log logu i,j. Tables 5 and 6 show that the diffusion and OT methods have a similar estimation error especially for large sample size 19 Diffusion differentiable resampling Table 5.The resampling error of diffusion resampling in terms of the number of time steps K and sample size N. In Tables 7 we find that the loss function estimation error seems to be invariant in the time T and steps K when using an ODE solver (except for Lord–Rougemont), giving a relatively large error 2.72. The observation rate functionλ:R×R→R 2 >0 is defined by λ(c, r):= 5 1 + exp −5c −c r  + 4 ! .(37) We simulate the model with Milstein’s method and generate data at t∈[0,3] with 256 dicretisation steps. Figure 8 shows the median loss evolution during training for the best model configuration (in terms of mean SSIM and PSNR) from each resampling class in each experiment setting. In our experiments, we consider end-to-end training 24 Diffusion differentiable resampling Table 13.Prediction quality in terms of structural similarity index (SSIM) and PSNR (higher the better) for the pendulum dynamics tracking experiment, with respect to models trained using diffusion resampling in the first experiment setting. In both settings, the network consists of 3 hidden layers of 256 units with sine activations (ω0 = 8.0 in the first layer). It first processes the input via two linear layers (16 and 256 units), reshaping the output into a spatial feature map of size 4×4×16 . ReLU activations are used for all hidden layers, while the output layer is linear. There are two steps moving from training a classical BNN to training a pBNN with SMC sampler. Specifically, we apply the pBNN setting and construct an SSM p(zj |z j−1) = N(zj;ρ zj−1,1−ρ 2), pθ(Dj |z j) = Softmax(fθ,zj ;D j), (38) for CIFAR10 classification, whereD j is a batch data at surrogate timej(training step), and we chooseρ= 0.99. 26 Diffusion differentiable resampling  /uni00000037/uni00000055/uni00000044/uni0000004c/uni00000051/uni0000004c/uni00000051/uni0000004a/uni00000003/uni0000004c/uni00000057/uni00000048/uni00000055/uni00000044/uni00000057/uni0000004c/uni00000052/uni00000051 /uni00000016/uni00000011/uni00000017/uni0000001c/uni00000013 /uni00000016/uni00000011/uni00000017/uni0000001b/uni00000018 /uni00000016/uni00000011/uni00000017/uni0000001b/uni00000