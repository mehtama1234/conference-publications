REAL METHOD of "Sample from What You See: Visuomotor Policy Learning via Diffusion Bridge with Observation-Embedded Stochastic Differential Equation" (arXiv 2512.07212v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

However, existing methods typically treat observations only as high- level conditions to the denoising network, rather than integrating them into the stochastic dynamics of the diffusion process itself. We propose BridgePolicy, a generative visuomo- tor policy that directly integrates observations into the stochastic dynamics via a diffusion-bridge formulation. To overcome this, we introduce a semantic aligner to unify the visual and state inputs and align the observations with action representations, making diffusion bridge applicable to heterogeneous robot data. These methods share a similar principle that perturb action chunks into random noise via a forward process defined by a Stochastic or Ordinary Differential Equation (SDE/ODE) and then train a neural network conditioned on observations to reverse this process, iteratively transforming noise into executable actions. The observation consists of robot states and point cloud. 3D Diffusion Policy (Ze et al., 2023a), known as DP and DP3, employ an SDE-defined forward process and train the neural network conditioned on visual inputs and robot states to control the denoising process to sample actions from the random noise. Similarly, FlowPolicy (Zhang et al., 2025) employs an ODE-defined forward and reverse pro- cess, which reduces the stochasticity during training and inference. Building upon these insights, we propose that diffusion bridge can also serve as a more effec- tive framework for visuomotor policy learning. First, robotic observations and ac- tions are inherently heterogeneous, violating the standard 2 Visuomotor Policy Learning via Diffusion Bridge with Observation-Embedded Stochastic Differential Equation diffusion-bridge assumption that the connected endpoint distributions share the same dimensionality. We introduce BridgePolicy, a generative visuomotor policy that directly samples actions from observation-informed priors rather than random noise. To resolve the modality and shape mismatches, we design a seman- tic aligner that maps the observation representation into an action-aligned latent space, ensuring compatible endpoints for the bridge. BRIDGER (Chen et al., 2024) first trains a coarse policy and then adopts diffu- sion bridge to refine the actions, whose performance largely depends on the quality of the coarse policy. Our method models the heterogeneous obser- vations in the diffusion SDE trajectory via diffusion bridge and generates the executable actions directly. Previous works such as DP3 (Ze et al., 2024) and FlowPolicy (Zhang 3 Visuomotor Policy Learning via Diffusion Bridge with Observation-Embedded Stochastic Differential Equation Algorithm 1BridgePolicy Training Input:Actions a, Observations o={o s,o pc}, loss weightα, and gradient descent step sizeη. Training Objective of BridgePolicy.In order to learn such a bridge to reverse the process, we adopt the following reconstruction training objective to train a data prediction neural network aθ(at,a T , t)that directly maps any at to the clean actiona 0: Sampling Algorithm of BridgePolicy.After we obtain the optimal model a∗ θ(at,a T , t), the decision making module of our BridgePolicy can be implemented as an iterative generation procedure starting from the given observation aT =o . As for specially designed solvers, UniDB++ (Pan 4 Visuomotor Policy Learning via Diffusion Bridge with Observation-Embedded Stochastic Differential Equation et al., 2025) provides a training-free acceleration algorithm with data prediction aθ(at,a T , t)that directly estimates the fixed and smooth target a0. Although the updating rule seems very complex on the coefficients of as, aT , aθ, and ϵ, actually these coefficients can be computed explicitly without incurring significant computational over- head, because they have closed forms and depend only on the specific time steps. This method still faces challenges of heterogeneous distribution bridging and data shape mis- match, which prevent the diffusion bridge from functioning effectively as a policy learning method. Therefore, even when the observation MLP incurs fitting errors, the actions generated from the sampling algorithms of diffusion bridge would be constrained within a narrow range, preventing significant deviation. To address this, we propose using the contrastive learning loss to train the aligner. Specifically, we adopt CLIP loss (Radford et al., 2021) as the contrastive learning loss: The entire model involves the diffusion bridge and the modality fusion module and therefore, the overall training objective integrates the diffusion bridge loss (3) with the alignment loss (8): We provide the pseudo-code Algorithm 1 and Algorithm 2 for the training and inference process of our BridgePolicy. During the training phase, the observations are firstly fused and reshaped by the modal- ity fusion module and aligner, and then alignment loss and diffusion bridge loss are computed by corresponding equa- tions. Method Oven-Closing Oven-Opening Pick Place Pour Unplug Average Simple DP3 0.8 0.6 0.6 0.6 0.7 0.66 DP3 0.9 0.9 0.7 0.6 0.7 0.76 FlowPolicy1.00.7 0.5 0.1 0.5 0.56 BridgePolicy 1.0 1.0 0.8 0.8 0.9 0.90 Table 3.Quantitative evaluation results (Success Rate) on different modality fusion methods (MetaWorld is denoted as MW in table). Task Modality Fusion Method Concatenation Cross-Attention Adroit Pen 0.780.81 Adroit Door 0.590.665 MW Handle-Pull 0.550.63 for all baselines except FlowPolicy. For each random seed, we train the models for 3000 epochs and evaluate the success rate on 20 episodes every 200 training epochs. For fair comparison, we keep the model architecture consistent with the baselines, the amount of parameters comparable, and the shared hyperparameters, such as learning rates, weight decays of the optimizer, batch sizes, and training epochs. For each task, BridgePolicy is trained on a single NVIDIA RTX 4090 GPU. All baseline methods show considerably lower success rates, particularly in more challenging settings (Hard and Very Hard in MetaWorld), which also showcases the robustness of our method in tasks with varying complexity. For each task, we collect 50 episodes to train the policy. Under the identical constraint of training with the same dataset of 50 episodes, our BridgePolicy yields the highest success rate on all the tasks, with an average of 0.9, underscoring its effectiveness. Specifically, we evaluate different weights α in the overall training loss (9) and the results are shown in Table 4.