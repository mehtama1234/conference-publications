REAL METHOD of "Primary-Fine Decoupling for Action Generation in Robotic Imitation" (arXiv 2602.21684v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

some methods discretize actions into tokens and therefore lose fine-grained action variations, while oth- ers generate continuous actions in a single stage tend to produce unstable mode transitions. To address these limitations, we propose Primary-Fine Decoupling for Action Generation (PF-DAG), a two-stage framework that decouples coarse action consistency from fine-grained variations. First, we compress action chunks into a small set of discrete modes, enabling a lightweight policy to select consis- tent coarse modes and avoid mode bouncing. Generative latent-variable methods instead modela=π(o, z)so that sampling differ- entzyields different plausible actions (Zhao et al., 2023; Chi et al., 2023). However, independent per-step resampling ofztends to produce random switches among modes (Chen et al., 2025) (see ∗Corresponding authors: Intuitively, primary modes capture coarse, discrete decisions while within-mode residuals encode fine-grained variations. Motivated by the above, we propose Primary-Fine Decoupling for Action Generation (PF-DAG), a two-stage imitation framework that explicitly separates primary mode selection from continuous action generation. Concretely, PF-DAG first learns a discrete vocabulary of primary modes and a lightweight policy that greedily selects a mode coherently. Then, we introduce a mode conditioned MeanFlow policy, which is a one-step continuous decoder to generate high-fidelity actions condi- tioned on the selected mode and the current observation. Among existing methods, single- stage generative policies (Chi et al., 2023; Zhao et al., 2023) are the most direct and competitive end-to-end approach for modeling continuous, multi-modal action distributions, so we focus our theoretical comparison on this family. In BC, a policy is trained to predict the expert’s action for each observed state, yielding a deterministic mapping from states to actions. converting a high-frequency, high- dimensional control signal into a sequence of discrete symbols so that standard sequence-modeling methods can be applied. First, next-token prediction over a discrete vocabulary represents multi-modal conditional action distributions without collapsing modes into a single mean. Second, sequence models bring powerful context modeling and scalable pretraining recipes from language and vision to control, enabling cross-task and cross-embodiment generalization when token vocabularies are shared or aligned. The simplest and most commonly used approach maps each continuous action dimension at each step to one of a fixed set of bins (Brohan et al., 2022; Zitkovich et al., 2023; Kim et al., 2024). Frequency-space methods like FAST (Pertsch et al., 2025) departs from it and instead compresses action chunks using a time-series transform and lightweight quantization. 2.3 GENERATIVEPOLICY A large class of imitation methods treat policy generation as a stochastic generative problem by introducing latent variables. Action Chunking with Transformers (ACT) (Zhao et al., 2023) is a sequence generator with Conditional Variational Autoencoder (CV AE) as backend. 3 PF-DAG FORMULATION ANDDESIGN This section first defines the task formulation as a closed-loop action-sequence prediction problem, and then presents the three main components of our approach: The policy predicts an action chunka t ∈R Tp×da and executes the firstT a ≤T p steps before re-planning. Given an action chunka, the encoder producesz e =E ϕ(a)and we quantize it to the nearest codebook vector: During trainingπ 1 is optimized with a standard cross-entropy objective that matches the encoder- assigned VQ indices. We train¯vθ with squared-error objective that supervises the predicted average velocity. 4 EXPERIMENTS 4.1 SIMULATIONEVALUATION Benchmarks and Datasets.We evaluate our method on manipulation benchmarks that cover a broad range of control domains. For each seed we evaluate the policy for 20 episodes every 200 training epochs and then compute the average of the top-5 highest success rates (Ze et al., 2024). first we pretrain the VQ-V AE to learn compact primary prototypes; then we freeze the codebook and jointly train the Primary Mode Policyπ 1 (cross-entropy to the VQ indices) and the mode-conditioned MeanFlow generator¯vθ (squared-error supervision on sampled(τ, r)intervals). Key Findings.Across Adroit, DexArt and MetaWorld, our method substantially outperforms dif- fusion and other baselines. Hardware.We evaluate our method on two single-arm hardware configurations commonly used in manipulation research: Baselines.We compare our method to two baselines.Vanilla BCprocesses observations through the same Observation Feature Extraction pipeline used by our method, and a 3-layer MLP is trained to regress actions in a standard behavior cloning setup (Levine et al., 2016).DP3(Ze et al., 2024) is a diffusion-based generative policy operating on 3D point-cloud-conditioned actions. Both baselines are trained on the identical demonstration sets and evaluated under the same closed-loop control as our method. Result Analysis.Our method consistently outperforms both baselines in success rate across low- DOF and high-DOF/tactile tasks (see Table 2). Typical failure cases for our method occur at out-of- distribution object placements or when tactile sensing is intermittently noisy. Results appear 8 Published as a conference paper at ICLR 2026 1-step 0.47 0.48 0.53 0.71 41.7 14.6 10.4 40.9 1-NFE DP3 5-NFE DP3 10-NFE DP3 Ours 1-step DP3 Ours Action Chunk Length Success SuccessFrames per Second (FPS) Real-time Control FPS: Our method mainly needs a reasonable set of coarse modes rather than a specific quantizer. For Runge-Kutta inte- gration, we adopt the Dormand-Prince 5 method, a widely used choice for adaptive-stepsize ODE solving. Our method maintains relatively high success even at short chunk lengths, showing the two-stage design preserves primary-mode consistency while allowing rapid reactivity. First, because primary-mode selection operates on discretized action chunks, the method exhibits reduced temporal granularity in very high-dynamics, low-latency tasks. A lightweight primary policy is then trained to predict these modes from observations, allowing for stable and consistent coarse mode selection. A.3 MEANFLOWDERIVATION This section provides a detailed derivation of the training objective for our mode-conditioned Mean- Flow policy, as mentioned in Section 3.4. To make this definition amenable to training, we first rewrite i