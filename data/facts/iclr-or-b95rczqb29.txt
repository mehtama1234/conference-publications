REAL METHOD of "Domain Expansion: A Latent Space Construction Framework for Multi-Task Learning" (arXiv 2601.20069v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Our framework uses a novel or- thogonal pooling mechanism to construct a latent space where each objective is assigned to a mutually orthogonal subspace. We validate our approach across diverse benchmarks—including ShapeNet, MPIIGaze, and Rotated MNIST—on challenging multi-objective problems combining classification with pose and gaze estimation. While existing multi-task learning methods attempt to mitigate this by re- weighting task losses or projecting gradients at runtime (Yu et al., 2020; Sener & Koltun, 2018), these approaches act on the optimization process itself, not the structure of the latent space. To address this gap, we propose Domain Expansion, a new framework for constructing latent spaces that are multi-objective by design. The core of our framework is orthogonal pooling, a lightweight architectural primitive that constrains features for different objectives to lie in mutually orthogonal subspaces. Across all experiments, our method successfully resolves latent representation collapse. • We demonstrate that our method constructs an explicit and interpretable latent space, where orthogonal axes correspond to distinct concepts, enabling compositional inference and analysis. Modern methods often learn such representations by optimizing for multiple, sometimes implicit, objectives. For example, GradNorm (Chen et al., 2017) dynamically adjusts the weights of each task’s loss to balance their training rates. (Center) Standard multi-objective training leads tolatent representation collapse, where concepts interfere and the latent space becomes entangled. Building on this, methods like CAGrad (Liu et al., 2021a) and MGDA (Sener & Koltun, 2018) seek a common gradient that represents a Pareto optimal solution or minimizes the worst-case loss across tasks. While effective, these gradient-level methods are fundamentally reactive; they resolve conflicts at each training step after they have already occurred. This proactive approach eliminates the need for runtime gra- dient manipulation and results in a latent space that is more explicit, interpretable, and robust to multi-objective training. 3METHOD Our goal is to design a latent space where a single representation can effectively encode information for multiple, competing objectives without interference. For each learning objectivem, a corresponding decoder,Dec(·), then maps the latent representationfto a target conceptC. The quality of this mapping is measured by an objective lossL. Z Enc − − → F Dec − − → C(evaluated byL)(1) In representation learning, the objective lossLis often defined directly on the latent representations F. In this scenario, a single, shared representationFis subjected to multiple objective losses, {Lm}M−1 m=0 , each guided by a distinct target concept,{C m}M−1 m=0 . (b) Analogously, our method treats a single latent feature as a rich object that encodes multiple concepts simultaneously. As we have argued, naively minimizing this sum often leads to latent representation collapse, where conflicting structural demands from different losses force the shared representationFinto a com- promised state that is suboptimal for all objectives (see Fig. 3.2 DOMAINEXPANSION To prevent latent collapse in multi-objective learning, we introduce Domain Expansion, a framework that structures the latent spaceFinto a set of dedicated, orthogonal subspaces. The core idea is to ensure that the updates for each objectivemare confined to a unique subspace, preventing them from interfering with the representations of other objectives (see Fig. The framework is a dynamic process applied at each training epoch, consisting of three steps: 1.Find Principal Axes.First, we estimate the empirical meanµand covarianceΣof the latent feature distribution over the current batch or entire training set: Each eigenvectorv m is assigned then to represent a single target conceptCm. (Center) For training, we define a numerical concept space by assigning coordinates to these attributes. F proj M−1 DecM−1 − − − − − → CM−1 (7) The total training loss is then computed as the sum of individual losses on these indepen- dent, projected latent features: 3.3 PROPERTIES ANDOPERATORS OF THEDOMAIN The orthogonal structure of our method is not merely a training aid; it endows the latent space with powerful properties, turning it into an interpretable and compositionalconcept algebra. 4.1 EXPERIMENTALSETUP Architecture and Training.We adopt an encoder-decoder architecture here. For each of theMobjectives, its corresponding decoder,Dec m(·), is a single linear layer. first, we train the encoder while dynamically updating the orthogonal basis (VM andµ) at each epoch. Based on these components, we define two primary sets of objectives for our experiments, which correspond in order to the concepts{C az,C el,C rot,C cat,C id}.Objective Set 1uses the RNC loss for all five concepts.Objective Set 2uses RNC for the first three concepts (regression) and SupCon for the final two (classification). 4.2 EVALUATINGMULTI-OBJECTIVEPERFORMANCE To answer our first two research questions (H1 and H2), we evaluate our method against a suite of baselines. Whereas the latent spaces of baseline methods appear entangled and unstructured, the space learned by our method is clearly organized, with concepts aligning along their corresponding orthogonal axes. Our method (bottom row) demonstrates a distinctly more organized structure with clear directional alignment for each concept, unlike the entangled representations produced by the baseline methods. Unlike other approaches that might constrain the entire latent vector, our method only guides its components along the pre-defined orthogonal axes. The Hungarian algorithm then finds the 12 Published as a conference paper at ICLR 2026 optimal one-to-one assignment that maximizes the similarity, resolving any permutation ambiguity. Figure 8:Eigenvector stabilization during training.We plot the cosine similarity between the aligned eigenvectors for each concept from the current and previous training epochs. We designed an experiment to validate this, demonstrating that our framework can successfully addNnew objectives (C M+1 , ..., CM+N ) to a model already pre-trained onMtasks, without retraining from scratch. 3.Train New Tasks (with Regularization):We then train theNnew tasks. Each new objectiveC j (wherej∈ {M+ 1, ..., M+N}) is trained using its lossL j on projections onto its corresponding new axisv j. Our assignment is based on vari