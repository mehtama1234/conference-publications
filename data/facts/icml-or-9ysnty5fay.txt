REAL METHOD of "One Model to Translate Them All: Universal Any-to-Any Translation for Heterogeneous Collaborative Perception" (arXiv 2605.17907v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Most existing methods, including direct adaption and protocol-based transforma- tion, typically rely on training adapters for newly emerging feature modalities and often require ad- ditional retraining or fine-tuning. Such repeated training is costly and is often infeasible across manufacturers due to model and data privacy con- straints, limiting real-world scalability. One-to-one adapta- tion methods (e.g., MPDA (Xu et al., 2023), PnPDA (Luo et al., 2025)) perform domain transfer by training a cus- tomized adapter for eachmodality mapping, i.e., a specific transformation from the source feature space to the tar- get feature space. trains a dedicated adapter for each pairwise modality mapping, incurring substantial training complexity. Specifically, we design a two-stage pretraining pipeline for UniTrans to enable effective on-the-fly instantiation of heterogeneous feature translators, as illustrated in Fig. To enable more targeted, real-time translator instantiation from single-frame heterogeneous feature, we first pretrain a Modality-Intrinsic Encoder (MIE) that extracts modality- specific, scene-invariant intrinsic codes from intermediate features and organizes them into a modality-intrinsic latent space, where newly emerging modalities can be reliably localized and compared to estimate their mappings. A Modality Mapping Router (MMR) then predicts combina- tion coefficients from the inferred mapping and instantiates a mapping-specific feature translator. • We propose UniTrans, the first universal any-to-any translation model for heterogeneous collaborative per- ception, which instantiates a mapping-conditioned fea- ture translator on the fly to translate features from newly emerging agents. • We design a tailored pipeline for UniTrans by pre- training a Modality-Intrinsic Encoder, a Modality Map- ping Router, and a reusable Translator Parameter Bank, while learning parameter combination coefficients as a function of the modality mapping. The results show that our method im- proves perception performance by up to 10%, enabling efficient any-to-any translation through a universal model. After a single two-stage pretraining procedure, UniTrans can perform zero-shot feature translation at inference time under arbitrary heterogeneous modality mappings, without any repeated retraining. Two-step methods build a shared protocol space, including HEAL (Lu et al., 2024), STAMP (Gao et al., 2025), and NegoCollab (Shao et al., 2025). Traditional Mixture of Experts Mixture of Experts (MoE) increases model capacity via con- ditional computation, where a router activates only a small subset of experts per input, enabling scalable specialization with near-constant compute (Shazeer et al., 2017; Fedus et al., 2022). Most existing approaches (Xu et al., 2023; Luo et al., 2025) approximate ∆j→i by training an adapter with many sam- ples spanning both subspaces and optimizing its parameters via backpropagation on translation objectives (e.g., feature reconstruction losses). z= MIE(F), z∈R d,(10) where z is the intrinsic code of feature F , and the train- ing objective encourages codes from the same modality to concentrate in a compact region of the intrinsic space. With suffi- cient pretraining over diverse modalities inMtr, MIElearns a transferable encoding F → Zthat disentangles modality cues from scene variations.As a result, newly emerging modalities can be localized to consistent intrinsic regions in Z without additional training, providing the first source of zero-shot capability. A MMR takes (zj, zi) as input, first es- timating the modality mapping δj→i in the intrinsic space and then outputting parameter combination coefficients: By train- ing MMR, we capture a shared regularity thattransforms arbitrary modality mappings in H to corresponding transla- tor instances in Φ , enabling synthesis of mapping-specific translators for unseen modality pairs without retraining, which constitutes our second source of zero-shot capability. For a sampled scene s∼ Dtr, we assign each agent a random modality label m∈ Mtr and use the corresponding encoder f enc m ∈ Rtr to extract an intermediate feature F=f enc m (X), and MIE then produces an intrinsic code z= MIE(F) , where d is the embedding dimension. Given a BEV feature map F∈R C×H×W , where C is the channel dimension and H, Wdenote the spatial height and width of the featire, we compute first- order channel statistics: Training objective.To structure the modality-intrinsic latent space, we jointly optimize a contrastive objective and a surrogate modality classification objective. Given a mini-batch of intrinsic codes {za} with modality la- bels {ma}, we define the positive set for anchor a as P(a) ={p̸=a|m p =m a} and adopt an InfoNCE- style (Chen et al., 2020; Poole et al., 2019) contrastive loss to pull together codes from the same modality while pushing apart codes from different modalities: Modality Mapping Router (MMR).Given intrinsic codes zj = MIE(Fj) and zi = MIE(Fi), MMR first forms a compact mapping descriptor and then predicts expert mix- ing coefficients: Training objective.To learn a reusable TPB and enable MMR to predict accurate parameter combination coeffi- cients for each modality mapping, we optimize objectives that supervise both translation quality and routing behavior. the downstream task loss Ltask( ˆYi, Yi) and a feature-level distillation loss. We then apply an InfoNCE loss on routing vectors: With router logits ua,k, we regularize the router using a Switch-style load-balancing loss (Fedus et al., 2022) and a light logit penalty (Yang et al., 2025), combined as Lr =K KX k=1  impk B  loadk B  + 1 B BX a=1  log KX k=1 exp(ua,k) 2 . To ensure a fair comparison, all heterogeneous feature translation methods are trained on the sameMtr and Dtr for 70 epochs using the same learning rate. Since baseline translators are not inher- ently designed to accommodate numerous newly emerging modalities, we further strengthen their training protocol to better accommodate newly emerging modalities to a certain extent (see Sec. 4 reports component ablations on OPV2V-H by re- moving one objective at a time from our full training recipe. A pronounced degradation is observed when both LIC and LIS are removed (ID 3, 0.662/0.540), which ef- fectively disables Stage 1 training, and the MMR must then infer routing directly from current intermediate fea- tures without intrinsic regularization, making routing harder and reducing generalization to