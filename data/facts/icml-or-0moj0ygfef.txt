REAL METHOD of "SciAgentGym: Benchmarking Multi-Step Scientific Tool-Use in LLM Agents" (arXiv 2602.12984v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

To bridge this gap, we introduce SciAgentGym, a scalable interactive environmentfeaturing1,780domain-specifictoolsacrossfournaturalsciencedisciplines, supported by a robust execution infrastructure. To address this, we propose SciForge, a data synthesis method that models the tool action space as a dependency graph to generate logic-aware training trajectories. To bridge this gap, we introduceSciAgentGym(§3), a hierarchical interactive environment designed for grounding LLM agents in multi-turn tool-use scientific reasoning tasks. Built upon an ex- tensible architecture, the framework seamlessly integrates 1,780 domain-specific tools across Physics, *Equal Contribution. Benchmarking Multi-Step Scientific Tool-use in LLM Agents Output : 20 DB Retrieval… Pictures saving… Environment Dataset Files:1 Toolkit:53 Code Setup: Figure 1| Benchmarking multi-step scientific tool- use in SciAgentGym.A representative trajectory where an LLM agent interacts with the environment to solve a complex chemistry task. Recognizing that standard training data lacks complex dependencies across diverse scientific tools, we designSciForge(§5), a data synthesis method that formalizes the tool environment from a flat collection into a dependency graph. A graph-based data synthesis method that generates logic-aware training trajectories, enabling our 8B model to outperform 200B+ scale models. Benchmarking Multi-Step Scientific Tool-use in LLM Agents BiologyChemistryPhysicsMaterials Installed Packages Env Architecture Environment Comput. The right panel illustrates the core capabilities supported by this environment, including SciAgentBenchfor evaluation, an interactiveAgent Interfacefor iterative multi-step reasoning with feedback, and aTrainingmethod to enhance performance via tool graph sampling and execution- grounded verification. SciAgentBench We introduceSciAgentBench, a benchmark for evaluating scientific agents on long-horizon reasoning and multi-step tool-use. L1 (≤3 steps), L2 (4–7 steps), and L3 (≥8 steps), with long-horizon tasks (L2+L3) comprising 79% of the benchmark. Benchmarking Multi-Step Scientific Tool-use in LLM Agents Evaluation Metrics.Each task requires solving a sequence of sub-questions through multi-step tool invocations. In this section, we introduceSciForge, a synthesis method that aims to address long-horizon performance degradation. Tool Dependency Graph and Program Sampling To systematically generate executable trajectories, we first construct aTool Dependency Graph G𝑑 =(V 𝑑,E 𝑑) for each domain𝑑. We then execute tools in topological order; at each step𝑖, the environment returns a response𝑟𝑖 (either a validated outputo𝑖 or fine-grained error feedback𝑒𝑖), updating the state to𝑠𝑖. What Failure Patterns Emerge in Multi-step Scientific Tool-use? We investigate failure mechanisms in multi-step scientific tool orchestration. Claude-Sonnet- 4’s recovery rate increases from 40% (steps 2–3) to 57% (steps 6–7), drops to 9% (steps 8–9), then rebounds to 63% (steps 10–12). What Makes Scientific Tool-use Training Effective? Building on evidence that fine-tuning significantly improves scientific tool-use capabilities, as shown in Figure 4(left), we now investigatewhich training design choicescontribute to these gains through systematic ablation studies. Error recovery trajectories are essential.We compare Qwen3-VL-8B-Merged trained on full trajectories with error recovery to Qwen3-VL-8B-NoError trained only on clean trajectories. Benchmarking Multi-Step Scientific Tool-use in LLM Agents we proposeSciForge, an execution-grounded synthesis method generating logic-aware data from real interactions. Due to a small number of unresolved non-response cases after repeated reruns, success rates are computed over the effective evaluated set for each model and subset. This taxonomy is consistent with our layered architecture (Atomic→ Composite→ Visualization). •Query:Retrieve hierarchical facts/records from external resources or local indices and return normalized fields for downstream steps. – Example:construct_hamiltonian(params, ...) •Analysis:Perform post-processing and higher-level interpretation over computed/retrieved artifacts (e.g., diagnostics, aggregation, comparisons, error/uncertainty handling). Data Annotation and Quality Review Process To ensure reliability and evaluability during benchmark construction, we conduct human quality review on all samples , verifying clarity and unambiguity, the logical correctness of step-by-step reasoning and tool-call ordering, the consistency of tool outputs via recomputation, the correctness of final answers, and compliance with the benchmark template. Algorithm Details A complete pseudocode description of the backward program construction process is presented in Algorithm 1. Benchmarking Multi-Step Scientific Tool-use in LLM Agents Algorithm 1Backward Program Construction Require:Target tool𝑣 goal, graphG𝑑 =(V 𝑑,E 𝑑), max depth𝐷max, exploration rate𝜖 Ensure:Executable program graphP=(V P,B) 1:V P←{𝑣 goal},B←∅ 2:anc[𝑣 goal]←∅,depth[𝑣 goal]←0 3:queue←[(𝑣 goal,0)] 4:whilequeue≠∅do 5:(𝑣,𝑑)←queue.popfront() 6:foreach input slot𝑗∈[𝑘 𝑣]do 7:A←anc[𝑣]∪{𝑣}{Path ancestors} 8:C←{(𝑢,𝑖)∈O(𝑣, 𝑗):𝑢∉A, 𝑑 < 𝐷 max} 9:ifC≠∅then 10:C ∗←{(𝑢,𝑖)∈C: The training infrastructure leverages DeepSpeed ZeRO-3, gradient check- pointing,andFlashAttentiontomaximizeefficiency. Tomaintainthepre-trainedmultimodalalignment stability, we explicitly freeze the vision backbone and the projector modules (freeze_vit=true, freeze_aligner=true), updating only the language model parameters. Planning.Before executing any actions, first generate a high-level, step-by-step plan outlining how you will break down the problem, which tools you will use, and the dependencies among tool calls. Execution.After providing the plan, execute it step by step. 1.[Your plan step 1, e.g., analyze the molecular structure in the input image] 2.[Your plan step 2, e.g., useanalyze_moleculeto obtain SMILES] 3.[Your plan step 3, e.g., pass SMILES intocalculate_properties] 4.[...] 5.[Your plan step N, e.g., summarize all properties and answer the question] [Stage 2: Planning.Before executing any actions, first generate a high-level, step-by-step plan outlining how you will break down the problem, which tools you will use, and the dependencies among tool calls. Execution.After providing the plan, execute it step by step. 1.[Your plan step 1, e.g., analyze the molecular structure in the input image] 2.[Your plan step 2, e