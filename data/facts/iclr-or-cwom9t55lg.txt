REAL METHOD of "On the Impossibility of Separating Intelligence from Judgment: The Computational Intractability of Filtering for AI Alignment" (arXiv 2507.07341v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

First, we show that there exist LLMs for which there are no efficient prompt filters: We conclude that safety cannot be achieved by designing filters external to the LLM internals (architecture and weights); in particular, black-box access to the LLM will not suffice. First, for prompt filtering, we construct LLMs for which no efficient filter can distinguish adversarial prompts—crafted to elicit harmful behavior—from benign ones. 2 1.1 Prompt Filtering The first approach we consider is running user prompts through a prompt filter, which can detect and discard prompts that might lead to harmful answers from the LLM. We focus on a setting in which the LLM itself is untrusted, but we can query it while training a prompt filter (during training we can try different prompts to observe if and where harmful behaviors occur). After this filter-training phase, the filter should detect (and reject) prompts that lead to harmful outputs. A prompt filter is a moderately powerful algorithm, which can be trained using real-world data and black-box access to the LLM but during deployment it does not have full access to the LLM architecture and weights. otherwise we can ignore the given LLM and just focus on training a new one from scratch and using it in place of a filter. First, our result relies on a computational asymmetry between the filter and the LLM. filtering is only meaningful when the LLM is too expensive or complex to retrain, and cannot be fully simulated by the filter. First, they require a weaker cryptographic assumption that the existence of time-lock puzzles. The LLMs constructed in Theorem 1 behave in a contrived and deliberately pathological manner, and Theorems 2 and 3 rely on cooperation between LLMs and malicious users—scenarios that may at first glance seem overly pessimistic or implausible. The term deceptive alignment refers to cases in which an AI system that is not genuinely aligned with human intent nonetheless behaves in an apparently aligned way, often strategically, in order to avoid detection, satisfy training objectives, or manipulate oversight. [2024], the LLMs were misaligned during training and aligned during deployment, but one could easily imagine the reverse scenario. 4 then passed to the output filter, which may choose to detect and reject the output. Note that if it is easy to determine whether a given LLM output is harmful, then output filters are trivial to construct. An output filter is an algorithm that can be more computationally4 powerful than the LLM, although it still runs in polynomial time (cannot break cryptography). First, the authors suggest using a Scientist AI A to generate candidate experiments and another, more powerful Scientist AI B to assess their safety. The algorithms are formally defined in Algorithms 1 and 2. The formal implementation of G′ is given in Algorithm 3 and the implementation of M ′ is given in Algorithm 4. First, we can choose t so that computing f is feasible for the LLM (which can run computations that take more thant steps), but not for a filter whose runtime is bounded to be significantly smaller than t. To achieve this, we design an algorithm Samp (Algorithm 1) that takes as input a generator G and randomness R and produces z = (z0, z1, . .); and an algorithm RecoverRand (Algorithm 2) that given z and G recovers R. If zi = 0 then the recovering algorithm might not know which of the three options for the randomness determined zi. More concretely, in the first step RecoverRand knows that the prefix of randomness R is between 0P and 1P . However, the filter (and the LLM) may have received training examples of both harmful and benign outputs, or an approximate description of H, during training. [2024] show that the DPO alignment algorithm only teaches a model to bypass toxic “regions” without removing toxic knowledge gained from the pre-training stage, making it possible to elicit this toxic knowledge with “jailbreaks”—changes to the prompt that lead a model from answer-refusal to generating harmful information. A Time-Lock Puzzle consists of a pair of algorithms Puzzle = (Gen, Sol) with the following properties: To compute re mod n it first evaluates ϕ(n) = (p − 1)(q − 1) and reduces the exponent e′ ← e mod ϕ(n). Then s = re′ mod n, which can be computed in only O(log(e′)) modular exponentiations. To do that, one would use a root function (Definition 8) that for x ∈ Z∗ n computes “√x” so that it is possible to compute r from s. Note that Sol computes s using t repeated squarings, each of which is inexpensive, but the full process requires Θ(t) sequential steps. That is any adversary who does not know the factorization of n must essentially compute each squaring step sequentially. The encryption algorithm uses k to map a message m to a ciphertext c, and the decryption algorithm uses the same key k to recover m from c. N → N we say that a pair of algorithms (Samp, RecoverRand) is an α- Recoverable-Randomness Sampling (RRS) scheme if • Samp(G) → y, given a language model G10 over token set T , Samp samples an output z ∈ T∗. , zi−1, before the if statement (step 8 of Algorithm 1) is executed, the distribution of q is uniform in [k]. However, step 4 of Algorithm 1 ensures that |q/k − pi| ≤2−P , so the errors accumulate as required, as for every (z0, . the lengths of a, band q from Samp are equal before the if statement (step 8 of Algorithm 2). a ≤lex q ≤lex b, where ≤lex is the lexicographic order, before the if statement (step 8 of Algorithm 2). Our construction is given in Algorithms 5 and 6, which are formal versions of Algorithms 3 and 4. Note that in Algorithm 5 (in contrast to Algorithm 3), to compute the puzzle s, an intermediate bijection ind (Definition 7) is used. The first step of G′ is to sample p, qat random from a set [1, k] (see Algorithm 5), where k = 2O(λ). For a message m ∈ {0, 1}lm (as in the statement of Theorem 7) and using the notation from Algorithm 5 we define R to be R ← h ∥ h(n) ∥ r ∥ v ∥ Q(s, v) ⊕ m, (1) where v ← {0, 1}O(λ) and Q(s, v) is a hardcore function with lm = Θ(log(λ)) output bits. 22 Note that the distribution considered in Lemma 2 is, a priori, different from the distribution of outputs of Algorithm 5, because in the actual algorithm we first sample r, not s. However, for the output filter result, the quality oracle could “run” in exponential time, and so the time-lock can be created with parameter 2λ.19 E Mitigation and Connections to Watermarking E.1 Watermarks First, we define wa