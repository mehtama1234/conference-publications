REAL METHOD of "SG2Loc: Sequential Visual Localization on 3D Scene Graphs" (arXiv 2606.11880v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

However, tra- ditional methods often require storing extensive image databases or point clouds, leading to sig- nificant overhead. Our method rep- resents the environment with a compact scene graph, where nodes represent objects (with coarse meshes) and edges encode spatial relationships. For each image in the localization phase, we ex- tract per-patch semantic features, predicting ob- ject identities. Each particle, rep- resenting a camera pose, projects the coarse ob- ject meshes from the scene graph into the image, assigning object identities to patches based on visibility. By lever- aging a compact scene graph and efficient se- mantic matching, our method significantly re- duces storage while maintaining performance on real-world datasets. As en- vironments grow both in scale and complexity, this burden becomes impractical for memory-constrained devices or applications with low bandwidth requirements. Nonetheless, global pose alignment typically depends on retrieving a dense 3D model or querying large-scale maps. Such approaches can be pro- hibitive in extended deployments, especially during frequent map updates or transfers on resource-constrained platforms. Early research on scene graphs primarily targeted scene understanding, reconstruction, and retrieval (Armeni et al., 2019; Johnson et al., 2015; Hudson & Manning, 2019). Building on these insights, we introduce a novel method for sequential visual localization that combines a lightweight 3D scene graph with a particle filter. • A new 3D scene-graph-based framework for sequential visual localization that jointly models semantic cues, geo- metric and photometric constraints. Related Work Visual localization is a long-standing problem in computer vision and robotics, with roots in early works on Structure- from-Motion (Kruppa, 1913; Moravec, 1980; Fischler & Bolles, 1981). Modern approaches can be categorized into single-image localization, SLAM-based methods, and se- quential localization methods. The current image is passed through the SceneGraphLoc (Miao et al., 2024) encoder to predict object labels for each image patch. Scene graphs first emerged as 2D representations of images, capturing objects and their relations for tasks such as image retrieval (John- son et al., 2015) and visual question answering (Hudson & Manning, 2019). Our approach builds upon the strengths of sequential localization methods and the light-weight na- ture of scene graphs to provide a new localization direction. The filter integrates acoarse, labeled 3D mesh (derived from a 3D scene graph) with the pre-trained SceneGraphLoc model (Miao et al., 2024) to compute observation likelihoods. Our method is not tied to a specific scene graph construction method, it only requires object meshes and semantic embeddings. At each timestep, the filter performs (i) a prediction step (motion model), (ii) an update step (observation model), and (iii) optional resampling, as described below in the next section. Patch similarity.We then compare the patch embedding ˆer,c from the query image It+1 (Section 3.1) with the object embedding ei assigned by raycasting. Color supervision.As we are given an RGB sequence as input, we can leverage photometric losses to further improve our particle state quality measurements. Combined score.The final weight is then computed by combining the semantic similarity scores(n) (from Sec. the particle filter works without retrieval (by uniformly sampling the space), and retrieval can stand alone as a place-recognition step. For completeness, we propose a sequential extension of (Miao et al., 2024) for this complementary scene-identification step. For each patch q∈ Qt, we compute its em- beddinge q using the pre-trained encoder. ACE and ACE+GS-CPR are shown faded on ScanNet since the ACE encoder was trained on parts of this dataset. HLoc is a hierarchical method combining image retrieval (10 nearest neighbors) with pose estimation from 2D-3D correspondences, requir- ing a large image database. Metrics.We evaluate the performance of our method and baselines using standard metrics for visual localization: Sequential visual localization.Table 1 shows that our method achieves the best joint recalls for all frame lengths on 3RScan. We excluded the scenes that the ACE encoder has been trained on from evaluation. Our method is roughly as good as ACE on ScanNet, especially for the longer seqeunces. Our method uses roughly 2500 particles on 3RScan and 3200 particles on ScanNet for the results reported in Table 1. Loc-NeRF, ACE and GS-CPR require training a separate map representation for each scene. The localization for SG2Loc consists of state transitions, particle updates (embeddings are computed per query image and raycasting is performed from all particles), and resampling steps, before post-processing with PnP. To reflect online deployment, both HLoc and our method are evaluated per keyframe, rather than selecting the maximum-inlier pose across a sequence. Our method significantly outperforms HLoc on the same inputs, demonstrating its practicality for robot localization on resource-constrained devices. We compare this extension to the original method, which performs localization using only the first image of the sequence. Additionally, we benchmark against state-of-the- art image retrieval methods, AnyLoc (Keetha et al., 2023) and CVNet (Lee et al., 2022). We follow the evaluation protocol used in (Miao et al., 2024), where, given a query image (or sequence), we retrieve the top- k scenes from a candidate set of either 10 or 50 scenes. We report recall at ranks 1, 3, and 5, measuring how often each method retrieves the correct scene among its top-k predic- tions (k∈ {1,3,5}). For our method, storage corresponds to the scene graph embeddings, whereas for CVNet and AnyLoc, it reflects the database of image embeddings. The first 3 ablations evaluate components of our method relative toSG2Loc w/ semantic. We report scene retrieval recall in the temporal setting (Rt) at 1, 3, and 5 indicating whether the correct scene appears among the top-k predictions, along with inference time (ms) and storage requirements for the map, demonstrating the efficiency of our approach. By leveraging semantic object descriptors and coarse meshes, our method efficiently refines pose estimates over time while significantly reducing storage requirements. The results for 50-frame sequences, shown below, demonstrate that our method can successfully localize in this s