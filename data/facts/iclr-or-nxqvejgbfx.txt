REAL METHOD of "CompMarkGS: Robust Watermarking for Compressed 3D Gaussian Splatting" (arXiv 2503.12836v6). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

However, compression techniques, especially quantization-based methods, degrade the in- tegrity of existing 3DGS watermarking methods, thus creating the need for a novel methodology that is robust against compression. To ensure reliable watermark detection under compression, we propose a compression-tolerant 3DGS water- marking method that preserves watermark integrity and rendering quality. We also propose a quantization distortion layer that injects quantization noise during training, preserving the watermark after quantization-based compression. Existing 3DGS watermarking methods (Chen et al., 2024b; Guo et al., 2024; Huang et al., 2025; Jang et al., 2025; Tan et al., 2024) embed watermarks by directly modifying Gaussian attributes, which makes the watermark vulnerable to loss when those attributes are altered during compression. To handle this problem, we propose CompMarkGS, an anchor-based 3DGS watermarking method. Our method leverages the anchor-based 3DGS architecture, which dynamically predicts Gaussian attributes from anchor points via multiple implicit multi-layer perceptrons (MLPs). Unlike prior work focusing 1 arXiv:2503.12836v6 [cs.CV] 29 Sep 2025 Initial PointsTraining 3DGS & Embedding Watermark 1011011… CompressionWatermarked 3DGSCompressed 3DGS Training Malicious Use & Identification zzImage Distortion Model Distortion CropJPEGBrightness Prune Clone Color WatermarkDecoder SuccessfulIdentification Deploy Figure 1: Owners embed the watermark into a 3DGS asset using our proposed method, then compress and distribute the model across various digital environ- ments. We propose a quantization distortion layer (QDL) that injects quantization noise into anchor attributes during training, teaching the watermark to be resilient. In addition to this structural enhance- ment, we design an HSV loss to address color artifacts that occur when embedding the watermark. Each compo- nent of our method—anchor-based 3DGS watermarking, the quantization distortion layer, and the frequency-aware anchor growing strategy—independently enhances watermark fidelity while pre- serving rendering quality, even under compression and various watermarking attacks. Furthermore, our method outperforms state-of-the-art approaches across various watermark message lengths. • We propose CompMarkGS, a compression-tolerant anchor-based watermarking method for 3DGS that embeds a learnable watermark embedding feature into the anchor feature, preserving the model structure while ensuring high rendering quality and bit accuracy. • We introduce a quantization distortion layer that injects quantization noise during training, en- abling 3DGS watermarking to remain robust against quantization-based compression while main- taining high rendering quality. • We propose a frequency-aware anchor growing strategy that selectively expands anchors in high- frequency regions, which enhances rendering quality, and an HSV loss that mitigates color arti- facts to further improve rendering quality. These methods prune Gaussians with minimal impact on rendering quality and use codebooks to encode the attributes of Gaussians compactly. In this paper, we propose a watermarking method that is robust to model compression. The quantized watermarked anchor feature from the quantization distortion layer is then fed into individual MLPs to predict Gaussian attributes. Moreover, an HSV lossL hsv, and a frequency lossL f req, are applied to mitigate visual artifacts. 4 To overcome this limitation, we propose a quantization distortion layer (QDL) that simulates quantization-induced distortions, encompassing losses in watermark robustness and rendering fi- delity. While the original method uses this mechanism for model compres- sion, our work is the first to adapt it as a form of data augmentation to train a robust watermark. To selectively grow anchors in detail-rich, high-frequency regions, our method first transforms the rendered image I ′ and the ground truth imageIinto the frequency domain using the Discrete Fourier Transform (DFT), as shown in Fig. 4.4 OBJECTIVE FUNCTION Watermark message loss.As described in Sec.4.3, we use the pre-trained HiDDeN (Zhu et al., 2018) decoder to produce the watermark messageM ′, and the watermark message lossL msg is defined as a binary cross entropy (BCE) loss: Then, HSV lossL hsv is computed as the mean squared error (MSE) between the pixel value of rendered imageI(p)and the ground truth Igt(p)over the pixels in the setΩwhereM c(p) = 1: Lhsv = 1 |C||Ω| X c∈C X p∈Ω ∥Mc(p)·(I(p)−I gt(p))∥2 .(8) The overall color loss is defined as the average of the losses for each target color in the setC. The overall training loss for our method is given by: Ltotal =λ img(Lscaf f old+λ hsvLhsv +λ f reqLf req) +λ msgLmsg,(9) whereL f req= 1 |P| P p∈P Perror (p), as the mean of all pixel-wise SSIM losses.L scaf f oldrepre- sents the reconstruction loss defined in Scaffold-GS (Lu et al., 2024), which consists of one scale regularization and two rendering fidelity terms. Implementation details.Our method trains end-to-end on a single A6000 GPU. For the decoder, we use a pre-trained HiDDeN (Zhu et al., 2018) decoder for each bit-length and keep its parameters fixed during watermark training. Robustness for the image distortion.We evaluate the robustness of our method against the loss of the embedded watermark when the rendered image is subjected to various distortions. 3, while the accuracy for all methods decreases with longer messages, our method maintains signifi- cantly higher bit accuracy. In stark contrast to other methods, its minimal performance drop even at a 64- bit messages demonstrates our method’s higher ef- fective messages capacity and greater robustness. By integrating a quanti- zation distortion layer, we train the watermark to withstand quantization-based compression, and we introduce a learnable watermark embedding feature for anchor-based insertion, achieving both high security and high fidelity. First, we plan to extend our method to operate independently of 3D Gaussian Splatting compression, which would broaden its applicability to a wider range of scenarios. The entire training process is optimized to minimize the message reconstruction lossL msg, with an additional HSV-based perceptual lossL hsv used to prevent visual quality degradation. C.3 DETAILS OF TRAINING Our proposed CompMarkGS trains the anchor-based 3DGS from scratch with the watermark, in contrast to existing methods Huang et al. The lo