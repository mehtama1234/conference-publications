REAL METHOD of "Prior-aware and Context-guided Group Sampling for Active Probabilistic Subsampling" (arXiv 2607.07083v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Herein, we enhance A-DPS by integrating a deterministic (fixed) prior-informed sampling pattern de- rived from the training dataset, along with group-based sampling via top-k sam- pling, to achieve more robust optimization—a method we call Prior-aware and context-guided Group-based Active DPS (PGA-DPS). In this work, we enhance Active Deep Probabilistic Subsampling (A-DPS) architecture by incor- porating deterministic (fixed) subsampling based on prior knowledge of the training data with group sampling to achieve more robust optimization (Fig. We demon- strate that PGA-DPS exploits training-data priors via deterministic sampling and reinforces this with instance-adaptive group sampling, resulting in a smoother loss landscape and more stable optimiza- tion. Additionally, a stochastic greedy algorithm has been applied in MRI to obtain the sampling pattern that minimizes the reconstruction loss (Sanchez et al., 2020). However, greedy algorithm- based methods require pre-trained reconstruction capable of handling any subsampling pattern to thoroughly evaluate the loss. Self-Representation Learning with Sparse 1D-Operational Autoencoder (SRL-SOA) employs a sparse autoencoder model to learn a sparse representation using a Taylor-series expansion of non-linear transformation (Ahishali et al., 2022). it first performs filter-based interband redun- dancy analysis, then trains a Convolutional Neural Network (CNN) to assess classification perfor- mance of the chosen bands (Morales et al., 2021). Although tuning the relaxation withτis crucial for managing the bias-variance trade-off of the gradient estimator (Tucker et al., 2017), the DPS architecture has been shown to perform effectively with a fixed temperature during training (τ= 2). We analyze group sampling enabled by DPS-top-kin terms of the loss, which captures the Lipschitzness of the loss and reflects the smoothness of the optimization landscape. The Lipschitzness of the loss function for each DPS-top-1 and DPS-top-kis as follows: If the Lipschitz constant of task model at each iteration Lj ≥1, Lk ≤ kY r=1 Lr =⇒sup x ∥LossDPS-top-k(x)∥2 ≤sup x ∥LossDPS-top-1(x)∥2 Neural networks typically have Lipschitz constants much greater than one, except in the trivial near-identity case (Malherbe & Vayatis, 2017; Bartlett et al., 2018; Latorre et al., 2020; Shi et al., 2022). Moreover, the same analysis can apply to the Lipschitz constants of gradient loss∥∇Loss∥. 4.1.2 TASK MODEL A multi layer perceptron (MLP) with 5 layers is used for the classification networkf θ(·). The sampling networkg k(·)of an LSTM (Long Short-Term Memory) followed by a two-layer MLP, which encodes the current task context and generates the corresponding sampling 5 Published as a conference paper at ICLR 2026 mask. Both the classification network and sampling network are simultaneously trained using a categorical cross-entropy loss. Detailed model architecture and training settings are provided in the Appendix B.1. This likely arises because A-DPS trains a separate classifier for each sampled pixel, inflating its Lipschitz constant (as discussed in Theorem 1). The training split was further divided into training and validation set at a 9:1 ratio. 4.2.2 TASK MODEL For the classification modelf θ(·), we employed a four-layer convolutional neural network (CNN) followed by three MLP layers. The CIFAR-10 images are first masked according to the subsampling pattern and then fed into the network. We employed the same sampling network architecture and the same cross-entropy loss function as in the MNIST classification experiments. The detailed model architecture and training settings are provided in the Appendix B.2. 4.3.2 TASK MODEL For the reconstruction taskf θ(·), a deep unfolded proximal gradient method is employed to recon- struct the original image ˆXfrom the partially acquired measurementY(Mardani et al., 2018), by unrolling iterations of the proximal gradient algorithm into a feed-forward neural network: The sampling networkg k(·)consists of a 3-layer CNN, followed by global average pooling, an LSTM, and a single MLP, in sequence. In the loss-pass method, lines nearest to the DC (k-space origin) compo- nents are selected; in VDS, sampling density decays with distance from the k-space origin, concen- trating the samples centrally and tapering off toward the periphery. Afterwards, the corresponding proximal gradient net- work was trained using the fixed optimal mask obtained from the greedy algorithm (Greedy Prox.). The performance gain is particularly notable in the low-measurement regime, where the number of k-space lines is small, further demonstrating the robustness of our method under limited data conditions. 4.4 MRIRECONSTRUCTION WITH ACTIVE BASELINES 4.4.1 EXPERIMENT SETUP To further validate the effectiveness of our method, we conducted an additional comparison against reinforcement learning (RL)-based sampling strategies: We adopt the train- validation-test split protocol from (Van Gorp et al., 2021), yielding 34,742 training, 1,785 validation, and 1,851 testing images. PGA-DPS was trained for 5 epochs with a batch size of 1, following the training scheme provided in A-DPS (Van Gorp et al., 2021). The sampling networkg k(·)consists of a 4-layer CNN, with each layer followed by Batch normalization and ReLU activation. The output of the CNN is aggregated into a feature vector via global average pooling, which is then fed to an LSTM and a single-layer MLP to generate logits. The categorical cross-entropy loss is computed for 5 classes and the model with the highest mean in- tersection over union (mIOU) on the validation set is selected for further evaluation. Detailed model architecture and training settings are provided in the Appendix B.4. Three different DPS methods (DPS, A-DPS, and PGA-DPS) are implemented and compare to the conventional methods including uniform sampling, SRL-SOA (Self-Representation Learning with Sparse 1D-Operational Autoencoder) (Ahishali et al., 2022), and GSS (Greedy Spectral Selection) (Morales et al., 2021). A fast and accurate first-order method for sparse recovery.SIAM Journal on Imaging Sciences, 4(1):1–39, 2011. Lipschitzness of the loss function for each DPS-top-1 is as follows: On the other hand, the Lipschitzness of the loss function for DPS-top-kis ∥LossDPS-top-k(x)∥2 =∥f k(x∗)−f k(x)∥2 ≤L k ∥x∗ −x∥ 2 The Lipschitz constant of the loss function for DPS-top-kis simplyL k. Therefore, if the Lipschitz constant of task model at e