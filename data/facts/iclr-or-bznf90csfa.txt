REAL METHOD of "Curation Leaks: Membership Inference Attacks against Data Curation for Machine Learning" (arXiv 2603.00811v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

The resulting model is then trained solely on curated public data. Specifically, we introduce novel attacks against popular curation methods, targeting every major step: These findings highlight the previously overlooked inherent privacy risks of data curation and show that privacy assessment must extend beyond the training procedure to include the data selection process. This paradigm is also particularly appealing for sensitive domains, such as finance or health- care (Schäfer et al., 2024; Assefa et al., 2021), where the available training datasets are usually limited, which hinders the training of powerful ML models. The curated public dataset is finally used to train an ML model that outperforms models trained onall public data or achieves similar results with greater computational efficiency on target domain tasks. Therefore, we design and carry out custom membership inference attacks (Shokri et al., 2017; Carlini et al., 2022a) on data points from the target set at every stage of the pipeline, and demonstrate that each step can leak private information. Private Data Trained Model Public Subset Public Data Curation Scores for Binary LiRACustom Attacks Custom End-to-End Attacks LiRACustom Attacks Figure 1:We attack private data T used to curate a public dataset D.We show that the scores s, top-scoring subsets ˜D and even trained modelsMleak membership information. Concretely, we design and eval- uate attacks against 1) the cu- ration methods’ released scores, 2) the selected public subsets, and 3) the final trained model, as shown in Figure 1. Finally, to attack the trained model, we insert a fewfingerprintedsamples F into the public pool whose curation score is highly influenced by a particular target, and which imprint a measurable signal in the model if trained on. We present the first comprehensive privacy analysis of data curation pipelines, highlighting that curation leaks private information at each step: We design custom membership attacks for each curation step, showing that both curation scores and curated datasets leak membership information without pipeline modifications. 2 BACKGROUND ANDRELATEDWORK Data Attribution.The influence of training samples on the behaviour of a trained ML model on some target data point is determined via data attribution methods. TheDataModelsapproach (Ilyas et al., 2022) trains many models on different training data subsets and then fits an influence estimator. To make that applicable to deep learning, they formulate the model training as a 2 Published as a conference paper at ICLR 2026 logistic regression on gradients of a trained model. With that approach, TRAK is more efficient than DataModels, but still requires training in the order of ∼20 models and another backward pass for each on the full training set. Ilyas & Engstrom (2025) introduce an alternative which requires training just one model, but requires a full backward pass on the entire training datasetfor each target sample. By selecting those training samples with the highest positive attribution scores, data attribution methods can serve as methods for data curation (Engstrom et al., 2024). (2025) propose attributing training data performance to target utility by computing correlations on the loss of the training data and the target utility. the performance and/or compute efficieny for utility on T is improved by training on ˜Dinstead of the fullD. It trainsshadow modelson various subsets of the target data and then fits distributions to the observed behaviour of the models with a target vs. Then, to attack a model, it compares the likelihoods of the observed behaviour under the distribution of the target being in the training vs. ⊆ T Public poolD, target setT, curation algorithm Passive observation Scoress∈R |D| Subset(Section 3.4) Passive observation Selectionm∈ {0,1} |D| Final Model(Section 3.5) Injectfingerprinted samples F into D before curation Trained model M (black- box query access) Table 1:Threat model summary.All adversaries aim to infer membership in the private curation target set Tsel (nottraining-set membership). For each t∈ Twe compute µin ∈R N as the average of si for every t∈ Ti and µout ∈R N where t /∈ Ti. Since training large models for each experiment is intractable, we validate the signal once and reduce the end-to-end attack to observing whether the fingerprinted samples were selected during curation (i.e., f∈ ˜D). We now detail how we construct F and compute membership scores for each curation method. For each candidate fingerprint f and target ti, we compute a correspondence score balancing two objectives. For each candidate-target pair, we compute a signal-to-noise ratio measuring how sensitively the fingerprint’s score responds to that target’s presence versus interference from other targets. Algorithm 1 provides the complete TRAK attack procedure; full mathematical details appear in App. The Image-based attack procedures are detailed in Algorithms 4 and 5. Gradients for TRAK are obtained from a model trained on the full pool dataset D using contrastive training as in Gadre et al. We propose DP adaptations of both curation methods to yield(ε, δ)- DP guarantees via the Gaussian mechanism (Dwork et al., 2006b). What hinders reproduction is the computational cost associated with running curation and training algorithms on dataset sizes that are relevant to the topic of data curation. The algorithmic foundations of differential privacy.Foundations and trends® in theoretical computer science, 9(3–4):211–407, 2014. Following the DataComp methodology, we use embeddings from OpenAI’s pretrained CLIP ViT-L/14 model (Rad- ford et al., 2021) to compute the image representations ϕ(·). For each pool sample xi ∈ D, the curation score is computed as the maximum cosine similarity to any target sample in T ,i.e.,the pool sample is scored based on its closest (most similar) target sample. A.2 TRAK The TRAK algorithm (Park et al., 2023) for computing influence scores is formally presented in Algorithm 2. First, we replace shadowmodelswith shadow curation runs,i.e.,instantiations of the curation algorithm using different random subsets of T , s.t. (b) We can then compute membership inference scores based on a likelihood ratio attack by computing the log-ratio of the in and out score distributions. B.2 METHOD-AGNOSTICATTACKS To attack the curation methods, we employ the likelihood-ratio attack (LiRA) from Carlini et al. We can then compute the log-ratio of the of th