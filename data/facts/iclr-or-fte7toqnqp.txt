REAL METHOD of "Inter-Agent Relative Representations for Multi-Agent Option Discovery" (arXiv 2512.24827v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Our approach builds on the inductive bias that synchronisation over agent states provides a natural foundation for coordination in the absence of explicit objectives. Our approach builds on the intuition that in the absence of an explicit objective, synchronisation over state features represents a natural basis for coordination. Given an eigenvector e, the intrinsic reward function for transitioning from discrete state s to s′ can be computed as re(s, s′) =e(s ′)−e(s) . Subsequent work (Wu et al., 2018; Jinnai et al., 2020; Wang et al., 2021) extends this framework to non-tabular domains by approximating the eigenvectors through neural networks trained to minimise objectives derived fromgraph-drawing theory(Koren, 2005). To alleviate this problem, we propose approximating theFermatstate, the state of minimum summed distance to each agent, through a parameterised function, ϕ:S → S∗, which we call the Fermat encoder and train by minimizing the following objective: The Fermat encoder ϕ and the distance approximator dθ are trained concomitantly by enforcing astop-gradientoperator on the distance estimator’s parameters ( θ) when integrating it in the Fermat encoder objective (Equation 4). 3.2 MULTI-AGENT OPTION DISCOVERY ON RELATIVE STATES Eigenoption discovery, introduced in Section 2.2, consists of two main steps: To address this issue, we compute then-distance along each feature dimension of the single-agent state space and encode the 1Single agent states can contain general environmental information. Option discovery oninter-agentrelative representations for a state factorisation function g, Fermat encoderϕ, state distance encoderd θ and a graph Laplacian eigenvector approximatorµ. We then use a linear projection layer to approximate the overall state-distance when training dF θ . (2023)2 to generate the set of options, but precede the eigenvector estimation by first representing joint states as their disentangled 2We use the ALLO method from Gomez et al. Both the n-distance encoder training and joint option discovery are done prior to the generic training. Following the procedure introduced in Section 2.2, we use these eigenvectors as intrinsic reward signals for option-policy training. Figure 3 shows a visualisation of the multi-agent options trained to follow the positive and negative versions of the first two eigenvectors for a team of four agents. The top row compares IQL+IARO against option-free baseline algorithms, while the bottom row compares it against IQL augmented with other option discovery methods. To train the n-distance encoder and the graph Laplacian eigenvector approximator ALLO (Gomez et al., 2024), we used a dataset of 500,000 transitions sampled from a random joint policy in each domain. Once estimated, we trained joint option policies based on these eigenvectors for one million steps, equivalent to 5% and 10% of the total training time in each task. These results highlight the benefits of joint options in overcoming the limitations of independent policy learning in multi-agent tasks by encouraging cooperation through the set of pre-computed coordination behaviours; note that our method does not rely on centralization (MAPPO) or value decomposition (VDN) to 8 Inter-Agent Relative Representations for Multi-Agent Option Discovery Figure 5: We report IQM scores over 15 seeds and 64 evaluation episodes at the end of training for each configuration, with standard deviations shown as error bars. However, we did observe a slower convergence at the start of training, which we attribute to known challenges of training with options underglobalinitiation sets (Jong et al., 2008; Machado et al., 2023). Figure 4 compares the resulting options across both domains and shows that the options discovered by our method better equip teams of agents with the cooperative skills necessary to solve these downstream tasks. While in LBF, the domain with simpler state definitions, both methods performed similarly, in Overcooked, the multi-distance method proved more effective, as agent states consist of multiple features of diverse semantics, highlighting the benefits of the disentangled n-distance representation. In contrast, our method learns inter-agent relations automatically, without any restrictions on feature semantics. This leaves the problem of discovering strongly coordinated behaviours still open, which is precisely what we aim to address with our method. We define this point as theFermat state and propose a method that estimates it explicitly. The discovery of intermediate coordination patterns (options) that aid in the reliable and efficient completion of such tasks by AI agents is central to our efforts; our objective (in this paper) is to develop a method capable of identifying these patterns from a limited number of interactions with the environment. 14 Inter-Agent Relative Representations for Multi-Agent Option Discovery A.3 CMIMINIMISATION ALGORITHM Algorithm 1 follows the framework of Dunion et al. The algorithm first samples a batch of state pairs from the same time step in a trajectory and encodes their multi-dimensional distance. A.4 EPISODICREWARD ANALYSIS& ADDITIONAL RESULTS Figure 6 presents an extended analysis of our method using episodic returns as the evaluation measure. These scenarios, together with the original four, were selected because the IQL baseline finds it difficult to compute solutions for these scenarios (Papoudakis et al., 2021; Rutherford et al., 2024), thereby highlighting the improvements achieved by our approach. The first five non-trivial eigenvectors resulted from eigenoption discovery on raw joint states, through the Kronecker product of single agent eigenvectors, and the two proposed inter-agent relative representation- based methods. Figures 8-10 provide a more extensive analysis of the first five non-trivial eigenvectors for each of the four frameworks: The first five non-trivial eigenvectors resulted from eigenoption discovery on raw joint states, through the Kronecker product of single agent eigenvectors, and the two proposed inter-agent relative representation- based methods. This is a consequence of the orthogonality constraint imposed by estimating multiple eigenvectors per cycle while using graph-drawing–based objectives, ALLO (Gomez et al., 2024). The first five non-trivial eigenvectors resulted from eigenoption discovery on raw joint states, through the Kronecker product of single agent eigenvectors, and the two proposed int