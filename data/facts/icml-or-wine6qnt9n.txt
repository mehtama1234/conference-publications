REAL METHOD of "CoMem: Context Management with A Decoupled Long-Context Model" (arXiv 2605.30842v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We propose a k-step-off asynchronous pipeline that overlaps the memory model’s sum- marization with the agent’s inference, effectively masking the latency of context processing. To en- sure robustness under this asynchronous setting, we introduce a reward-driven training strategy that aligns the memory model to capture suffi- cient statistics for the agent’s decision-making. Despite the increasing attention to extend the effective con- text window, there has been surprisingly little attention paid to the severe computational costs introduced by long-context processing during agentic inference. Sliding-window attention (Fu et al., 2025) restricts the agent to a fixed local context, inherently sacrificing the abil- ity to recall distant dependencies, such as the initial user goal or a bug identified thousands of steps prior. Similarly, Sparse Attention mechanisms (Xiao et al., 2024) and KV Cache Quantiza- tion (Hooper et al., 2024) reduce the compute and memory bandwidth requirements by sparsifying interactions or low- ering numerical precision. While these methods provide substantial throughput improvements, they do not solve the the redundancy of re-encoding or re-attending to a massive, largely static interaction history for every single decoding step. To ensure this com- pressed state effectively guides the agent, we introduce a reward-driven alignment strategy that trains the memory model to capture the ”sufficient statistics” required for opti- mal decision-making. We design a k-step-off pipeline that overlaps memory summarization with agent execution, masking the latency of context pro- cessing and reducing the decoding overhead by up to 1.4×. • We introduce a novel training methodology that aligns the memory model using a functional equivalence re- ward, ensuring the compressed summary recovers the full-context agent’s policy without expensive online interaction. The Prefill Stage serves as the initial step where the key-value cache (KV cache) for the prompt (also called prefix) sequences are built for each KV head in the Transformer architecture1. Given hidden states X∈R l×dmodel, each attention layer computes Q=XW q ∈R l×hq×dh, K=XW k ∈R l×hkv×dh, and V=XW v ∈R l×hkv×dh after reshaping, where hq is the number of query heads, hkv is the number of KV heads, and dh is the head dimension. The Prefill Stage is usuallycompute-bound, since it involves matrix-matrix mul- 1Note that here we discriminate KV head from attention head because of Grouped Query Attention (GQA) 2https://lmcache.ai/kv_cache_calculator. Context Management with A Decoupled Long-Context Model (a) Standard Agent InferenceFull TrajectoryLarge ModelFull history decoding àlarge KV cache readsAction (b) CoMem(Ours)Full TrajectorySmaller Model Compressed Text Recent Steps Large ModelActionk-step-off asyncRL-trained memory Short context decoding àreduced KV cache reads HBM Bandwidth Bottleneck Lower KV cache traffic Figure 1.COMEMframework: We useTime to First Token(TTFT) to calculate the time spent from waiting the first token to emit to represent the prefilling latency. For each step t, the model processes the cur- rent prefix and concatenates a new KV pair: By decoupling the long- context processing from agentic process and then overlap- ping them via k-step-off generation, our method effectively alleviates the memory-bound bottlenecks and improves the end-to-end performance while keeping most of the accuracy. Next, we present the key insights of our proposed framework COMEMwhich includes a novel k-step-off in- ference strategy (§3.2), and a new RL training pipeline that 3 COMEM: First, the total wall-clock time at each step is predominantly attributed to LLM execution, whereas envi- ronment execution constitutes a negligible fraction. Second, the LLM execution time exhibits a non-monotonic trend, increasing progressively from the initial steps up to approx- imately step 20, after which it begins to decrease toward the final steps. k-step-off PipelineTo mitigate this sequential bottleneck, we propose a novel k-step-off asynchronous pipeline (Algo- rithm 1), illustrated in Figure 4. Larger k amortizes the un- cached prefilling cost over more steps (as shown in Figure 4, comparing 1-step-off with 2-step-off), at the expense of in- creased staleness in the summary. Algorithm 1k-step-off Pipeline Require:Agent Modelπ, Memory Modelf, Retain Turnsk 1:Initialize:s← ∅,t←1,P mem ←None 2:whilenot finisheddo 3:{Synchronization (everyksteps)} 4:ifP mem is completethen 5:s←result(P mem); invalidate KV cache 6:P mem ←None 7:end if 8:{Agent Inference} 9:ifKV cache validthen 10: Executea t, observeo t+1 17:τ t+1 ←τ t ∪ {at, ot+1} 18:{Launch Memory (async, everyksteps)} 19:iftmodk= 0then 20:P mem ←asyncf(τ ≤t) 21:end if 22:t←t+ 1 23:end while 3.3. Context Management with A Decoupled Long-Context Model 𝑃!(𝑜")+𝐷!(𝑎")𝑡! 𝑃#𝑜"+𝑎"+𝑜$+𝐷#(𝑠$) 𝑡" 𝑡#𝑃!(𝑜$)+𝐷!(𝑎$)𝑃!𝑠$+𝑎$+𝑜%+𝐷!(𝑎%) 𝑡$𝑃!𝑠%+𝑎%+𝑜&+𝐷!(𝑎&) 𝑃#𝑎$+𝑜%+𝐷#(𝑠%) PrefillingDecoding 𝑃!(𝑜")+𝐷!(𝑎")𝑡! 𝑡" 𝑡#𝑃!(𝑜$)+𝐷!(𝑎$)𝑃!(𝑜%)+𝐷!(𝑎%)𝑡$𝑃!(𝑜&)+𝐷!(𝑎&)𝑡%𝑃!(𝑜')+𝐷!(𝑎') 𝑃!𝑠&+𝑎&+𝑜'+𝐷!(𝑎') 𝑡%𝑝𝑟𝑒𝑓𝑖𝑥=𝑜"+𝑎"𝑝𝑟𝑒𝑓𝑖𝑥=𝑜"+⋯+𝑎$ 𝑝𝑟𝑒𝑓𝑖𝑥=𝑜"+⋯+𝑎&𝑝𝑟𝑒𝑓𝑖𝑥=𝑜"+⋯+𝑎%Full-context 1-step-off UncachedPrefilling 𝑃#𝑎%+𝑜&+𝐷#(𝑠&)𝑃#𝑎&+𝑜'+𝐷#(𝑠') 𝑃!(𝑜")+𝐷!(𝑎")𝑡! 𝑃#(𝑜"+𝑎"+𝑜$)+𝐷#(𝑠$) 𝑡" 𝑡#𝑃!(𝑜$)+𝐷!(𝑎$)𝑃!𝑠$+𝑎$+𝑜%+𝐷!(𝑎%) 𝑡$𝑃!(𝑜&)+𝐷!(𝑎&)𝑝𝑟𝑒𝑓𝑖𝑥=𝑜"+𝑎" 𝑃#(𝑎$+⋯+𝑜&)+𝐷#(𝑠&) 𝑝𝑟𝑒𝑓𝑖𝑥=𝑠$+𝑎$+𝑜%+𝑎%𝑃!𝑠&+𝑎&+𝑜'+𝐷!(𝑎') 𝑡% 2-step-off Agent: For 1-step-off, every step after the first requires uncached prefilling due to the continuously updated summary. For 2-step-off, the uncached prefill occurs only once per cycle, with the intermediate step reusing the cached KV prefix. For each step t in a trajectory, we record the ground-truth ac- tion a∗ t =π(· |τ <t) produced by the full-context agent. given a candidate summary s=f(τ <t−k), the frozen agent produces ˆat =π(· |s, Ct), and we define R(s) =sim(ˆat, a∗ t )(8) 6 COMEM: Specifically, following (Jain et al., 2025), we train our memory model using R2E-Gym-Lite and then test on 500 github issues on the SWE-Bench-Verified test set. We use the default scaffold provided by R2E-Gym and set maxi- mum steps to 40 with an extended allowance to 100 steps to let the LLM submit the answer. Speedup Peak Speedup 16 28.4 28.9 1.02×1.14×(step 62) 32 42.9 55.7 1.30×2.03×(step 56) 64 52.3 88.8 1.70×4.95×(step 61) Table 3.KV cache utilization and prompt length comparison. un- der concurrency 16, the two approaches are comparable as memory bandwidth is not yet saturated; under concurrency 32, COMEMbecomes consistently fast