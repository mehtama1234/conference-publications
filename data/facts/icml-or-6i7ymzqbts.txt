REAL METHOD of "BioFormer: Rethinking Cross-Subject Generalization via Spectral Structural Alignment in Biomedical Time-Series" (arXiv 2605.22468v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

1College of Computer Science, Nankai University, Tianjin, China 2College of Cyber Science, Tianjin Key Laboratory of Interventional Brain-Computer Interface and Intelligent Rehabilitation, Key Lab of Data and Intelligent Sys- tem Security, Frontiers Science Center for New Organic Matter, Nankai University, Tianjin, China. Second, adversarial methods explicitly encourage subject-invariant features by training a subject discriminator with gradient reversal layer (GRL) (Ganin et al., 2016) to penalize subject predictability while retaining label-relevant information (Zhou et al., 2024). can we align these transformations to suppress subject-specific variability while preserving task-relevant oscillatory structure? Specifically, we introduce the Frequency-Band Alignment Module (FBAM), which models subject variability as struc- tured spectral drift and performs adaptive frequency-band alignment in the Fourier domain. To further stabilize cross-subject represen- tations, we propose Sample-Conditional Layer Normaliza- tion (SCLN), which adaptively calibrates sample-level fea- ture statistics to mitigate residual distribution shifts with- out subject supervision. In the cross- subject setting, subjects are partitioned into disjoint training Training Set Test Set Training Set Test Set Subject-Dependent Cross-Subject S1 S2 S3 S4 S1 S2 S3 S4 UnseenSeen Figure 2.Comparison between subject-dependent and cross- subject evaluation settings.Left: and test sets, denoted by Strain and Stest, respectively, with Strain ∩ Stest =∅ , as shown inFigure 2. A model fθ is trained using samples from subjects in Strain and evaluated on samples from previously unseen subjects inS test. Given an input segment Xi ∈R T×C , BioFormer first applies an embedding layer, followed by Pyramid Convolutional Em- bedding (PCE) to generate multi-scale features {Xi,j} with Xi,j ∈R Tj ×D, where Tj ∈ {T 2 , T 4 , T 8 } via progressive con- volutional downsampling (Details in Appendix B.1). The resulting multi-resolution features are fed in parallel into a 6-layer Hybrid Transformer Encoder, where each layer al- ternates between temporal self-attention and the Frequency- Band Alignment Module (FBAM) (Section 4.2). The three parallel encoder outputs are concatenated to form h, which is then computed by Sample Conditional Layer Normal- ization (SCLN) to obtain hout (Section 4.3). Frequency-Band Alignment Module The Frequency-Band Alignment Module (FBAM) is the core component of BioFormer, designed to explicitly model and align subject-specific variability via band-wise dynamic modulation rather than individual frequency component. Rethinking Cross-Subject Generalization via Spectral Structural Alignment in Biomedical Time-Series TimeBiomedical Time-Series PCE FBAM Attention SCLN Classifier Frequency-Band Alignment Module (FBAM) Channel ×L Detach Gradient Pyramid Convolutional Embedding (PCE) ×3 Embedded Representation D ... Magnitude Spectrum mean var energy index Embedding Output Bandwidth Statistics Cross-Band Attention MLP MLP MLP Phase Spectrum Sample- level LN Mean MLP Sample Conditional Layer Normalization (SCLN) Concat ADD Dot Mul : Down sample Figure 3.Overall architecture of BioFormer.The model consists of four main components: (1) a Pyramid Convolutional Embedding (PCE) module, (2) a Frequency-Band Alignment Module (FBAM), (3) a hybrid Transformer encoder that alternates FBAM and temporal self-attention layers, and (4) a Sample Conditional Layer Normalization (SCLN) module followed by a classification head. For each band Bm, we compute a descriptors m ∈R 1×5: To mitigate this effect, we in- troduce Sample Conditional Layer Normalization (SCLN), which complements FBAM by performing sample-adaptive calibration at the encoder output. Given an intermediate representation h∈R T×D , SCLN first applies standard layer normalization (LN), ˆh= LN(h) . Concretely, we compare FBAM with representa- tive, plug-and-play DG methods (GRL (Ganin et al., 2016), MMD (Long et al., 2015), CORAL (Sun & Saenko, 2016), SubjectNorm (Rayatdoost et al., 2021), MixStyle (Zhou et al., 2021b), and DSU (Li et al., 2022)) that promote fea- ture alignment through adversarial or statistical objectives. For each fre- quency band b, we define the intra-class discrepancy Intra(b) as the standard deviation of band-wise average spectral power across subjects within the same class, and the inter-class discrepancy Inter(b) as the maximum differ- ence in band-wise average spectral power between classes. As shown inFigure 6, FBD is computed from the power spectra of the final-layer embeddings. Fol- lowing a standard subject-probing protocol, we freeze the trained backbone and train a lightweight MLP probe to pre- dict subject identities from the extracted features. Appendix B details the architecture of BioFormer, including the Pyramid Convolutional Embedding (PCE), the purely time- domain baseline TSAM for controlled comparison, and the complete FBAM forward computation with its corresponding algorithm. Given an input segment Xi ∈R T×C , where T denotes the sequence length and C the number of channels, PCE first maps the input into a latent space via a token 13 BioFormer: Finally, PCE produces a set of multi-scale embeddings {Xi,1, Xi,2, Xi,3}, which serve as the hierarchical inputs to subse- quent frequency-band alignment and attention encoding modules. Temporal Segment Alignment Module (TSAM) TheTemporal Segment Alignment Module (TSAM)is a purely time-domain alignment module introduced as a controlled baseline to contrast with FBAM. For each temporal segment, TSAM computes a set of low-order statistical descriptors that summarize its global characteristics, including magnitude, variability, peak response, and energy. We then partition the non-DC frequencies into K contiguous bands {Bk}K k=1 and compute band-wise statistical descriptors (e.g., moments, extrema, and energy), which are stacked and projected to obtain compact band context embeddings. All datasets are evaluated under a unified cross-subject setting, where subjects are split at the identity level into training, validation, and test sets, and all samples from the same subject are assigned to the same split. Each trial is first normalized using a standard scaler and then segmented into nine half-overlapping 1-second samples, yielding 5,967 samples in total. For subject-independent evaluation, subjects are randomly split into training, validation, and test sets with a 60/20/20 ratio, and all sample