REAL METHOD of "Scaling Laws of SignSGD in Linear Regression: When Does It Outperform SGD?" (arXiv 2603.02069v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We express the risk as a function of model size, training steps, learning rate, and the feature and target decay parameters. We then obtain compute-optimal scal- ing laws under the optimal choice of learning rate. (2020) observe that the language model cross-entropy loss scales as a power-law of model sizeMand number of stepsNin terms of the risk formulaR(M, N)≂M −τ1 +N −τ2 for some τ1, τ2 >0. 1 Also, they observe that loss scales as the power of training compute, under optimal allocation of compute between model size and number of steps. warmup-stable-decay schedules.Colored lines represent the training trajectories of each algorithm, and black lines de- note the compute-optimal curves. SignSGD achieves a steeper compute-optimal slope than SGD (left panel), and warmup-stable-decay scheduling sharpens the compute-optimal slope relative to a con- stant schedule (right panel), for some parameter configurations. We derive a scaling law of signSGD with constant learning rates involving three variables (model sizeM, training stepsN, learning rateγ 0) and two PLRF model parameters (feature decayα, target decayβ); see (12). Under the fixed compute budget, we balance model sizeMand training stepsN, and optimize over learning rateγ 0. This allows us to characterize the compute-optimal loss decay rate and optimal model size with respect to the compute budget (see Table 1). (2020) document power-law loss trends over multiple or- ders of magnitude and simple budgeting rules linking model size, data, and compute. (2022) argue that many LMs were under-trained on tokens and propose data-optimal scaling that substantially improves accuracy at fixed compute. (2024) derive a scaling-law formula for one-pass SGD, whereM,N, andγ 0 denote the model size, number of training steps, and learning 2 Published as a conference paper at ICLR 2026 rate, respectively, andαandβare the feature- and target-decay parameters. (2024) explain thatD SGD al (N, γ0)represents thealigned feature loss, as it coincides with the loss for a diagonal sketch matrixS(see Section 2.2 for a formal description). They also explain that DSGD dis (M, N, γ0)corresponds to thedistorted feature loss, arising from projection with a random matrixS, and it decays more slowly than the aligned feature loss. Scaling Behavior of Linear Models in the Context of Kernel Methods.The power-law settings for data and targets adopted in our work are deeply rooted in the literature on kernel methods and their finite-width approximations. entriesN(0,1/M), is drawn once and then held fixed throughout training; we refer toM (withM≤d) as the model size. L(N) = MX i=1 ri(N) +∥H 1/2w⊥∥2.(5) In Appendix E.1, we derive the one-step update formula for signSGD on a quadratic objective, using a second-order Taylor expansion and sign–Gaussian identities. Then by (5) our risk is decomposed as L(N) =L drift(N) +L noise(N) +∥H 1/2w⊥∥2 | {z } approx .(11) 4 MAINRESULTS 4.1 LOSSFORMULA FORCONSTANTLEARNINGRATE We now analyze (11) to express it in the form ofR(M, N, γ0), the loss afterNsteps with constant learning rateγ 0 and model sizeM; time-varying schedules are discussed later. For theN-exponent inD al andD dis, when the absolute value of the exponent isxfor SGD, then it changes to 2 2−x xin signSGD, which is strictly larger thanx. Settingγ 0 =M −e is without loss of generality, since in the compute-optimal case bothMandNare expressed as powers of the total computef. The maximal rate is the largest step that yields a stable (non-exploding) recursion; for signSGD, it leads to a zero compute-optimal slope (see Appendix F.1). Comparison of Compute-optimal Results.For the intersection of Phase Aa, Ab, Ac, Ba and Phase I, II, the compute-optimal slopeη(α, β)and optimal model sizeM⋆ are the same for signSGD 7 Published as a conference paper at ICLR 2026 Table 1: In other words, the learning rate increases linearly for the firstwN steps, stays constant for the next(p−w)Nsteps, and finally decays as a polynomial of exponentc for the remaining(1−p)Nsteps. In detail, we expect Adam to follow the same asymptotic loss formula (12) with signSGD, and therefore to follow the same compute-optimal scaling law with respect to FLOPSfin the Ta- ble 1. We also conducted an experiment on Adam and checked that the exponents in the Table 1 and the measured compute-optimal loss exponents and optimal model size exponents for Adam match well (see Figure 24). Train- ing compute-optimal large language models.Advances in Neural Information Processing Systems, 35:30016–30030, 2022. We first derive a one-step update formula and convert it to an ODE to get an integral equation. Then we set a proxy of the loss function and verify that it satisfies the integral equation. (6) In Appendix F we discuss the maximal learning rate deferred from the main text, and derive the optimal learning rate, compute-optimal loss, and optimal model size in Table 1. Future Work.Combining signSGD with dimension-adapted acceleration (Ferbach et al., 2025) and extending the framework to more complex architectures (e.g., two-layer linear networks or self-attention) are promising avenues. (2025) investi- gate the scaling law of a two-layer linear neural network with projected gradient descent, and argue the benefit compared to the PLRF model, which is one-layer. Lastly, they derived a scaling law where the model size goes to infinity; in contrast, our scaling law covers both finite model size and infinite limit by representing the loss as a function of model size, number of steps, and learning rate. They analyze the Early Stop- ping and that concept is closely related to the number of optimal stepsN=f/M ⋆ under fixed compute in our paper. The strength of our paper compared to theirs is that we provide an asymptotic loss function with model size and number of steps (which is the same as sample count in one-pass setting), while they 15 Published as a conference paper at ICLR 2026 provide an upper bound of loss by a polynomial of the sample count. They represent the expected loss as a function of the number of online stepsT. Their paper investigates the universal polynomially decaying step size and constant step size depending on the number of online stepsT. (i) we consider a model size parameterM; (ii) we cover the regime2α <1; (iii) we derive the asymptotic loss formula rather than an upper bound; (iv) we consider the compute- optimal aspect. Figure 6:comparison of SGD and signSGD on Compute-Optimal Scaling.Colored lines rep