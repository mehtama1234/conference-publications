REAL METHOD of "LAST: Bridging Vision-Language and Action Manifolds via Gromov-Wasserstein Alignment" (arXiv 2606.11221v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

To resolve this incompatibil- ity, we introduceLAST(Lie-algebraicAction SpaceTokenizer), which reconstructs the action space to establish local metric compatibility with the VL modality via a two-stage transformation: (b)LASTmaps actions to the tangent space se(3) for locally additive residuals, then performs covariance-aware whitening to reduce anisotropy. Consequently, Euclidean resid- uals and linear mixing can produce geometrically invalid rotations and mismeasure physical discrepancy (Zhou et al., 2019), causing off-manifold mixing and cross-modal mis- alignment.(2) Statistical Mismatch.VLM embeddings are trained contrastively and compared via cosine similarity on ℓ2-normalized features (Radford et al., 2021), encouraging a locally isotropic hyperspherical geometry (Wang & Isola, 2020). In contrast, action patterns are strongly anisotropic along dynamics-constrained directions (Sola et al., 2018). As a result, using the isotropic VL metric to retrieve ac- tion neighbors selects false neighbors across the ellipse’s short axis while missing true neighbors along the principal axis. This neighbor drift breaks local metric consistency and destabilizes training. To realize this view, we presentLAST(Lie-algebraicAction SpaceTokenizer), which encodes manifold-valued actions to facilitate alignment with the semantic space. Building upon this foundation, we integrateLASTinto a unified VLA training framework to enforce structural align- ment between the semantic and action spaces. We first pre-trainLASTto encode continuous trajectories into a compact discrete space via Global Topological Lineariza- tion, then apply Local Metric Discretization to hierarchically cluster motion patterns into discrete action tokens. • We introduceLAST, a tokenizer that establishes lo- cal metric compatibility between action and semantic spaces via Lie-algebraic linearization and statistical whitening. Structured VQ-based methods (Lee et al., 2024) rely on Eu- clidean metrics that assume isotropic distributions, creating a statistical mismatch with the highly anisotropic nature of robotic motion. For notation simplicity, we introduce state s= [o,l] . The VLA is trained to minimize the negative log-likelihood: ˆz= R VQ(z) = LX l=1 c(l) kl ,wherec (l) ∈ C(l).(2) Here, C(l) denotes the codebook at layer l, and c(l) kl is the codebook vector retrieved fromC (l) at indexk l. GLOBALTOPOLOGICALLINEARIZATION To resolve the non-additivity issue, the first objective is to map the non-Euclidean action manifold into a globally consistent linear space. Coarse Schema Selection.We first partition the continuous linear space into local neighborhoods by quantizing z with the Layer 1 codebook C(1) ={c (1) k }K k=1. To enable differentiable switching between these local charts, we compute the soft-assignment probabilityp k(z)using a temperature-scaled softmax: value ① -1 0 1 ? ∈ ℛ??×? 10 steps 20 steps 30 steps non-additive variable-length action traj. 10 steps 20 steps 30 steps timestamp out of manifold without physical meanings Eq. in the manifold · Figure 2.Overview of LAST Tokenization Pipeline.In Global Topological Linearization, the variable-length action trajectories are first linearized into the Lie-algebraic tangent space and then abstracted into fixed-length B-spline control points. Tokenizer Pre-training.We first train the tok- enizer T to minimize a compound objective consisting of reconstruction loss and commitment loss. VLA Dual-Objective Training.The VLA policy πθ bridges vision-language inputs (o,l) to actions via a dual-head architecture. Let h be the final-layer hidden state at the action step shared by both heads: For tokenizer pretraining, we train for 5 epochs to update the codebooks with loss weight λ1=0.5, using a B-spline tem- poral basis with Tc=16 and a codebook size of K=1024. Method Space MAE (↓) CR (↑) CU (↑) Binning Euclidean 4.2e-2 1.0× N/A FAST Frequency 1.8e-2 4.0× 78% RVQV AE Euclidean 1.1e-2 7.4× 51% LAST (Ours) Tangent 0.6e-2 8.0× 96% of 256 on 8×A800 GPUs, using loss weight λ2=10. Gains in Generalist VLA Learning In tokenizer-based methods, “Discretized” refers to using predicted tokens directly for action generation, while “Con- tinuous” refers to generating actions based on trained hidden states with an additional Diffusion Transformer (DiT) head, (a) LAST (Ours). se(3)W Spatial Object Goal Long ✗ ✓ 88.5 96.1 93.8 83.4 ✓ ✗ 92.8 93.5 89.2 84.1 ✓ ✓ 94.1 98.7 94.6 86.0 (b)Effects of Tokenizer Training Objectives. Setting Spatial Object Goal Long w/oL com (λ1 =0) 82.4 93.6 89.1 66.2 dominantL com (λ1 =10) 51.8 38.7 73.5 26.4 Full (λ 1 =0.5) 94.1 98.7 94.6 86.0 Table 5.Ablations of VLA Policy Architecture and Objectives. Implementation details of the VLA architecture, specifically the Dual-Head mechanism, and training hyperparameters. we first prove the inherent failure of Euclidean regression on non-convex action manifolds (Lemma A.1), then demonstrate the optimization instability caused by statistical anisotropy (Lemma A.2), and finally justify the efficiency of our hybrid architecture via complexity analysis (Lemma A.3). Without a hybrid architecture, a model approximates the manifold M within the high-dimensional ambient space RN (whereN=T·d, i.e., horizon times per-step action dimensionality). High spatial precision, Tight tolerances Init Step 1 Step 2 Step 3 Step 4 TubeRack: Init Step 1 Step 2 Step 3 Step 4 ZipSeal: Init Step 1 Step 2 Step 3 # Var PlaceObj: It consists of a linear projection layer that maps the hidden statehto the logit space of theLASTtokenizer’s vocabularyV. (1) Update Strategy.During training, we optimize the cross-entropy loss between the predicted logits and the ground-truth 15 LAST: The DiT head consists of 16 layers with a hidden dimension of 1024, accepting a noisy actiona k, a diffusion timestepk, and the conditioning featurehinjected via cross-attention. (1) Update Strategy.The head is trained to predict the noise ϵ added to the whitened Lie-algebraic actions using a standard denoising objective: Hybrid Optimization Strategy.The framework is trained end-to-end using a joint objective that balances discrete intent classification and continuous flow refinement: Category Parameters and Values Architecture VLM Backbone Qwen2.5-VL-3B-Instruct Vision Backbone Internal ViT (unpooled features) VLM Hidden Dimension 2048 Action Model Type DiT-B (16 layers, 1024 dim) Conditioning Cross-Attention (ada norm) VLA Training Max Training Steps 100,000 Warmup Steps 5,000 (Rat