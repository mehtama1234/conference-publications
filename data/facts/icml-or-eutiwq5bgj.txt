REAL METHOD of "Feedback Control for Multi-Objective Graph Self-Supervision" (arXiv 2602.05036v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Most multi-pretext pipelines useper-update mixing, forcing every parameter update to be a compromise, leading to three failure modes:Disagreement(conflict- induced negative transfer),Drift(nonstationary objective utility), andDrought(hidden starvation of underserved objectives). We introduce ControlG, a control- theoretic framework that recasts multi-objective graph SSL asfeedback-controlled temporal al- locationby estimating per-objective difficulty and pairwise antagonism, planning target bud- gets via a Pareto-aware log-hypervolume plan- ner, and scheduling with a Proportional–Integral– Derivative (PID) controller. Yetwhichobjective works best depends heavily on the dataset, downstream task, and training stage—motivating multi-pretext approaches that combine or search over objec- tives (Jin et al., 2021). A natural response is to train with multiple pretext objec- tives jointly, hoping that the encoder absorbs a richer set of signals. Many multi-pretext and multi-task recipes op- erationalize this by optimizing a weighted sum of losses or by combining gradients into a single update direction at every step. each training step blends signals across objectives in parameter space, implic- itly assuming that an appropriate instantaneous compromise can be found and maintained. Research gaps in multi-objective graph SSL.When ob- jectives are self-supervised pretext tasks, training exhibits three recurring failure modes.(a)Drift: the utility of a pretext objective is nonstationary—an objective that acceler- ates learning early can become redundant later—so fixed or weakly adaptive weights lag behind regime changes (Guo et al., 2018a).(b)Disagreement: Key idea.Instead of blending all self-supervised objectives at every step, train one objective at a time using feedback control to schedule and adapt. 1 arXiv:2602.05036v1 [cs.LG] 4 Feb 2026 Feedback Control for Multi-Objective Graph Self-Supervision Per-step Mixing (Objective Blending) L1 L2 L3 L4 Compromise Conflicting objectives → suboptimal for all ControlG Temporal Allocation (Block Scheduling) Time T1 T2 T3 T1 One objective per block → reduced interference (a)The core insight.Per-step gradient mixing forces compromise when objectives conflict. ControlG separates objectives in time, giving each dedicated compute blocks—eliminating instantaneous interference. Early training explores all objectives; mid-training prioritizes MI after detecting interference; late-training bursts on lagging reconstruction. temporal allocation avoids objective conflict by dedicating compute blocks to one objective at a time.(b)The learned schedule is interpretable and auditable—the planner adapts allocation based on interference and demand signals. • We introduce ControlG, a closed-loop control frame- work for coordinating multiple graph SSL pretext objectives via sensing, Pareto-aware planning, and feedback-based execution. To the best of our knowl- edge, this is the first work to cast multi-objective graph SSL coordination as aclosed-loop scheduling problem with explicit allocation tracking, using deficit-based feedback control to execute discrete single-objective blocks rather than per-step loss weighting. • We propose full-graph state estimation signals that quantify objective demand and objective interference, enabling principled adaptation to nonstationarity (drift) and gradient conflicts (disagreement) without collaps- ing all objectives into a single per-step compromise. Since no single objective dominates across graphs (Liu et al., 2022; Ju et al., 2024), recent work integrates multiple objectives. PARETOGNN (Ju et al., 2022) applies multi-objective gradient updates for Pareto efficiency, yet per-step mixing can force compromise when objectives conflict and provides no explicit coverage accounting. Multi-objective optimization and MTL.Gradient-based MTL methods design per-step update rules: We consider K pretext tasks {Tk}K k=1 with losses {Lk(θ)}K k=1, treating training as minimizing the vector objectiveL(θ) = (L 1, . objectives with high weight are actively constraining the trade-off at the current iterate. Overview The core idea behind ControlG is to replace per-step objective blending withtemporal allocation: rather than forcing every parameter update to compromise across all objectives, we dedicate consecutive optimization steps to a single objective at a time and use feedback control to decide which objective to train next. This reframes multi- objective coordination as a closed-loop scheduling problem, where the learning system repeatedly (i)senseswhich objec- tives are currently difficult or mutually antagonistic for the 3 Feedback Control for Multi-Objective Graph Self-Supervision 1. SENSE Full-Graph State Estimation (Timescaleu) Graph G,X Encoder f θ Embed Z Losses L 1 , . A shared encoder fθ maps the graph (G,X) to embeddings Z, from which per-task losses Lk are computed. Normalized losses ˜Lk are converted toLog-HV sensitivities wHV k = 1/(rk − ˜Lk), quantifying each objective’s marginal contribution to Pareto progress. shared encoder, (ii)plansan explicit target allocation over objectives using a Pareto-monotone progress surrogate, and (iii)tracksthat plan with a lightweight controller. The key question is therefore not how to combine objectives within a step, but how to select the sequence {kt,m} so that progress across objectives is balanced and robust. We answer this by maintaining a compact per-task state computed on the full training graph (Sec. State estimation is performed on thefull training graph G= (V, E) so that the signals correspond to the same global objectives be- ing optimized. At each sensing step, for every objective k∈ {1, . , K}we estimate (i) aspectral demandscore that captures how rapidly the objective’s learning signal varies over the graph, (ii) aninterferencescore that captures conflicts with other objectives in parameter space, and (iii) a normalized loss ˜Lk used for planning. Let zv ∈R h denote the embedding of 4 Feedback Control for Multi-Objective Graph Self-Supervision node v∈V , and for objective k define therepresentation- gradient field hk(v) :=∇ zv Lk ∈R h, stacking rows into Hk ∈R n×h. Then, for fixed gradient energy ∥Hk∥2 F , the first-order loss de- crease per update is upper-bounded by a quantity that is non-increasing inRQ k. A first-order expansion shows that a step on k increases Lj whenever ⟨gj, gk⟩<0 (Claim A.10 in App. Because MGDA’s QP depends on gradient magnitudes, we first normalize e