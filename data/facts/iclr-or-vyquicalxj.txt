REAL METHOD of "Cross-Modal Redundancy and the Geometry of Vision–Language Embeddings" (arXiv 2602.06218v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We operationalize this assumption with an Aligned Sparse Autoencoder (SAE) that encourages energy consistency during training while preserving reconstruction. More recent concept-based methods (Ghorbani et al., 2019; Zhang et al., 2021; Fel et al., 2023b; Elhage et al., 2022; Fel et al., 2023a) have emerged to answer this, seeking to extract meaningful features (concepts) that models implicitly compute over. Prior work has described this separation geometrically, attributing it to a conical structure in the embedding space (Liang et al., 2022; Ethayarajh, 2019), or through the lens of training dynamics induced by the contrastive loss (Fahim et al., 2024; Shi et al., 2023; Yaras et al., 2024). We then operationalize this simple assumption with a natural method: an alignment-penalized Matching Pursuit Sparse Autoencoder (Aligned SAE), which encourages energy consistency across modalities during training. • We operationalize this assumption with an Aligned Sparse Autoencoder that enforces energy consistency during training while preserving reconstruction, and validate it with ground-truth sanity checks where classical SAEs fail. This modality gap has been attributed to a cone effect (Liang et al., 2022; Ethayarajh, 2019; Schrodi et al., 2025) and training dynamics induced by the contrastive loss and by mismatched pairs of data (Fahim et al., 2024; Shi et al., 2023; Yaras et al., 2024). Dual-encoder models (e.g., CLIP, SigLIP) map these observations to a shared activation space, which sparse autoencoders (or other overcomplete dictionary learning methods) then attempt toliftback to concept-like atoms. A VLM encoder f maps each observation x(d) to a shared embedding, and a sparse autoencoder ϕ attempts todisentanglethis embedding back into concept coordinates, so that ϕ◦f approximates the inverse of Definition 1. We then incorporate Iso-Energy into the sparse autoencoder recipe as a soft regularizer: Givenℓ 2-normalized codesZ (d),Z (d′) ∈R b×K, our training loss becomes: LSAE−A =L SAE +β· Lalign withL align =− 1 b Tr  Z (d) Z (d′)⊤  (1) with LSAE being the training loss of a standard SAE, typically an ℓ2 norm measuring reconstruction error with a sparsity constraint on the encoder’s output. We then compare a standard Sparse Autoencoder (SAE) to its ”aligned” variant trained with the loss function in Eq. (Left)Synthetic illustration comparing our method with the embedding shift baseline (Liang et al., 2022). Closing the modality gap.First, we find that by filtering outunimodalatoms with a binary mask (Figure 3), we nearly eliminate the modality gap while preserving retrieval and zero-shot performance. Our method consists in the following intervention on the activations, indicated with a tilde: This constrains the reported performance to the autoencoder’s reconstruction regime. Whether the same structural invariants and alignment properties hold in models with cross-attention mechanisms or generative training objectives remains an open question. Multimodal autoregressive pre-training of large vision encoders.Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR), pp. Sigmoid loss for language image pre-training.Proceedings of the IEEE International Conference on Computer Vision (ICCV), pp. We provide definitions along with interpretations and implicit inductive biases for each algorithm. Sparsity is enforced through a soft constraint, encoded in the loss typically as an L1 penalty. (2025) to facilitate training by enforcing a hard constraint on sparsity, rather than the soft constraint, as an inductive bias in the loss of the ReLU SAE. Matching Pursuit.As described in Algorithm 1, the Matching Pursuit algorithm iteratively adds the best atom di to the code z, removes it from the residual r, and starts again. This removes the inductive bias that constrains the geometric similarity between two atoms, which is present in all other methods. To compute β, we select the only positive real-valued root of the underlying 4th-degree polynomial to satisfy the constraint given by τ2. In this case, we observe that training a SAE fails to uncover the ground truth dictionary and learns spurious,unimodalfeatures, while training a SAE-A succeeds at doing so. D MODEL-DEX We studied a collection of models, varying in size and training procedure from the CLIP (Radford et al., 2021), SigLIP (Zhai et al., 2023), and OpenCLIP (Cherti et al., 2023) implementations. These were trained with the contrastive softmax loss to align matching pairs and separate non-matching ones. SigLIP.We conducted our experiments using both SigLIP, trained with a sigmoid-based contrastive loss, and SigLIP2, trained with a heterogeneous mix of objectives. Retrieval.We evaluate retrieval performance, another downstream task, on the FashionIQ dataset (Appendix J). Quantitatively, we evaluate both if our queries have high recall—meaning they can retrieve images of blue shoes—and whether they belong to the distribution of images—meaning they are actually embeddings of blue shoes—using the distance to the K-th Nearest Neighbor, a standard out-of-distribution (OOD) detection method (Sun et al., 2022; Yang et al., 2022). In some architectures, a term similar to L1 is included in the loss to promote sparsity, while in others, where sparsity is directly controlled, it is not necessary. After training SAEs with desirable properties, based on our hypotheses on the representations of VLM dual encoders, we now ask whether they can be used to improve their performance on downstream tasks. Retrieval.We find that QSAE systematically outperforms Q in terms of retrieval recall, across the six models we tested (Table 8). We also include COCO and ImageNet to study the transferability of our methods on unseen data distributions, and on unseen tasks. Our first method comes from the analysis of our SAEs : We propose the following baseline for this method : 35 Published as a conference paper at ICLR 2026 These two methods are not comparable to the first two in terms of modality gap, as even if the DiM is similar, the Wasserstein distance W behaves very differently. Indeed, in the first two methods, data is centered around 0 and is roughly uniformly spread on the unit sphere. We illustrate these four methods on synthetic 3D points in Figure 24. We also indicate in Figure 25 the KNN distance histograms for our four methods, following the OOD detection method described in Appendix H, for the CLIP-L model on the LAION dataset.