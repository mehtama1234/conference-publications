REAL METHOD of "AsyncBEV: Cross-modal flow alignment in Asynchronous 3D Object Detection" (arXiv 2601.12994v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Kooij* *Intelligent Vehicles Group and †3D Geoinformation Group, Delft University of Technology {s.wang-15, h.caesar, liangliang.nan, j.f.p.kooij}@tudelft.nl ABSTRACT In autonomous driving, multi-modal perception tasks like 3D object detection typ- ically rely on well-synchronized sensors, both at training and inference. However, despite the use of hardware- or software-based synchronization algorithms, per- fect synchrony is rarely guaranteed: To address this challenge, we propose AsyncBEV , a trainable lightweight and generic module to improve the robustness of 3D Birds’ Eye View (BEV) object detection models against sensor asynchrony. Inspired by scene flow estimation, AsyncBEV first es- timates the 2D flow from the BEV features of two different sensor modalities, tak- ing into account the known time offset between these sensor measurements. The predicted feature flow is then used to warp and spatially align the feature maps, which we show can easily be integrated into different current BEV detector archi- tectures (e.g., BEV grid-based and token-based). b) Asynchronous Detection of pretrained CMT c) Asynchronous Detection of CMT+AsyncBEV(ours) GT TPFPdata streams to detector𝑡! 𝑡" a)Concept of Asynchronous 3D Object Detection reference timestamp∆𝑡 Figure 1:Multi-modal 3D object detection under sensor asynchrony.a) shows a general con- cept of asynchronous 3D object detection. b) shows multi-modal 3D object detection under sensor asynchrony of a pretrained CMT (Yan et al., 2023). First, sensors may operate at different sampling frequencies or cannot be triggered syn- chronously with other sensors (e.g. However, downstream multi-modal 3D object detectors are usually trained on well-synchronized and well-curated datasets, and do not consider the possibility of asynchronous inputs. when one modality is delayed, a detector pretrained on synchronized data produces spatial errors in bounding box predictions, which can compromise safety in autonomous driving. Scene flow methods, on the other hand, estimate the motion of dynamic objects (Zhang et al., 2025a), but they require two point clouds as input and assume fixed time offsets, which limits their applicability to asynchronous multi-modal settings. To address sensor asynchrony, We proposeAsyncBEV, a lightweight and generic module that can be seamlessly integrated into diverse architectures of multi-modal 3D object detectors. To this end, we define a new task,∆-BEVFlow estimation, which aims to predict the flow in the BEV space between multi-modal BEV features given the known time offset between the features. Its offset-aware design facilitates the motion prediction for time offsets that can vary during operation, without significant performance loss in the synchronization case. Since AsynBEV computes flow from BEV features, it does not make assumptions on the sen- sor modalities, nor which sensor is considered the reference. • We introduce AsyncBEV , a trainable and lightweight module that can be integrated into diverse multi-modal 3D object detector frameworks, and improves a detector’s robustness against small, moderate, and severe time differences between sensors, especially for dynamic objects. We inte- grate AsyncBEV in both a token-based and a dense grid-based detector architecture. 2 RELATEDWORK Multi-modal 3D Object Detection.To leverage the complementary information of different modalities (e.g., LiDAR and cameras), multi-modal 3D object detection has drawn wide attention in both academia and industry (Song et al., 2024b; Wang et al., 2023c; Huang et al., 2022). These methods require strict cross-sensor synchronization so that the images and point clouds align in the 3D coordinate system, though 3D object detection under sensor asynchrony re- mains underexplored. CoBEVFlow (Wei et al., 2023) first generates object proposals from distinct agent features and then applies a computation-intensive attention-based module to associate the proposals from different agents. Early works (Caesar et al., 2020; Huang & Huang, 2022; Park et al., 2023) pioneered the use of EMC to align the static objects, while following works (Li et al., 2022b; Wang et al., 2023b; Cai et al., 2023) further apply attention modules to encode the object motion implicitly. 3 METHODOLOGY Most multi-modal 3D object detectors follow a similar structure, which we describe here first under the common simplifying assumption that all sensors are synchronous. First,IandPare fed into two separate backbones to generate modal-specific 3 Preprint a) Asynchronous multi-modal 3D Object Detection 𝑡! 𝑡" Projection & FusionDetectionHead Wrong prediction at 𝑡!frameCorrected prediction at 𝑡"frame Voxelization&LiDAR Backbone Image Backbone Δ-BEVFlow b) Δ-BEVFlowEstimation ∆𝑡=𝑡! −𝑡" SimpleBEVEncoderC BEV Velocity Estimator motiondirection Supervision by ℒ!"#$ :variable:moduleC:concatenation:object position : Token-based methods(Yan et al., 2023; Wang et al., 2023a) use a sparse set of object queries, which are defined in the 3D space, to retrieve information from featuresF C P through a set of 3D positional encoding generated fromC C 3D. 4 Preprint 3.1 ASYNCBEV We propose aAsyncBEVto predict the flow (movement) of dynamic objects in the BEV space and then warp the misaligned BEV spatial locations before the detector’s fusion. The exact implementation of this step depends on the aforementioned detector architecture, token-based or grid-based. Token-based methodsencode spatial locations of tokens (sensor features) in their 3D positional encoding. Grid-based methodsimplicitly encode spatial positions in the grid indices. Loss functions.AsyncBEV can be trained by back-propagating through an existing frozen object detector framework, using the regular object detection losses. Following the common practice of 3D object detection losses, we also adopt focal loss for classificationL cls and L1 Loss for 3D bounding box regressionL reg. The overall training objective is then a weighted sum of the three components, Ltotal =ω 1 · Lflow +ω 2 · Lcls +ω 3 · Lreg, whereω 1,ω 2, andω 3 are hyperparameters that balance the contributions of the loss terms. Compared to its original end-to-end training schema, we retrained UniBEV with a two- stage training protocol to accelerate the training process. In the default setting, we use frozen pre- trained backbones and finetune only the AsyncBEV module and the detection heads. We also train a variantAsyncBEV (FD)of each architecture withfrozendetection 