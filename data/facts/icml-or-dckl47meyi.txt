REAL METHOD of "Swordsman: Entropy-Driven Adaptive Block Partition for Efficient Diffusion Language Models" (arXiv 2602.04399v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

However, existing block-wise decoding methods typically partition blocks in a rigid and fixed man- ner, which inevitably fragments complete seman- tic or syntactic constituents, leading to subopti- mal performance. Therefore, we propose Swordsman, an entropy-driven adaptive block-wise decoding framework for DLMs. As a training-free framework, supported by KV Cache, Swordsman demonstrates state-of-the-art performance across extensive evaluations. Introduction In recent years, diffusion language models (DLMs) (Austin et al., 2021; He et al., 2023; Gong et al., 2022) have rapidly emerged as a promising alternative to autoregressive lan- guage models by breaking the strictly sequential token de- coding bottleneck through parallel decoding of multiple tokens at each denoising step. However, limited by diffusion’s inherently numerous iterative denoising steps, DLMs cur- rently face a severe challenge: This gap motivates the community actively explore better diffusion language decoding methods that balance both speed and quality (Li et al., 2025b; Zhong et al., 2026; Hong et al., 2025). In light of the above discussion, we propose Swordsman, a training-free block-wise decoding framework for DLMs through entropy-driven adaptive block partition. Specifi- cally, Swordsman calculates the predictive entropy of tokens during unmasking and then identifies constituent boundaries by detecting sharp entropy shifts between adjacent tokens to adaptively partition blocks. Compared with LLaDA-architecture Fast-dLLM, Swordsman achieves an accuracy improvement from 35.59% to 43.90% on Hu- maneval and delivers up to 8.31% accuracy improvement while maintaining comparable inference speed. Among DLMs, Masked Diffusion Models (MDMs)(Sahoo et al., 2024) emerge as the mainstream paradigm by surpassing alternatives(Austin et al., 2021; Lou et al.) through simpli- fied and efficient training objectives. However, these methods fail to address critical bottlenecks in inference latency and generation quality. DCoLT (Huang et al., 2025b) enhances reasoning capabilities via chain-of-thought methods. Despite progress in specific areas, these methods fail to address LLaDA’s core challenges: Several methods have been proposed to address these chal- lenges. How- ever, these methods rely on bidirectional attention, imposing substantial computational overhead that limits practical in- ference latency even with parallelization. With advantages in both latency and quality, block-wise methods have become the mainstream solution. However, existing methods typically use fixed block sizes (Hong et al., 2025; Agrawal et al., 2025), ignoring semantic constituents within sentences. To address this, we develop a training-free fine-grained partitioning framework that organizes blocks by semantic constituents, maintaining low inference latency while improving genera- tion quality. Starting from a fully masked sequence x(T) where all tokens are [MASK], the model progressively unmasks tokens over T steps. Entropy-Driven Adaptive Block Partition for Efficient Diffusion Language Models where M(t) contains masked positions at step t and pθ de- notes the learned decoding transition. c(t) i ≜max v∈V pθ(v|x (t), i), U (t) = n i∈ M(t) |c (t) i ≥τ o , (2) where V denotes the vocabulary, c(t) i is the confidence score of token i, and U (t) represents the set of tokens selected for unmasking at stept. At each step t, only tokens within the current blockB (t) are considered for unmasking: Intra-constituent Smoothness.Within a semantic con- stituent, adjacent positions share consistent syntactic and semantic constraints from the decoded prefix. |∆Hintra i | ≈ |log(1 +ξ)| ≈ |ξ| ≤δ.(9) This proves that within semantic constituents, entropy shifts are constrained to a small neighborhood. Constituent Boundary Abruptness.When decoding tran- sitions from position i∈ GA to i+ 1∈ GB, the local strong constraints imposed by GA terminate, while those of GB remains unestablished. Swordsman Building upon the entropy analysis in Section 3.2, we present Swordsman, a training-free block-wise decod- ing framework that leverages entropy shifts to achieve constituent-aware generation in DLMs. each step splits at the maximum entropy shift, decodes the block with a dynamic threshold, refreshes entropy, and repeats. In contrast to traditional methods that employ rigid partitioning with predetermined fixed-length blocks before inference, we propose adaptive partitioning that divides block boundaries based on the local maximum entropy shifts ∆Hi between adjacent tokens during decoding, which reveal the natural transitions between semantic constituents. We re-perform forward propagation on the residual sequence to compute the updated predictive entropy Hi at each remaining position, calculate the entropy shifts ∆Hi based on these updated values, and identify the next maxi- mum shift point as the right boundary for current block: To prevent inefficient over- segmentation in the tail region where uncertainty has sub- stantially converged, we introduce an adaptive termination mechanism with a minimum shift threshold τmin = 0.1 , below the entropy shift at semantic constituent boundaries. To further leverage the acceleration benefits brought by semantic priors, we propose a dynamic threshold unmask mechanism based on block average entropy as put: Entropy-Driven Adaptive Block Partition for Efficient Diffusion Language Models Based on the calibrated difficulty metric λk, we compute the dynamic confidence threshold for the blockB k as: τt =τ init ·  (1−λ k) +λ k · vuut ¯H (k) t ¯H (k) start   ,(16) where τinit denotes the global base threshold, while ¯H (k) t and ¯H (k) start represents the average entropy of block Bk at the current decoding steptand the initial step respectively. This progressive relaxation allows the model to perform strict selection initially, but gradually ease constraints for better parallelization. Experimental Setup Implementation DetailsOur method is evaluated on three pretrained DLMs: We compare our proposed method against three baselines, including two fixed block meth- ods:Fast-dLLMandD2F(Wang et al., 2025b); and one adaptive block partitioning strategyAdaBlock(Lu et al., 2025). For fixed block methods, the block size is set to 32 (the optimal set for speed and accuracy identified by Fast-dLLM). Fur- thermore, we evaluate the methods across three KV cache configurations: While D2F achieves higher TPS on Dream- 7B (89.82) by distilling multi-s