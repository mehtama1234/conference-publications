REAL METHOD of "Translating Flow to Policy via Hindsight Online Imitation" (arXiv 2512.19269v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Our method, Hindsight Flow-conditioned Online Imitation (HinFlow), instantiates this idea with 2D point flows as the high-level planner. Across diverse manipulation tasks in both simu- lation and physical world, our method achieves more than2×performance im- provement over the base policy, significantly outperforming the existing meth- ods. A direct approach is using analytical or optimization-based methods to compute actions, but they often struggle with real- world complexities, e.g., visual occlusions or non-rigid-body dynamics (Bharadhwaj et al., 2024b; Yuan et al., 2024). Crucially, this result is achieved with only 80K online interaction steps, showcasing the high sample efficiency of our approach. Following existing research (Wen et al., 2023),D h is leveraged to train a high-level planner that produces subgoalsG t at each timestept. To extract point flow from videos, we first select a set of initial pointsp 0 ={p 0,k}K k=1 in the first camera frame, wherep 0,k = (x0,k, y0,k)denotes pixel coordinates. To realize the flow-based planner, we first extract flow information from the video datasetDh for the high-level training process. Specifically, for each observationo t in Dh, we apply our task-centric point sampler to generate a set of query pointsp t and then track their future trajectoryp t+H t+1 . The model is trained using a flow prediction loss, which can be formally expressed as: This training objective ensures that the model accurately predicts the future trajectories of task- relevant points, thereby enhancing the high-level planner’s ability to generate effective subgoals. These achieved flows then serve as the goal labels for trainingπ. Policy model training.The architecture of our flow-conditioned low-level policy adheres to the transformer-based design established in prior research (Kim et al., 2021; Wen et al., 2023). Specifi- cally, the inputs of the current timestep, comprising both visual and proprioceptive data, are initially encoded into a transformer. The training objective aligns with Equation (3), using data sampled from the offline demonstrationsD a. For training, we assume a limited set of action-labeled demonstrations (one for each LIBERO task, five for each ManiSkill task) and a large collection of unlabeled videos covering related tasks in the same scene. Online methods interact with the environment for 80000 steps. Notably, our method achieves significantly higher performance and sample efficiency. •ATM(Wen et al., 2023) first pretrains a track transformer on large-scale video dataset to predict the future flow of the query points. VPT first trains an inverse dynamics model (IDM) on action-labeled demonstrations. The trained IDM is then used to provide action labels for a large-scale video dataset, from which a BC policy is learned. it uses online rollouts from the policy to fine-tune the IDM, then uses the improved IDM to relabel the dataset and retrain the policy. A dataset of 100 action-free videos is used for training the high-level planner, and only 2 action- labeled expert demonstrations are used to pretrain the low-level policy. The low-level policy is trained on the original task and is directly evaluated on the two modified setups without any training or interaction. Number of action-labeled demos.We conduct evaluations using 0, 1, and 3 pretrain demos on LIBERO, and 0, 2, 5, and 10 pretrain demos on ManiSkill. Therefore, extending our method to more advanced goal representations, such as 3D motion fields (Yin et al., 2025), presents a valuable direction for future investigation. 14 Published as a conference paper at ICLR 2026 A IMPLEMENTATIONDETAILS A.1 IMPLEMENTATIONDETAILS OF OUR METHOD Policy Architecture.In our experiments, the low-level policy is designed as a transformer-based architecture. Initially, we encode the image data into spatial tokens at each timestep. The encoded proprioceptive data, the spatial CLS token, and a learned action CLS token are then interleaved across timesteps into a sequence. Finally, each timestep’s output is treated independently with an MLP, which parameterizes actions by taking the action CLS token and fusing it with the reconstructed flows of the current timestep. Exploration Noise.During the online interaction process of our method, we add Gaussian noise with a standard deviation of 0.1 to each action dimension to encourage exploration. Action chunking.Owing to the inherent challenges posed by temporally correlated confounders, a single-step policy is likely to encounter significant difficulties in achieving stable training. The steps to interact with the environment and the steps to update the model are the same 80000 steps. For a fair comparison, both baselines utilize the same flow prediction model as our method for the high-level planner. This baseline uses a frame stack of 10 and an input flow length of 16, which contrasts with our method’s use of 2 and 8, respectively. During training, the image is first fed into the flow prediction model to obtain a predicted flow, which serves as input for the low-level policy. However, during training, it directly uses ground-truth flow as input, aligning with our method. BC:BC shares the same model architecture as ATM except that the predicted flow is zero-out in both training and evaluation. The model first encodes each frame into a 64-dimensional spatial token. 18 Published as a conference paper at ICLR 2026 0 2 4 6 8 Env Steps (×104) 0% 25% 50% 75% 100%Success Rate Place Book w/ Cross-embodiment Data w/o Cross-embodiment Data 0 2 4 6 8 Env Steps (×104) 0% 25% 50% 75% 100%Success Rate Poke Cube w/ Cross-embodiment Data w/o Cross-embodiment Data Figure 12: Both methods are trained exclusively on the tex- ture variations. For each task, we train an individual in- verse dynamics model on a single action-labeled demonstration, while the high-level planner is a language-conditioned model trained on a large collection of unlabeled videos across all tasks. C.4.3 HUDOR In this section, we compare our method against HuDOR (Guzey et al., 2025), which uses object- oriented trajectory rewards to perform online residual reinforcement learning. (1) While HuDOR uses human-hand-retargeted trajectories as the base policy, we replace that with the same pretrained policy used by our method. (3) We follow HuDOR’s implementation for the residual policy architecture and RL algorithm. (5) In addition, on top of the RL loss, we add an auxiliary behavior cloning loss computed f