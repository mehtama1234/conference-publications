REAL METHOD of "No Labels, No Problem: Training Visual Reasoners with Multimodal Verifiers" (arXiv 2512.08889v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

language-only chain-of-thought approaches, which demand large-scale (image, query, answer) supervision, and program-synthesis approaches which use pre- trained models and avoid training, but suffer from flawed logic and erroneous grounding. We propose an annotation-free training framework that improves both reasoning and grounding. We evaluate our approach across diverse spatial rea- soning tasks, and show that our method improves visual reasoning and surpasses open-source and proprietary models, while with our improved visual grounding model we further outperform recent text-only visual reasoning methods. To tackle the task, we propose an annotation-free training paradigm, called V ALOR, that learns to decompose the task and invoke tools by leveraging multimodal verifiers, without the need of ground truth supervision. However, our experiments show these methods struggle with grounding and reasoning, while requiring ground truth labels to train. Model Distillation.Methods in knowledge distillation typically trains a student model to imitate a teacher’s output distributions (Zelikman et al., 2022; Li et al., 2024; Muennighoff et al., 2025) or internal representations (Hu et al., 2024; Xu et al., 2025; Sarch et al., 2024), offering dense target- level supervision. Training with LLM Verifiers.LLM verifiers provide pseudo-supervision when ground truth labels are unavailable, with self-verification methods SCoRe (Zweiger et al., 2025), SQLM (Chen et al., 2 LLM Visual Grounding Model ! ! LLM Verifier VLM Verifier ❄ ❄ RL SFT Training Query: Rightmost chair Figure 2:Method overview.Our method, V ALOR, tackles visual reasoning across a broad range of spatial tasks, in 2D and 3D. SWiRL (Goldie et al., 2025) extends this approach by using high-capacity verifiers to improve low-capacity models through multi-step RL, while other methods (Liu et al., 2025a; Chen et al., 2025a; Zhao et al., 2025) leverage verifiers at inference time. Training with VLM verifiers.Recent work integrates VLMs into vision model training to guide training. Compute pseudo-3D width of the sink sx1, sy1, sx2, sy2 = sink_det["bbox"] sink_width_2d = float(sx2 -sx1) sink_depth= depth(img_pth, sink_det["bbox"]) sink_pseudo= sink_width_2d * sink_depth # Step 4: Compute pseudo-3D width of the shelf tx1, ty1, tx2, ty2 = shelf_det["bbox"] shelf_width_2d = float(tx2 -tx1) shelf_depth= depth(img_pth, shelf_det["bbox"]) shelf_pseudo= shelf_width_2d * shelf_depth # Step 5: Generating training data.We train on image–query pairs,{(I j, qj)}N j=1, without requiring ground- truth answers, using the reward model as the sole learning signal. Modern detectors like GroundingDINO (Liu et al., 2023b), trained on web data, are error-prone and struggle to generalize beyond their train- ing domains. Generating training data.Our approach requires no ground truth object annotations. Leveraging VLM verifiers as pseudo-labelers allows us to scale object detection training annotation-free, going beyond specialized spatial reasoning datasets to large, unconstrained image collections. In total, our training set consists of7,373images and yields a total of30,826bounding box annotations spanning a broad range of object categories. For each stage, we compute precision from manually identified true positives (TP), false positives (FP), and false negatives (FN) usingP= T P T P+F P+F N. We keep the vision and language encoder frozen and train the rest of the model on4A100 GPUs for7epochs with a batch size of16and learning rate of10 −6. (1) to measure if verifier-driven RL improves task decompo- sition and tool use over strong LLM baselines; (2) to assess if multimodal verifiers improve visual grounding without annotations; and (3) to compare our approach against state-of-the-art alternatives, including supervised RL-tuned VLMs, program synthesis methods, and direct-answer models. These comparisons contrast our proposed annotation-free training framework with competing training-free program synthesis methods. Compute pseudo-3D width of the sink sx1, sy1, sx2, sy2 = sink_det["bbox"] sink_width_2d = float(sx2 - sx1) sink_depth = depth(img_pth, sink_det["bbox"]) sink_pseudo = sink_width_2d * sink_depth # Step 4: Compute pseudo-3D width of the bathtub tx1, ty1, tx2, ty2 = bathtub_det["bbox"] bathtub_width_2d = float(tx2 - tx1) bathtub_depth = depth(img_pth, bathtub_det["bbox"]) bathtub_pseudo = bathtub_width_2d * bathtub_depth # Step 5: Compute pseudo-3D combined width of the sinks sink1_bbox = sinks_dets[0]["bbox"] sink2_bbox = sinks_dets[1]["bbox"] sink1_width_2d=float(sink2_bbox[2]-sink2_bbox[0]) # x2-x1 sink2_width_2d=float(sink1_bbox[2]-sink1_bbox[0]) # x2-x1 sink1_depth = depth(img_pth, sink1_bbox) sink2_depth = depth(img_pth, sink2_bbox) average_depth= (sink1_depth + sink2_depth) / 2 pseudo_3d_combined_width = (sink1_width_2d + sink2_width_2d) * average_depth # Step 4: Compute scale factor scale_factor= 1.5 / pseudo_3d_combined_width # Step 5: Compute pseudo-3D height of the bathtub btub_bbox= bathtub_det["bbox"] btub_height_2d = float(btub_bbox[3]-btub_bbox[1]) # y2-y1 bathtub_depth= depth(img_pth, bathtub_bbox) pseudo_3d_height = bathtub_height_2d * bathtub_depth # Step 6: Compute pseudo-3D height of the coffee table wcx1, wcy1, wcx2, wcy2 = white_coffee_table_det["bbox"] white_coffee_table_height_2d = float(wcy2 -wcy1) white_coffee_table_depth= depth(img_pth, white_coffee_table_det["bbox"]) white_coffee_table_pseudo= white_coffee_table_height_2d * white_coffee_table_depth # Step 4: Compute pseudo-3D height of the fireplace fpx1, fpy1, fpx2, fpy2 = fireplace_det["bbox"] fireplace_height_2d = float(fpy2 -fpy1) fireplace_depth= depth(img_pth, fireplace_det["bbox"]) fireplace_pseudo= fireplace_height_2d * fireplace_depth # Step 5: Compute the ratio of the fireplace's pseudo-3D height to the white coffee table's pseudo-3D height ratio = fireplace_pseudo/ white_coffee_table_pseudo # Step 6: Compute pseudo-3D height of the coffee table wt_x1,wt_y1,wt_x2,wt_y2 = white_coffee_table_det["bbox"] wt_height_2d = float(wt_y2 -wt_y1) wt_depth= depth(img_pth, white_coffee_table_det["bbox"]) wt_pseudo= wt_height_2d * wt_depth # Step 3: Compute pseudo-3D height of the fireplace f_x1, f_y1, f_x2, f_y2 = fireplace_det["bbox"] f_height_2d = float(f_y2 -f_y1) f_depth= depth(img_pth, fireplace_det["bbox"]) f_pseudo= f_height_2d * f_depth # Step 4: Compute horizontal overlap and sofa width overlap = max(0.0, min(tx2, sx2) -max(tx1, sx1)) sofa_width= float(sx2 -sx1) # Step 5