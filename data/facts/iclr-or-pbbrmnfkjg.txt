REAL METHOD of "Into the Rabbit Hull: From Task-Relevant Concepts in DINO to Minkowski Geometry" (arXiv 2510.08638v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

each attention head produces convex combinations of value vectors, and their outputs add across heads; tokens can thus be understood as convex mixtures of a few archetypal landmarks (e.g., a rabbit among animals, brown among colors, fluffy among textures). We operationalize it using overcomplete sparse autoencoders, producing a large-scale set of visual concepts: In the first part, we analyze how different downstream tasks recruit concepts from our learned dictionary, revealing functional specialization: First, we found that representations are partly dense rather than strictly sparse. images are partitioned into fixed-size patches, each linearly projected into an embedding vector (a patch “token”), then processed by layers of self-attention and feedforward modules. (2025) are one practical instantiation of this idea, enforcing sparsity through a simple encoder-decoder architecture. (2002), which minimizes Coulomb electrostatic repulsion energy between charged particles constrained to a sphere; and the spherical code 2This poses challenges for cosine-based analyses in clustering or retrieval, which may misinterpret antipodal pairs as unrelated features. Codes Z are produced via a single-layer encoder with BatchTopK projection Bussmann et al. With this basis in hand, we first investigate which concepts are recruited by different downstream tasks (Section 3), then discuss their statistical and geometric organization in detail (Section 5). Their ubiquity reflects the persistent relevance of spatial structure within the model, and their emergence aligns with our decoding experiments, which reveal that positional information remains linearly decodable up to the intermediate layers (see Section I) and form a 2-dimensional subspace at the penultimate layer (see Section 6). Patch embeddings from DINO are projected via PCA (on a per-image basis) and visualized by assigning the first three principal components to RGB color (normalized to [0,1] ). We trained linear decoders to decode the token position from each layer’s activations and extracted the corresponding decoding vectors at each spatial location (i, j)to form a position encoding matrix P∈R 256×d at each layer (see Section I for more details). This compression is reminiscent of a transition from place cell-like coding schemes O’Keefe & Dostrovsky (1971), where individual units encode specific spatial locations, to axis coding Chang & Tsao (2017) where each dimension encodes the continuous value of hori- 15 Published as a conference paper at ICLR 2026 (Extended Version). Figure 14:(Top) Positional encodings compress over layers(Left) Linear decodability of token position across layers: from high-rank, dispersed geometry in early layers (Figure 14) to a low-dimensional sheet in the final layers. This interpolative structure finds natural support in DINOv2’s training objectives. 7 MINKOWSKIREPRESENTATIONHYPOTHESIS While hypotheses about representational composition are rarely stated explicitly, the geometry assumed by our methods is foundational: If sparse autoencoders implicitly answer to the Linear Representation Hypothesis, then we understand the importance of specifying the correct ambient geometry: A single attention head already performs convex interpolation over its values, creating an archetypal geometry; multi-head attention then aggregates these convex pieces additively, yielding a Minkowski sum. In this section, we will start by making this idea explicit, then we will review some theoretical evidence based on previous works and showing how simple attention blocks generate such geometry, Then, we will follow showing some empirical signals that make the proposal a plausible candidate and the implication of such geometry. First (i), in piecewise-linear networks, ReLU- based architectures partition input space into convex polyhedral linear regions (often unbounded). feasible connections follow polytope face-walks rather than straight lines through empty space.(Middle)Archetypal Analysis (single-tile MRH with |S|= 1 ) achieves lower error than a sparse autoencoder (dashed line) with as few as 10 archetypes, despite stronger constraints, supporting criterion(ii)convex coding assumptions.(Right)Archetypal coefficient matrix Z T Z after clustering reveals emergent block structure with bright diagonal clusters. This means that, under the MRH, any method attempting to recover individual concept contributions z or concept polytopes Ponly from activations from a single layer alonefaces a non-unique solution. Accessing attention weights and activations from earlier layers may render the factorization tractable, and pointing interpretability efforts toward exploiting architectural structure rather than treating learned representations as opaque geometric objects. Masked autoencoders are scalable vision learners.Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR), 2022. Open world concept discovery and their interlayer connections in deep models.Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR), 2024b. Sparse autoencoders trained on the same data learn different features.ArXiv e-print, 2025. Sigmoid loss for language image pre-training.Proceedings of the IEEE International Conference on Computer Vision (ICCV), 2023. Concretely, given activations A∈R n×d (we adopt the row–atom convention D∈R c×d used in the main text so that A≈ZD with Z∈R n×c), concept extraction becomes a dictionary–learning problem with method-specific constraints onZandD: For each subset, we computed 2D PCA projections to visualize their geometric arrangement within the concept space. With Z∈R nt×c that we reshape to the tensor Z∈R n×t×c , then for the concept i we compute the footprintω∈R t as: In Figures 12 to 25, patch tokens are projected into their top PCA components, computed independently for each image. For each layer, we extract positional embeddings from 1 million ImageNet images, yielding A∈ RN×t×d token representations where n is the number of images, t= 261 tokens per image, and d= 768 is the embedding dimension. Direct averaging.We compute the average embedding for each spatial position across all images: We repeat this procedure for each layer.Linear classification.We train a linear classifier to predict token position from embeddings, yielding weight vectorsw i for each position at each layer. Early layers maintain high-rank positional representations that allow precise spatial localiza