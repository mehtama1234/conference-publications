REAL METHOD of "UniOD: A Universal Model for Outlier Detection across Diverse Domains" (arXiv 2507.06624v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Although there have been many insightful OD methods, most of them require troublesome hyperparameter tuning (a challenge in unsupervised learning) and costly model training for every task or dataset. In this work, we propose UniOD, a universal OD framework that leverages labeled datasets to train a single model capable of detecting outliers of datasets with different feature dimensions and heterogeneous feature spaces from diverse domains. These approaches train a separate model per dataset, while UniOD leverages a collection of historical labeled datasets to train a single universal model. • High effort on model selection and hyperparameter tuningParticularly, for deep learning based OD methods, we need to determine the network depth, width, learning rate, 2 and method-specific hyperparameters. The main idea is to use labeled historical datasets (widely available) to train a universal model capable of detecting outliers for all other tabular datasets without retraining. • We propose a novel outlier detection method UniOD, that is able to leverage knowledge from historical datasets and directly classify outliers inside a newly unseen dataset without training. 2 RELATEDWORK OD Methods without Model TrainingSeveral nonparametric OD methods, e.g. ℒ Training & Testing FLOW Testing FLOWTraining FLOW!"# OD Tasks Historical Labeled Datasets FeatureGeneration$#$ $#% $#&⋯%#$ %#% %#&⋯ LabelsData Data Figure 3: ,DTB, from B different domains, corresponding to B independent OD tasks, existing OD methods train one model on each dataset independently, which impliesB times of hyperparameter tuning and model training or fitting. 3.2 PROPOSEDMETHOD To tackle the challenges faced by previously proposed OD methods, we propose UniOD, which is able to use labeled historical datasets from different domains, easily available in this big data era, to train a general model that can detect outliers in a dataset from any unseen domain without conducting any retraining. The primary goal of UniOD is to train a universal deep-learning based model using these historical datasets DH, which can directly detect outliers in these test datasets DT without any further training or tuning. To deal with graph-structured data, we use K graph isomorphism networks (GINs) (Xu et al., 2018) and K graph transformers (GTs) to build our model, where each GIN is composed ofL 1 layers and each GT is composed ofL 2 layers. Then we apply an L3-layer MLP with parameters θ3 followed by a softmax function to predict the labels: Then, we minimize cross entropy loss L(θ) to train our proposed UniOD: After training, for each specific testing datasetDTi ∈D T , we can use (2) and (3) to construct multiple graph-structured data and obtain ˜XTi ,A Ti, and then we can input these graphs into our trained GINs and GTs to obtain ZTi, and finally use the trained classifier MLPθ∗ 3 to obtain the outlier score for each data point: These graphs are then used to train our GNN-based classifier (6,7 and 9) using (10). After training, any new dataset is processed through the same graph-construction pipeline and fed into the pretrained model to assign outlier scores according to (11), thereby identifying outliers in the newly unseen datasets. 3.2.3 ALGORITHM ANDIMPLEMENTATION OFUNIOD We provide a detailed algorithm including both the training and testing stages of UniOD in Algorithm 1, which is in Appendix C due to space limitations. For UniOD, we denote its total MLP layers by ¯L and its atten- tion layers by ¯L′. Previously proposed deep learning OD methods require training for each specific dataset, resulting in an increasing training time as the number of datasets grows. In contrast, UniOD uses only one model for all datasets, and its training phase is entirely decoupled from DT , eliminating any need for per-dataset retraining. This decoupling enables UniOD to perform online outlier detection, yielding greater efficiency compared to methods that require both training and testing stages for each dataset. 6 • Given that we are training a universal model for outlier detection across diverse domains, the training data is not a single dataset. Although in (10) we used the cross-entropy loss, other loss functions, such as mean square error, mean absolute error, and hinge loss, are also applicable. Then the average training error (empirical risk) on the M historical datasets is denoted as ˆL(f) := 1 M PM i=1 1 n Pn j=1 ℓ(y(j) Hi , ˆy(j) Hi ). Baselines and Hyperparameter SettingsUniOD is compared with 17 widely-used baseline methods, including traditional methods: For both traditional and deep methods, we use grid search to obtain the best-performing set of hyperparameters in the historical datasets, which is then used in our experiments. KPCA+MLP and MLP+TF are two baseline methods that uses historical labeled datasets for training. Average AUROC (%) and AUPRC (%) of each method over 5 runs, including both training and testing, on Group I datasets. Average AUROC (%) and AUPRC (%) of each method over 5 runs, including both training and testing, on Group II datasets. Another interesting phenomenon is that simple traditional methods, such as kNN and KDE, out- perform most deep learning methods in various datasets. However, as the dimension of datasets increases, deep-learning based methods will be more powerful since methods like kNN and KDE provide implicit prediction results for high-dimensional datasets as discussed in (Jiang, 2017; Gu et al., 2019). We first evaluate the performance of UniOD with 1,3,5,10,15 training historical datasets (with sub-sampling augmentation) shown in Figure 4a. The core idea of UniOD is to leverage historical datasets to train a deep universal model that can detect outliers in newly unseen datasets from diverse domains without retraining. For convenience, in the main paper, we have let each GIN have L layers and each layer has an MLP of depth L′. If all self-attentions areη-Lipschitz, then τGT =b LL′ W ρL norm  1 + √ Hη L (15) According to Lemma A.3 (a well-known result), we can bound the covering numbers of the GT: Lemma A.4.Consider the L-layer GIN, where each layer has an MLP of L′ layers. Consequently, lnN(F, ϵ,∥ · ∥F )≤ Kτ 2 MLP(RGIN +R GT) ϵ2 (18) Since the overall loss is ˆL(f) := 1 M PM i=1 1 n Pn j=1 ℓ(y(j) Hi , ˆy(j) Hi ), the loss on one dataset is ¯e(y,ˆy) = 1 n Pn j=1 ℓ(yj,ˆyj). Then the covering number of our model with loss is bounded as lnN(ℓ◦ F, ϵ,∥ · ∥F )≤ Kµ 2τ 2 MLP(RGIN +R GT) nϵ2 (19) The Dudley entr