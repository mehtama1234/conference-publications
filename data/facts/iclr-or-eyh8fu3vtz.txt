REAL METHOD of "Safety at One Shot: Patching Fine-Tuned LLMs with A Single Instance" (arXiv 2601.01887v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We validate our findings across five safety-aligned LLMs and multiple datasets, demonstrating the generality of our approach. Typical approaches include supervised fine-tuning (SFT) (Wei et al., 2021) and preference-based methods such as Reinforcement Learning from Human Feedback (RLHF) (Ouyang et al., 2022; Dai et al., 2023; Bai et al., 2022) and Direct Preference Optimization (DPO) (Rafailov et al., 2023), which enable models to detect harmful prompts and issue safe refusals. Several follow-up works (Leong et al., 2024; Wei et al., 2024; Peng et al., 2024; Jain et al., 2024; Zhang et al., 2025; Che et al., 2025) further investigate the mechanisms behind this phenomenon, demonstrating that alignment can be fragile under adversarial training. This risk becomes particularly critical under the Language-Model- as-a-Service (LMaaS) paradigm, where users upload fine-tuning datasets to providers, who then per- form fine-tuning and inference on their servers and deliver results via APIs (Figure 1). Alarmingly, prior work (Qi et al., 2024) shows that even strongly aligned models like GPT can be compromised with as few as 10 harmful examples trained 1 arXiv:2601.01887v2 [cs.LG] 6 Jan 2026 Preprint Figure 1: Methods like Lisa attempt to inject safety data during fine-tuning, which improves alignment but demands large-scale curated datasets and significant computation. Even more sophisticated objectives like ConstrainedSFT (Qi et al., 2025) constrains the alignment on initial tokens. However, it requires additional utility datasets as constraints, and balancing safety recovery with downstream performance remains challenging. The time (h) represents the addi- tional GPU hours required by the recovery method compared to Standard SFT. First, we uncover the phenomenon of one-shot safety recovery. 2 Preprint Third, we validate the effectiveness and generality of our method through extensive experiments. We systematically evaluate our method across diverse open-source models (e.g., Llama, Mistral, Qwen) and closed-source APIs (e.g., GPT-4.1), spanning multiple downstream tasks and adversarial fine- tuning scenarios (e.g., harmful injection, identity shifting, backdoor poisoning). More recently, ConstrainedSFT (Qi et al., 2025) proposes a regularized fine- tuning objective that constrains updates on initial tokens, improving persistence of safety alignment. STAR-DSS (Peng et al., 2025) uses a token-level signal that enables shaping to operate dynamically over the training sequence, mitigating finetuning risks and delivering substantial safety improve- ments across diverse threats. However, this design requires additional utility datasets as constraints, and balancing safety recovery with downstream performance remains challenging. While these methods advance the defense against fine-tuning attacks, they each reveal a fundamen- tal trade-off: 2.2 EVALUATION OFSTATE-OF-THE-ARTREALIGNMENTMETHODS Evaluation Metrics.We measure LLM’s safety by evaluating the Attack Success Rate (ASR) and the Harmful Score (HS). For Harmful Score (HS), we follow established practice (Qi et al., 2024) to utilize GPT-4 to judge the harmfulness automatically, further details of the scoring methodology can be found in Appendix A.2. For the MT- Bench, we use GPT-4 as judges to evaluate the general capabilities of chat assistants by assigning a 3 Preprint Table 2: Comparison of safety recovery methods in terms of safety, utility, and efficiency under Llama-2-7B-Chat and GPT-4.1. Evaluation Results.Comparison of safety recovery methods on Llama-2-7B-Chat and GPT-4.1. Standard SFT leads to severe safety degradation (high ASR and HS), while prior defense methods such as Vaccine (Huang et al., 2024b), BackdoorAlign (Wang et al., 2024), Antidote (Huang et al., 2025), DirectionAlign (Yang et al., 2025), and ConstrainedSFT (Qi et al., 2025) partially mitigate safety risks but still leave non-negligible vulnerabilities or incur additional cost. A major line of research scores or filters training examples using strong teacher models or task-specific heuristics (Chen et al., 2023; Cao et al., 2023; Lu et al., 2024; Zhao et al., 2024). Bi-level optimization (BLO) offers a principled frame- work for data re-weighting and other nested objectives, with applications to hyperparameter tuning, meta-learning, and LLM data selection (Sabach & Shtern, 2017; Franceschi et al., 2018; Liu et al., 2020). Recent first-order penalty-based BLO methods enable scalable training without second-order derivatives (Liu et al., 2022; Shen & Chen, 2023; Kwon et al., 2023; Lu & Mei, 2024; Xiao et al., 2023b;a; Shen et al., 2024; Lu, 2024). Building on this line of work, we formulate safety-driven data selection as a first-order, penalty-based bi-level optimization problem. Our Approach.We denote the initial safety-aligned model asθ 0. To obtain a fine-tuned model, we first adaptθ 0 on the mixed datasetD ∗ =D harm ∪ Dtask, whereD harm consists of harmful pairs and 4 Preprint Dtask contains task-specific data. The fine-tuning objective is formulated as θ∗ = arg min θ E(x,y)∼D∗  −logπ θ(y|x)  , θ←θ 0.(1) Next, we introduce a binary data selection vectorw∈ {0,1} N , whereNdenotes the total number of training samples. In this way,wencodes a data selection scheme that determines which training points constitute the safety-patch dataset. At the upper level, we optimizewto identify the subset of training samples that are most critical for safety, thereby yielding the overall BLO problem: Then, we generate refusal answers for each harmful instruction using the initial Llama-2-7B-Chat. the singular value spectra of safety gradients across all layers of LLaMA-2-7B-Chat, including the attention projections (attn q, attn k, attn v, attn o) and MLP blocks (mlp up, mlp gate, mlp down). To gain a more fine-grained understanding, we further compute layer-wise subspace similarity (Fig. Letθ∈R d be the model parameters, in each gradient descent step with step sizeη >0, we perform θt+1 ←θ t −η∇L(θ t). Theorem 1(Dimension-free global convergence).Under Assumptions 1, if the step size satisfies η≤ 1 ℓr , then one step of gradient descent satisfies: Leveraging this 10 Preprint property, we proposed a projection-based one-shot recovery method that restores safety alignment efficiently and effectively, without compromising model utility. Extensive experiments across multi- ple model families (Llama, Mistral, Qwen), diverse downstream tasks, and varying levels of harmful fine-tuning 