REAL METHOD of "MicroMix: Efficient Mixed-Precision Quantization with Microscaling Formats for Large Language Models" (arXiv 2508.02343v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Although the new FP4 Tensor Cores in NVIDIA’s Blackwell architecture offer up to 4×speedup over FP16, existing INT4-based kernels fail to fully exploit this capability due to mis- matched data formats. To bridge this gap, we propose MicroMix, a co-designed mixed-precision quantization algorithm and GEMM kernel based on Microscal- ing (MX) data formats. Tailored for the Blackwell architecture, the MicroMix kernel supports arbitrary combinations of MXFP4, MXFP6, and MXFP8 chan- nels, and produces BFloat16 outputs. To achieve a favorable trade-off between accuracy and efficiency for each linear layer, we introduce quantization thresh- olds that identify activation elements where lower-precision formats (MXFP4 or MXFP6) incur excessive quantization error. Our algorithm selectively allo- cates higher-precision channels to preserve accuracy while maintaining compute efficiency. In particular, Qwen2.5-32B-Base, Coder and Math exhibit lossless ac- curacy on zero-shot, code generation, and mathematical reasoning benchmarks. Weight-only methods (Lin et al., 2024b; Frantar et al., 2023; Yang et al., 2025) have substantially mitigated the precision loss associated with 4-bit weights and 16-bit activations (W4A16). In parallel, weight-activation methods (Dettmers et al., 2022; Xiao et al., 2024) suppress activation outliers effectively, enabling accurate 8-bit quantization of both weights and activations (W8A8). More recently, mixed-precision and rotation-based quan- tization algorithms (Ashkboos et al., 2024; Zhao et al., 2024) have pushed the frontier further to W4A4, achieving strong performance on downstream tasks. Peng Zhang 1 arXiv:2508.02343v2 [cs.LG] 30 Mar 2026 Published as a conference paper at ICLR 2026 (2) NVIDIA’s latest Blackwell architecture introduces FP4 Tensor Cores that offer up to 4×higher throughput than FP16 and 2×higher than FP8 or INT8. As a result, significant room remains for optimizing quantization kernel throughput on the Blackwell architecture. (a) Overview of MicroMix (c) MSE Loss on 32 Samples from WikiText2 (b) Quantization Threshold 𝑇(4) 𝑇(6) Figure 1: (c) MicroMix consistently achieves lower quantization error across all layers. In this paper, we proposeMicroMix, a mixed-precision quantization framework based on Microscal- ing (MX) data formats, featuring a co-designed algorithm and kernel. (1) Flexible bit-width ratios (4, 6, and 8 bits).To balance efficiency and accuracy, MicroMix assigns customized ratios of three precision levels to each linear layer. (2) Low-error precision assignment strategy.We propose a bit allocation algorithm that adapts to input distributions from the perspective of quantization error. To this end, we define explicit quantization thresholds for MXFP4 and MXFP6: elements exceeding the threshold at a given bit-width are reassigned to higher-precision formats (see Figure 1(b)). (3) Efficient reorder-and-quantize operation.Since adjacent channels may be assigned differ- ent bit-widths, channels of the same precision need to be reordered into the same block. To address this, MicroMix integrates the reordering step into the quanti- zation kernel (Figure 1(a)), enabling high-throughput quantization across heterogeneous precision levels with negligible additional latency. When integrated into the Transformer architecture, MicroMix achieves 1.98-2.02×higher end-to- 2 Published as a conference paper at ICLR 2026 end compared to FP16. 2 PRELIMINARY ANDMOTIVATIONS 2.1 PRELIMINARY Given an activation tensorXand a weight tensorW, quantization approximates the original matrix multiplication with a low-precision computation: Empirically, we observe that model accuracy remains close to the FP16 baseline as long as the quantization error is constrained within a specific threshold. Recently, DeepSeek V3.1 (DeepSeek-AI, 2024) was trained using the UE8M0 FP8-scaled data format for both model weights and activations, ensuring compatibility with microscaling formats. Given a FP16 tensorX∈R L×I, quantization to MXFP8/MXFP6/MXFP4 first partitionsXinto blocks of 32 elements{X i}N i=1, N= L·I 32 , then applies per-block symmetric quantization for∀X j ∈X i as follows: 2.2 MOTIVATIONS The primary motivations of this paper stem from addressing the limitations in current quantization methods and their corresponding kernels. Compared to prior methods, MicroMix assigns a larger portion of channels to higher-precision formats and applies layer-wise adaptive precision ratios across all lin- ear layers. Existing mixed-precision quantization methods such as Atom (Zhao et al., 2024), employ a fixed number of high-precision channels across all layers. This uniform allocation fails to ac- count for the heterogeneous activation distri- butions observed in different layers (see Fig- ure 2). Specifically, layers with larger activa- tion values across channels require more high- precision channels to reduce the quantization error. To overcome this, we propose a novel strategy that 3 Published as a conference paper at ICLR 2026 flexibly allocates the number of 4, 6, and 8-bit channels per layer. Leveraging the inherent flexibility of multiple MX data formats, we propose MicroMix, a novel co-designed mixed-precision quantization algorithm and kernel. Given a token X∈R I, our key idea is to constrain the quantization error of MXFP4 and MXFP6 such that it remains within the upper bound of the error introduced by INT8: G4 ={X|X≤T(4)}G 6 ={X|T(4)< X≤T(6)},G 8 ={X|T(6)< X}(8) We calculate the proportionsp 4,p 6, andp 8 corresponding to the channel groupsG 4,G 6, andG 8 for each linear layer in Llama3.1-8B. For each sample,p 4,p 6, andp 8 are computed over all linear layers. Instead, leveraging the observed stability ofp 4,p 6, andp 8, we pre- compute{p k 4, pk 6, pk 8, σk}for thekth linear layer offline using calibration data. Specifically, for thekth linear layer input tensorX k ∈R L×I, the channel-wise absolute mean vectorM k ∈R I is computed as: Our algorithm divides channels of activation into three distinct parts, to which we then apply block-wise scaling quan- tization in 32-element blocks. Since MicroMix employs non-fixed bit-widths across linear layers, we additionally report the average bit-width per token element for all methods in Table 1. We also propose an offline calibration strategy to determine the optimal channel assignments for each precision level on calibration dataset. Unlike previous approaches that use a fixed number of high-precision channels for all linea