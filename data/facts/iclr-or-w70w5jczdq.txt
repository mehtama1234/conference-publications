REAL METHOD of "DisTaC: Conditioning Task Vectors via Distillation for Robust Model Merging" (arXiv 2508.01148v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

In this work, we first investigate the vulnera- bilities of model-merging methods and pinpoint the source-model characteristics that critically underlie them. In particular, a flurry of recent methods aims to build multi-task models by merging networks that have been fine-tuned independently for each task, rather than retraining a single shared model from scratch (Ilharco et al., 2023; Yadav et al., 2023; Ortiz-Jimenez et al., 2023; Wang et al., 2024; Yoshida et al., 2025; Gargiulo et al., 2025). (i) they eliminate the need to aggregate all task-specific labeled data in one location, sidestepping data-access constraints, and (ii) they make it easy to add or edit the model’s skill on a particular task after deployment (Yang et al., 2024a). To this end, we proposeDistillation forTask-vectorConditioning (DisTaC) a lightweight knowledge distillation (KD) procedure that tackles both issues using only unlabeled data: To correct task vector–norm disparities, DisTaC first rescales each vector to a chosen target norm and then restores any performance lost through this scaling by distilling knowledge from the original model. Algorithm 1 combines these two conditioning steps, allowing them to be carried out in a single pass. • We proposeDisTaC, a distillation method of source model’s weights under appropriate conditions (Section 4), and demonstrate that it mitigates aforementioned failure modes (Section 5.2.1). Our DisTaC is a computationally efficient method, as it requires only a small number of training steps and relies solely on unlabeled data (Section 5.2.2). (i) when the task vec- tor norms differ, it is better to shrink the larger vector rather than stretch the smaller one (Section 6.1); and (ii) when the source models have low confidence, it is more effective to make them overconfident before merging, and then apply a calibration method to the merged model (Section 6.2). By minimizing a joint loss that combines ground-truth supervision with a soft- target signal derived from the teacher’s output distribution, the student acquires the teacher’s dark knowledge, namely, fine-grained inter-class relationships encoded in the soft logits, while retaining a substantially smaller parameter footprint. The KD objective then augments the conventional cross- entropy lossL CE with a softened Kullback-Leibler (KL) divergence term: In practical fine-tuning, practitioners select diverse hyperparameters, including learning rate, number of training steps, weight decay, and optimizer, each of which influences the distance between the final weights and their initialization, i.e. Injecting a single high-norm task vector degrades every merging method, with losses of up to14%. (Appendix C) 4 KNOWLEDGEDISTILLATION FORTASKVECTORCONDITIONING Algorithm 1DisTaC Require:Pre-trained parametersθ pre, task vec- torτ t, scaling factorκ t, temperature pair (Ttcr, Tstu), regularization weightβ, unlabeled dataset ˜Du t drawn from the distribution of task t, learning rateη, number of stepsK Ensure:Fine-tuned student parametersθ 1:θ 0 ←θ pre +κ tτt ▷Anchor point 2:θ←θ 0 ▷Student initialization 3:fork= 1,2, . Unified algorithm.The two conditioning strategies above are unified by Algorithm 1. In the Norm Mismatchregime we assign a task–specific scaling coefficientκ t individually for each of the eight norm–disparity configurations: The rows highlighted in gray show the performance obtained by first applying DisTaC for pre-conditioning and then merging. 7 Published as a conference paper at ICLR 2026 First, Figure 2a depicts the training history under theNorm Mismatchsetting in Table 1. It achieves performance comparable to, or even surpassing, the teacher model’s test performance within 500 steps, while theℓ 2 regularizer of DisTaC keeps the task vector norm to roughly1.1×that of the initialization point,θ pre +κ tτ, at the end of the 500 steps. Within 500 steps, particularly during the first 100 steps, it achieves a substantial reduction in prediction entropy while maintaining test accuracy at a level nearly equivalent to that of the teacher model. First, the calibration curves in Figure 4a show that a model trained without label smoothing (dark- purple line) is strongly overconfident, which is consistent with the well-known tendency of modern deep networks (Guo et al., 2017). An improved training procedure in timm.arXiv preprint arXiv:2110.00476, 2021. For instance, in early approaches to model merging, models with the same architecture were fine-tuned and then merged by averaging their parameters (Wortsman et al., 2022a; Choshen et al., 2022). We introduce DisTaC as a simple pre- conditioning step that mitigates both problems before merging. Conversely, it is also possible to distill models with different architectures individually to obtain task vectors, which can 15 Published as a conference paper at ICLR 2026 then be merged using task arithmetic (Merugu et al., 2025). C THEORETICALINSIGHTS INTOTASKVECTORMERGING FORMODELS OPTIMIZED WITHDISTINCTOBJECTIVES This appendix provides a step-by-step derivation of the theoretical results concerning the effect of calibration penalties on the arithmetic merging of task vectors. We demonstrate how calibration can introduce a first-order degradation in cross-entropy (CE) performance upon merging, an effect not observed when merging standard CE-trained task vectors. 16 Published as a conference paper at ICLR 2026 C.2 THEORETICALPRELIMINARIES FOR THEMAINRESULT C.2.1 DERIVATION OF THESTANDARDTASKVECTOR The optimal parameter vectorθ CE i for the standard cross-entropy loss satisfies the first-order opti- mality condition, which states that the gradient at this point is zero. τ CE i =−H −1 i gi.(9) C.2.2 DERIVATION OF THECALIBRATEDTASKVECTOR We now apply the same procedure to the calibrated objective functionJ CAL i (θ). Gradient and hessian at the base point.First, we compute the gradient and Hessian ofJ CAL i (θ) at the base pointθ 0. θCE merge :=θ 0 +ατ CE 1 +βτ CE 2 ,(30) θCAL merge :=θ 0 +ατ CAL 1 +βτ CAL 2 .(31) Taylor expansion of the CE loss for merged vectors.Our goal is to evaluate the CE lossJ CE i not at its own optimum, but at the merged parameter points. However, the dominant contribution to the loss change is typically negative and of orderO(∥τ∥ 2), indicating that merging CE vectors does not increase the loss at first order. We adopted a cosine- annealed learning-rate schedule preceded by 200 warm-up steps and used a mini-batch size of 128; ViT-L-14 training employed 