REAL METHOD of "Graph Random Features for Scalable Gaussian Processes" (arXiv 2509.03691v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Wall-clock speedups and memory savings unlock Bayesian optimisation with over 1M graph nodes on a single computer chip, whilst preserving competitive performance. One can then perform inference and make principled predictions, including during Bayesian optimisation, using GPs on graphs.¹ Scalability of GPs on graphs. In this paper, we propose to instead use the recently-intro- duced class of graph random features (GRFs) – sparse, unbiased estimates of graph node kernels computed using random walks (Choromanski, 2023; Reid et al., 2023 ). The GRF algorithm constructs random feature 𝜑(𝑖) for node 𝑖 ∈ {1, …, 𝑁} using random walks. We showcase our new techniques by performing Bayesian optimisation on massive graphs, implementing Thompson sampling with > 1M nodes on a single computer chip.² 2 Preliminaries Consider an undirected graph 𝒢 = (𝒱, ℰ, 𝐖), consisting of nodes 𝒱 = {1, …, 𝑁}, edges ℰ ⊆ 𝒱 × 𝒱, and a weighted adjacency matrix 𝐖 ∈ ℝ𝑁×𝑁. Heuristically, it assigns similarity scores to every pair of nodes, which are assembled into a Gram matrix 𝐊≔ [𝑘(𝑖, 𝑗)]𝑁 𝑖,𝑗=1 ∈ ℝ𝑁×𝑁. Suppose we have access to a sequence of 𝑇 noisy observations of the objective 𝑦𝑡 = ℎ(𝑥𝑡) + 𝜀, 𝜀 ∼ 𝒩(0, 𝜎2 𝑛), 𝑡 ∈ (1, 2, …, 𝑇) at distinct nodes 𝑥𝑡 ∈ 𝒱. A common choice of statistical surrogate for the objective function ℎ, for which we can perform analytic Bayesian inference given observations 𝒟𝑇 = {(𝑥𝑡, 𝑦𝑡)}𝑇 𝑡=1 (also denoted 𝒟𝑇 = {𝒙, 𝒚}), is the Gaussian process (GP) (Rasmussen, 2003): In our setting, the input domain consists of the nodes of a fixed graph. We can ‘train’ the kernel parameters (𝛼𝑟)∞ 𝑟=0 by maximising the log-marginal likelihood on the training data 𝒟𝑇, and then compute the analytic posterior mean and covariance: The mathematical details of GRFs are involved and can be safely omitted on a first reading, but their behaviour can be intuitively understood as follows. Then it is straightforward to see that for symmetric 𝐖 (undirected graphs), we have 𝚿⊤𝚿 = 𝐊𝛼. The key insight of GRFs is that we can compute a Monte Carlo estimate 𝚽 ∈ ℝ𝑁×𝑁 that satisfies 𝚿 = 𝔼(𝚽) by importance sampling random walks. Each random walk of length 𝐿 consists of a number of ‘prefix subwalks’ – namely, for each step 𝑙 < 𝐿, the sequence of the first 𝑙 nodes visited. We begin by proving novel theoretical results (Section 3.1), and then describe our full efficient GP workflow (Section 3.2). 3.1 Novel theoretical results We first recall the following result for GRFs, proved by Reid et al. Suppose we sample GRFs {𝜑(𝑖)}𝑁 𝑖=1 by sampling 𝑛 random walks that terminate with probability 𝑝 at each timestep, with modulation function 𝑓. Then we have that ℙ(|𝜑(𝑖)⊤𝜑(𝑗) − [𝐊𝛼]𝑖,𝑗| > 𝑡) ≤ 2 exp(− 𝑡2𝑛3 2(2𝑛 − 1)2𝑐4). (5) to compute the number of walkers 𝑛 needed to guarantee an accurate estimate of 𝐊𝛼 with high probability. 𝑛 then determines the number of nonzero entries in the GRF, which also inherits independence of graph size 𝑁. Then note that ‖ ̂𝐊‖2 ≤ ‖ ̂𝐊‖F ≔ √ √√√ ∑ 𝑁 𝑖,𝑗=1 | ̂𝐊𝑖,𝑗|2 = √ √√√ ∑ 𝑁 𝑖,𝑗=1 |𝜑(𝑖)⊤𝜑(𝑗)|2 ≤ 𝑁 max 𝑖,𝑗 |𝜑(𝑖)⊤𝜑(𝑗)|. This can be achieved with the conjugate gradient method in 𝒪(𝑁3/2) time. Using the conjugate gradient method, it is known that the system can be solved in √𝜅( ̂𝐊 + 𝜎2𝑛𝐈) iterations (Shewchuk, 1994), which by property (2) above is 𝒪(𝑁1/2). ∎ We remark that this is substantially less than the 𝒪(𝑁3) time complexity of exact GP methods that use 𝐊𝛼 rather than ̂𝐊. just considering a set of training nodes of cardinality 𝑁train ≤ 𝑁. 3.2 From pathwise conditioning to conjugate gradients We now introduce the three-step ‘recipe’ of posterior inference using GRFs: Denote the training data 𝒟𝑇 = {𝒙, 𝒚}, containing training nodes 𝒙 and corresponding noisy observations 𝒚. ∇ℒ(𝜽) = 1 2(𝐇−1 𝜽 𝒚) ⊤𝜕𝐇𝜽 𝜕𝜽 (𝐇−1 𝜽 𝒚) − 1 2 tr(𝐇−1 𝜽 𝜕𝐇𝜽 𝜕𝜽 ), (9) using iterative methods (Gardner et al., 2018; Lin et al., 2024a). This gives a batch of linear systems, 𝐇𝜽 [𝒗𝒚, 𝒗1, …, 𝒗𝑆] = [𝒚, 𝒛1, …, 𝒛𝑆], (11) which can be solved via iterative methods. We perform posterior inference using pathwise conditioning (Wilson et al., 2020; 2021) and iterative methods – a combination that has attracted recent interest in the literature (Lin et al., 2024b; 2025). This facilitates the use of iterative linear system solvers to compute ( ̂𝐊𝒙𝒙 + 𝜎2 𝑛𝐈) −1 (𝒚 − (𝒈(𝒙) + 𝜺)), which again avoids the explicit inverse and leverages sparse matrix multiplication.4 Once more, we use CGs (Hestenes and Stiefel, 1952; Shewchuk, 1994) as linear system solver, though alternatives have recently been proposed (Lin et al., 2023; 2024c). Training and inference are dominated by CG solvers, with 𝒪(𝑁3/2) time complexity (Lemma 1). First, we consider a dense baseline that uses GRFs, but explicitly materialises the 𝑁 × 𝑁 kernel approximation and computes its inverse. Second, we take the sparse GRF method described in Section 3.2, storing the random walk trajectories and solving the corresponding linear systems with CG methods. At the cost of sacrificing sparsity, we can then use the Woodbury Identity to efficiently solve a smaller linear system. Empirical scaling exponents (± s.d.) for memory usage, kernel initialisation, training, and inference with respect to graph size 𝑁. time Training time Inference time GRFs (Dense) 2.00 ± 0.00 1.21 ± 0.06 1.97 ± 0.38 2.16 ± 0.33 GRFs (Sparse) 1.00 ± 0.00 0.81 ± 0.04 1.04 ± 0.04 1.04 ± 0.05 Figure 2 shows log–log scaling curves. As expected, GRFs attain linear memory and initialisation cost, and sub-quadratic training and inference, scaling to graphs with 1M nodes. 4.2 Regression Tasks Next, we apply our method to regression with a variety of real-world datasets. In each case, we compare our efficient BO method with random search, breadth first search and depth first search policies. 5 Conclusion We demonstrated how graph random features (GRFs), a recently-introduced Monte Carlo algorithm, can be used to speed up training and inference with Gaussian processes on discrete input spaces. This algorithm has time complexity 𝑂(𝑁2𝑚 + 𝑚3), where 𝑚 ≪ 𝑁 is the number of the output dimensions (a hyperparameter of the JLT algorithm). (15) The expression on the right side can clearly be computed in time 𝑂(𝑁𝑚 + 𝑚3) since brute-force inversion of 𝐗 = (𝑰𝑚 + 𝐔⊤𝐔) −1 ∈ ℝ𝑚×𝑚 takes 𝑂(𝑚3) time and expression 𝐔(𝐗𝐔⊤𝒃)) for 𝐔 ∈ ℝ𝑁×𝑚 can be computed in 𝑂(𝑁𝑚) time. In each experiment, algorithms are initialised with up to 1,000 samples and then run for up to 1,000 BO iterations, repeated across five random seeds. Denote the 𝑁train training nodes as 𝒙 and the 𝑀 induc