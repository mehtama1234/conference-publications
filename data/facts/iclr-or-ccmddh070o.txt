REAL METHOD of "ParaS2S: Benchmarking and Aligning Spoken Language Models for Paralinguistic-aware Speech-to-Speech Interaction" (arXiv 2511.08723v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

To address this, we introduce a new reinforcement learning (RL) framework for paralinguistic-aware S2S,ParaS2S, which evaluates and op- timizes both response content and speaking style directly at the waveform level. We first constructParaS2SBench, a benchmark that evaluates the naturalness of input–output pairs in terms of content and speaking style using expressive and challenging queries. For the automatic judge, we propose a PolyTone training strategy and a multi-stage framework, preventing the style hallucination of end- to-end audio LLM judging. Inspired by DeepSeek-R1 (Guo et al., 2025), which incentivizes novel behaviors through RL with- out SFT demonstrations, we askwhether paralinguistic-aware dialogue capabilities can simi- larly emerge via RL with minimal supervision.To answer, we introduce a novel framework for paralinguistic-aware S2S,ParaS2S. We design a data curation pipeline to automatically generate high-quality speech prompts adhering to these properties, covering key paralinguistic as- pects—emotion, sarcasm, age, and gender. Instead, we propose the PolyTone training strategy to ground the captioners’ paralinguistic judgments in vo- cal cues. In our multi-stage framework, the grounded captioners separately analyze spoken content and speaking style—including age, gender, emotion, and tone—into textual representations, which are then reliably evaluated by large language models (LLMs). We verify the effectiveness of the automatic judge in guiding RL post-training withParaS2SAlign. We first distill the pipeline-based judge into a smaller and faster reward model for online learn- ing, and optimize the base S2S model with Group Relative Policy Optimization (GRPO) (Shao et al., 2024). Our results show that while supervised fine-tuning (SFT) is effective and outperforms existing models3, RL surpasses SFT by more than 10% in response content and speaking-style ap- propriateness on ParaS2SBench under both objective and subjective evaluations, demonstrating the effectiveness of our automatic judge. • We propose a pipeline-based automatic evaluator for S2S interaction naturalness. • WithParaS2SAlign, we show that our judge is accurate enough to guide RL post-training, improving S2S models on ParaS2SBench under both objective and subjective evaluations and achieving state-of-the-art performance. AudioReasoner (Xie et al., 2025) introduces Chain-of-Thought (CoT) reasoning to mitigate hallucination, while Qwen-Audio 1/2 (Chu et al., 2023; 2024) and StepAudio (Huang et al., 2025b) further extend dialogue capabilities to enable spoken agents 5. GLM-4-V oice (Zeng et al., 2024) and Step-Audio-AQAA (Huang et al., 2025a) rely on interleaved text and audio tokens for grounded speech generation. Qwen2.5 Omni (Xu et al., 2025) proposes the thinker-talker architecture, while Kimi-Audio (Ding et al., 2025) introduces a dual-head design for text and audio generation. Paralinguistic-aware dialogue models.Among these models, ParalinGPT (Lin et al., 2024b) and StyleTalk (Lin et al., 2024a) are the first to enable speech-to-text LLMs to respond differently to diverse speaking styles. Step-Audio (Huang et al., 2025b) and Step-Audio-AQAA (Huang et al., 2025a) rely on human feedback, which is annotation-heavy. ParaS2SAlign is the first RL framework to model content–style and input-output dynamics using scalable AI feedback. 5The response is usually in text, and thespeakingcapability is enabled by a separate TTS module. The distilled reward model can then be used by standard RL algorithms such as PPO and GRPO. 2.Scenario-controlled queries.To ensure realistic scenarios, we design each test query to beparalinguistically relevantso that speaking style genuinely influences how the response should be generated6. In Section 3.2, we present the methodology for automatically evaluating model responses given an input query. 3.1 DATASETCONSTRUCTION In this section, we aim to curate a benchmark dataset, specifically the testing quries, following the first two principles. Appendix A.2 provides detailed steps of dataset construction, and we provide the high-level concepts here for conciseness. Synthetic speech.We design a multi-stage generation and filtering pipeline to generate synthetic test queries. First, an LLM generates scenario-controlled and challenging queries where each spec- ifies one spoken content and two contrasting speaking styles in text. Model (confident):Sure! I can walk you through opening a brokerage account, researching companies, and placing your first order step by step. Human annotator check is conducted to ensure test set authenticity. We rely on two emotion datasets, IEMOCAP (Busso et al., 2008) and MELD (Poria et al., 2019), as they provide sufficient queries that meet our constraints. PolyTone training.We train acoustic analysts for emotion, sarcasm, gender, and age by fine-tuning state-of-the-art open-source captioners. To encourage these analysts to attend to vocal cues rather than infer labels from the spoken content, we propose PolyTone training, where the training set consists of utterances spoken in diverse styles but sharing the same linguistic content. This makes typical online RL training impractical when rewards are computed using the automatic-judge pipeline. We follow similar steps to those in Section 3.1 to construct the training queries. Reward model distillation.With the warm-up model, we sample diverse responses for each training query and score naturalness using the full judge pipeline described in Section 3.2. For RL training, we use all speech prompts; however, we use only the speech and discard all labels during training. Training.For PolyTone training, a single H100 GPU is used. For each query–response pair, we have a human final score and several automatic judgment scores from different methods. As shown in O5 and O6, the PolyTone training technique yields significant improvements for emotion and sarcasm but smaller gains for age and gender, suggesting the benefit of content-invariant, style-augmented training data. Note that the training speech prompts for SFT, the reward model, and RL fol- low the same construction pipeline; however, the training data does not undergo human verification due to the high cost. 4.Train/Test Split.To avoid overlap between training and testing, we use disjoint query topics and TTS speakers. Moreover, in our case, only the last token of the response is assigned a final reward, which complicates the training of the value func