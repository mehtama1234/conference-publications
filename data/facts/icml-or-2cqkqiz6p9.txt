REAL METHOD of "Training-Free Vector Quantization via Gaussian VAEs" (arXiv 2512.06609v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Training-Free V ector Quantization via Gaussian V AEs Tongda Xu∗1 2 3 Wendi Zheng 2 3 Jiajun He 4 Jos´e Miguel Hern ´andez-Lobato 4 Yan Wang1 Ya-Qin Zhang 1 Jie Tang 3 Abstract Vector-quantized variational autoencoders (VQ- V AEs) are discrete autoencoders that compress images into discrete tokens. In this pa- per, we propose a simple yet effective technique dubbedGaussian Quant (GQ), which first trains a Gaussian V AE under certain constraints and then converts it into a VQ-V AE without addi- tional training. Practically, we propose a heuristic to train Gaussian V AEs for effective con- version, named the target divergence constraint (TDC). As a result, special techniques are required, including commit- ment loss (Van Den Oord et al., 2017), expectation maxi- mization (EM) (Roy et al., 2018), Gumbel-Softmax (Jang et al., 2016; Maddison et al., 2016; Sønderby et al., 2017), and entropy loss (Yu et al., 2023; Zhao et al., 2024). In this paper, we circumvent the difficulty of training VQ- V AEs by not training them at all. Specifically, we propose Gaussian Quant (GQ), which first trains a constrained Gaussian V AE and then converts it into a VQ-V AE with- out additional training. To train a Gaussian V AE for effective conversion, we in- troduce thetarget divergence constraint (TDC), which encourages the Gaussian V AE to achieve the same Kull- back–Leibler (KL) divergence for each dimension, corre- sponding to the bits-back coding bitrate. • (Section 3.1) We propose Gaussian Quant (GQ), a sim- ple yet effective approach that constructs a VQ-V AE by training a constrained Gaussian V AE and converting it into a VQ-V AE without additional training. • (Section 3.3) We introduce the target divergence con- straint (TDC) to train Gaussian V AEs for effective conversion, and show that GQ outperforms prior VQ- V AEs on both UNet and ViT architectures. V ector Quantized Variational Autoencoder VQ-V AE (Van Den Oord et al., 2017) encodes images into integer tokens. For each dimension i, the encoder output f(x) i is quantized to the closest codeword cj in c1:K. LV Q=λlogK| {z } bitrate +E[∆(X, g(ˆz))]| {z } distortion +LReg, ˆzi = arg min cj ∈c1:K ||f(x) i −c j||,(1) where c1:K is the learned codebook, and LReg is a regu- larization term that ensures VQ-V AE convergence, such as commitment loss, codebook loss (Van Den Oord et al., 2017), and Gumbel-Softmax loss (Sønderby et al., 2017). first train a constrained Gaussian V AE, then convert it into a VQ-V AE. Converting a Gaussian V AE into a VQ-V AE Given a Gaussian V AE, we convert it into a VQ-V AE by generating one-dimensional Gaussian noise as a codebook and quantizing the posterior mean independently for each latent dimension. Then, for each dimension i, we select the codeword cj closest to the posterior mean µi and denote the quantized value asˆzi: Training a Conversion-ready Gaussian V AE with Target Divergence Constraint Now that we know how to convert a Gaussian V AE into a VQ-V AE, we discuss how to train a Gaussian V AE for effective conversion. (6) 3 Training-Free V ector Quantization via Gaussian V AEs To train a Gaussian V AE under stricter per-dimension con- straint, we propose a heuristicTarget Divergence Con- straint (TDC), which is an extension of previous LHiF iC heuristic with per-dimension rate control. For each dimension i, we apply a larger penalty if Ri exceeds logK+α and a smaller penalty if Ri falls below logK−α , using different λ values for each case. Now we have the conversion method for multi-dimensional codebook, we consider how to train a Gaussian V AE for multi-dimensional codebook. • First, GQ trains a Gaussian V AE with latent dimension d=mN , using the TDC constraint with target log 3K as in Eq. Improving TokenBridge with Target Divergence Constraint TokenBridge (Wang et al., 2025) also converts a pre-trained Gaussian V AE into a VQ-V AE. 4 Training-Free V ector Quantization via Gaussian V AEs Table 1.Quantitative results on the ImageNet dataset, comparing different VQ methods using the same model architecture. Our GQ outperforms other VQ methods on both UNet and ViT architectures.Bold: Experimental Setup Models and BaselinesFor image reconstruction, we use two representative autoencoder architectures:UNetfrom Stable Diffusion 3 (Esser et al., 2024), andViTfrom BSQ (Zhao et al., 2024). Datasets, Bitrates, and MetricsWe use theImageNet (Deng et al., 2009) training split for training, and theIm- ageNetandCOCO(Lin et al., 2014) validation splits for testing. Main Results Image ReconstructionTables 1 and 15 compare GQ with other quantization methods with same model architecture. Figure 2 shows that GQ produces 5 Training-Free V ector Quantization via Gaussian V AEs Table 2.Quantitative results on the ImageNet dataset, compared with pre-trained VQ-V AEs. Our GQ outperforms prior pre-trained VQ-V AEs while requiring less training.Bold: Moreover, Table 2 shows that GQ achieves competitive performance relative to several pre-trained models, such as FlowMo, while re- quiring fewer training epochs and smaller or comparable model size. Additionally, we train a DiT (Peebles & Xie, 2022) with the same architecture and training setup using Gaussian V AEs with and without TDC. GQ is effective only when applied to a Gaussian V AE trained with the TDC constraint. Method Codebook usage↑gFID↓IS↑ Diffusion Gaussian V AE w/o TDC - 8.35 202.19 Gaussian V AE w/ TDC - 8.47 205.94 Auto-regressive VQGAN 16.4% 8.01 151.40 FSQ 94.3%7.33224.88 LFQ 24.9% 7.73 142.09 BSQ 99.8% 7.82 221.64 TokenBridge 94.6% 7.82 198.24 GQ (Ours)100.0%7.67 230.79 to that of the TDC-constrained V AE (3.99 vs. converting a pre-trained Gaussian V AE yields better performance than training GQ from scratch, and further fine-tuning provides only marginal improvements. In Appendix B, we propose a numerically stable version of Mean-KL (Lin et al., 2023), which extends IsoKL 7 Training-Free V ector Quantization via Gaussian V AEs Table 10.Ablation study on hyperparameters α, β, and codebook dimensionm. Related works V ector-Quantized Variational AutoencoderVQ-V AE (Van Den Oord et al., 2017) is an autoencoder that com- presses images into discrete tokens. On the other hand, TokenBridge (Wang et al., 2025) and ReVQ (Zhang et al., 2025) convert a pre- trained Gaussian V AE into a VQ-V AE, but they do not constrain the divergence of the Gaussian V AE. Conclusion & Discussion In this paper, we proposeGaussian Quant (GQ), a simple yet effective method that constructs a V