REAL METHOD of "Real-Time Robot Execution with Masked Action Chunking" (arXiv 2601.20130v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Previous methods attributed this failure tointer-chunk discontinuityand developed test-time algorithms to smooth chunk boundaries. To address this, we propose REMAC, which learns corrective adjustments on the pretrained policy through masked action chunking, enabling the policy to remain resilient under mismatches between intended ac- tions and actual execution during asynchronous inference. Overall, our method delivers more reliable policies without incurring additional latency. Extensive experiments in both simulation and real-world settings demon- strate that our method enables faster task execution, maintains robustness across varying delays, and consistently achieves higher completion rates. We first formulate intra-chunk inconsistency as a partial mismatch between observations and executed actions, leading to a shift in intra-chunk distribution between training and sampling. To address this, we proposeReal-timeExecution with MaskedActionChunking (REMAC) to learn corrective adjustments to the pretrained policy by masking arbitrary portions of action chunks. Our method introduces no additional inference delay compared to the pretrained policy (Black et al., 2024; 2025), and can be seamlessly integrated into existing VLA frameworks. Moreover, our approach can be combined with existing test-time algorithms, further underscoring its utility to produce stronger backbone policies for asynchronous execution. Our method introduces no additional inference overhead, consistently outper- forms existing strategies, and can be seamlessly combined with test-time correction methods. Training proceeds by minimizing anℓ 2 objective between the predicted flow ˆuand the ground-truth targetu. 4.1 MASKEDACTIONCHUNKING Given the pretrained policyv π(At|ot), asynchronous inference executes the first few actions from the previous chunkA t−h, while the remaining actions are drawn fromAt, wherehdenotes the exe- cution horizon. By incorporating the prefix mask into the con- ventional flow-matching loss, we obtain the following objective: However, during training the executed portion of the chunk is unavailable, unless the current policy is rolled out from scratch for each input sample—a strategy that is prohibitively expensive and impractical, even in simulation. To address this limitation, we introduce a self-conditioned curriculum scheduling method that leverages the pretrained policy to imitate test-time conditions, thereby improving both intra-chunk consistency and inter-chunk discontinuity. Algorithm 1REMAC Input:Delay setS d, dataloaderD, epochsE Initialize:Pretrained policyv π(·), target policy ˆvπ(·) 1:fore←0toE−1do 2:Sampled∈S d and computem d via Eq. This scheduled curriculum not only stabilizes training, but also mitigates exposure bias by aligning training inputs with test-time conditions. The correction is then encouraged to match the residual toward the target through the following objective: 5 Published as a conference paper at ICLR 2026 Finally, the overall training objective is defined as: 4.2 PREFIX-PRESERVEDSAMPLING Having obtained the new policy ˆvπ(At|ot, d), we further adjust the sampling pipeline to align with the training procedure and enhance inter-chunk continuity. Instead, it is initialized using the can be executed actions during the delayed time steps, denoted asA p t ∈R P×D , whose firstP−hentries are filled with the lastP−hactions from the previous predicted chunk, while the remaining entries are zero-initialized. Specifically, if the sampling process integrates the learned velocity field ˆvπ overnintegration steps, letm∈ {0,1} P denote the delay-conditioned prefix mask andA τ t is the intermediate action state at integration timeτ, then: First, we con- ceptualize the transformation fromv π(At|ot)to ˆvπ(At|ot, d)as a distributional adjustment with respect to the pretrained policy, rather than a wholesale re-learning of the policy under a differ- ent objective. Second, LoRA offers a flexible mechanism that can leave the original parameters untouched, thereby preserving the predictive capacity of the pretrained model and making it well suited to our method. Training and Sampling.During training, we control the prefix-mask coverage by uniformly sam- plingdin Eq. The LoRA modules can be further merged into the backbone model layers, such that no additional time cost is introduced. Our method consistently outperforms baselines under all delay settings and exhibits smaller performance degradation as delay increases.Right (bottom):Average execution time across all environments. Our method requires fewer steps and achieves faster task completion. 5 EXPERIMENTS In this section, we present experiments evaluating the effectiveness of our method against baseline approaches under various inference delay settings. 5.1 KINETIXENVIRONMENT We first evaluate our method on the Kinetix simulator (Matthews et al., 2025), following the protocol of Black et al. Expert policies are first trained under a bi- nary success reward using RPO (Rahman & Xue, 2022), from which training data are collected via demonstration generation. For each task, the trained policies are configured with a prediction horizon of 8. LoRA is applied to all linear layers except the time-embedding and AdaLN layers (Peebles & Xie, 2023), with each LoRA layer assigned a rank of 4. Our method consis- 7 Published as a conference paper at ICLR 2026 Table 1: Methodd=0d=1d=2d=3d=4 Ours0.888 0.879 0.859 0.817 0.779 + BID0.888 0.880 0.862 0.821 0.781 + RTC0.888 0.879 0.864 0.826 0.791 Our method also improves performance under d= 0. Moreover, as the inference delay increases, our method exhibits both a smaller performance drop and a lower rise in execution time compared to baselines. • Table 1 reports the contribution of each component in our method. First, adding LoRA alone—without modifying the training paradigm—yields no performance gain, indicating that the effectiveness of our approach cannot be attributed merely to the increase in parameters. • Our method can further be integrated with other test-time approaches such as BID and RTC, since it only modifies the backbone policy. This demonstrates both the compatibility of our approach with existing test-time strategies and its potential as a plug-and-play method. A total of 200 trajectories are col- lected for model adaptation, with LoRA layers of rank 8 inserted only into the action expert module. For each task, we conduct 30 evaluation trials per method, with each trial capp