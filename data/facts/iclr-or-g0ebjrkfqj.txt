REAL METHOD of "BoGrape: Bayesian optimization over graphs with shortest-path encoded" (arXiv 2503.05642v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Bayesian optimization offers a principled approach for such settings, but existing methods largely focus on functions defined over nodes of a fixed graph. Moreover, graph optimization is often approached heuristically, and it remains unclear how to systematically incorporate structural constraints into BO. Through a novel formulation based on mixed-integer programming, we enable global exploration of the combinatorial domain over graph structures and explicit embedding of problem-specific constraints. We demonstrate that our method, BoGrape, is competitive both on general synthetic benchmarks and representative molecular design case studies with application-specific constraints. 1 INTRODUCTION Graph-structured data are playing an emerging role across scientific and industrial fields, giving rise to a series of decision-making problems over graph domains, such as graph-based molecular design (Korovina et al., 2020; Mercado et al., 2021; Yang et al., 2024) and neural architecture search (Elsken et al., 2019; White et al., 2023). (i)optimizing over nodes, with a given (unknown) graph as the search space and a function over nodes as the objective, and (ii)optimizing over graphs, with the entire (constrained) graph domain as the search space and a function over graphs as the objective. For both aforementioned scenarios, the objective function can be a black-box, and, when expensive to evaluate, discourages gradient- and population-based methods. a surrogate model, e.g., Gaussian processes (GPs), trained on available data to approximate the underlying function, and an acquisition function used ⋆Equal contributions. Thegraph GPis subsequently trained using the chosen kernel, and its posterior is used to build the acquisition function, e.g., LCB. Recent advances on applying MIP to optimize trained machine learning (ML) models (Ceccon et al., 2022; Schweidtmann et al., 2022; Thebelt et al., 2022b) suggest pathways to address these challenges. Moreover, some works develop MIP-based techniques to handle optimization problems constrained by graph neural networks (GNNs), with applications to molecular design (Zhang et al., 2023; McDonald et al., 2024; Zhang et al., 2024) and robustness certification (Hojny et al., 2024; Gaines et al., 2025). This paper proposes BoGrape, a MIP-based graph BO method to optimize functions over connected graphs with attributes. By introducing a representation of the shortest paths as decision variables, the acquisition function optimization is formulated as a MIP with a mixed-feature search space, graph kernel, and relevant problem-specific constraints. • We propose graph representations with their corresponding shortest paths, and theoretically prove that the feasible domain of our formulation is equivalent to the graph space consisting of all connected graphs. With the surrogate constructed and trained, an acquisition function is then formulated based on the posterior information, e.g., probability of improvement (PI) (Kushner, 1964), expected improvement (EI) (Jones et al., 1998), lower confidence bound (LCB) (Srinivas et al., 2010), predictive Entropy search (PES) (Hernández- Lobato et al., 2014), etc.. Observe that both the SP and SSP kernels are linear kernels if we pre-compute all shortest paths in each graph and count the number of occurrence for each shortest path length. We first introduce the formulation of shortest paths in Section 3.3 and then explicitly derive Eq. 3.3 ENCODING OF THE SHORTEST PATHS AS OPTIMIZATION CONSTRAINTS For the sake of exposition, we first consider all connected graphs G with fixed size, i.e., node number n is given (Appendix A.3 discusses formulations for graphs of unknown size). If graph G is given, all variables in Table 1 can be computed using classic shortest-path algorithms, such as the Floyd–Warshall algorithm (Floyd, 1962). In graph optimization tasks, however, we need to encode the relationships between these variables as constraints. Motivated by the Floyd–Warshall algorithm, we first present the constraints in Eq. (5) of Appendix A.2 and then prove there exists a bijective between the feasible domain given by these constraints and all connected graphs with size n. It is noteworthy that the architectures of GNNs, the training mechanism, and the choice of datasets are not major components of our work, since they merely serve as benchmarks of black-box graph functions. Each GNN consists of two convolutional layers to learn graph embeddings, and two linear layers. (i) there are no existing synthetic benchmarks in graph BO literature, (ii) these benchmarks impose neither problem-specific constraints nor assumptions over the graph space (except for connectivity), making them suitable for comparison of a wide class of methods. We found these structural constraints to effectively prevent the sampling- and evolutionary-based methods used in Section 4.2 from producing feasible solutions. To support the replication of empirical findings, we provide code implementations including our method, synthetic graph functions mentioned in Section 4.2 and models used in experiments in the supplementary materials. Mixed-integer optimisation of graph neural networks for computer-aided molecular design.Computers & Chemical Engineering, 185:108660, 2024. Multi-objective constrained optimization for energy applications via tree ensembles.Applied Energy, 306:118061, 2022a. Bayesian optimization with neural architectures for neural architecture search. Recall that the search domain considered here consists of all connected graphs with node number ranging from n0 to n, each node hasMbinary features with the firstLnode features as the one-hot encoding of node label. A.6 SIMPLIFY PATH ENCODING OVER UNDIRECTED GRAPHS For undirected graphs, we first add the following constraints to guarantee symmetry: Thus, current graph BO works mostly use evolutionary algorithms that generate candidates and then evaluate them, where graph kernels are used as graph functions to calculate the posterior mean and variance. Therefore, it is not that we chose shortest-path kernel first for specific reasons then developed necessary formulations, but that the fundamental advances in graph optimization led us to shortest-path kernels. Since the acquisition only appears in objective, all graph-relevant constraints still work as before. For example, if we use Dijkstra’s algorithm, which is a single-source shortest path algorithm, then we need to repeat it n times and the c