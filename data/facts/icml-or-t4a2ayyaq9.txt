REAL METHOD of "PhyScene3D: Physically Consistent 3D Interactive Tabletop Scene Generation" (arXiv 2606.01649v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Existing methods, ranging from decoupled symbolic solvers to end-to-end regression models, often suffer from error propagation or overfitting to noisy supervision containing widespread physi- cal violations. To address imperfect supervision and phys- ical infeasibility, we introducePhysics-Aware Denoising Alignment (PADA). Experiments demonstrate that PhyScene3D outperforms state- of-the-art approaches in both semantic accuracy and physical validity, achieving a 40% reduction in scene-wise collision rate relative to the human- annotated training data. However, tradi- tional scene construction methods, relying on manual heuris- tics (Raistrick et al., 2024) or random arrangements (Jia & Chen, 2024), fail to provide the scalability required for ro- bust policy learning. Consequently, automated scene genera- tion driven by Large Language Models (LLMs) has emerged as a promising paradigm, leveraging the extensive world knowledge embedded in pre-trained models (Yang et al., 2024). Conversely, image- mediated pipelines suffer from high latency and error ac- cumulation inherent in their multi-stage “generate-parse- retrieve” workflow (Figure 1(b)). To overcome these limitations, we proposePhyScene3D, a framework that rethinks generation not as distribution matching, but as a “Human-Mimetic Constructive Process”. Crucially, we treat the noisy training data not as a ground truth to be replicated, but as an imperfect reference to be “denoised”. By integrating a differentiable physical signal, the model is enabled to transcend its supervision and learn layouts that are physically more plausible than the human demonstrations on which it was trained. First, theCognitive Topological Reasoning Chain (CTRC)acts as a structural sequencer. Second, to guide the planner through noisy data, we introducePhysics-Aware Denoising Alignment (PADA). • We propose PhyScene3D, the first framework to inject a differentiable 3D physics engine into the training loop of a vision-language model. This enables generative models to surpass the physical quality of their training data through self-supervised denoising. • We introduce Cognitive Topological Reasoning Chain (CTRC), a hierarchical sequencer that imposes a strong structural inductive bias by linearizing complex scene graphs into anchor-relative assembly sequences. Text-centric agents, such as Holodeck (Yang et al., 2024) and I-Design ( C ¸elen et al., 2024), translate prompts into symbolic constraints. book_1 bowl coaster lamp book_2 book_3 cupbanana orange_1 orange_2 apple book_1 bowl coaster lamp book_2 book_3 cupbanana orange_1 orange_2 apple Sampling k Candidate Semantic & Physical Differentiable Guidance step 1: (a) first, supervised fine-tuning equips the model with the Cognitive Topological Reasoning Chain mindset; (b) then, Physics-Aware Denoising Alignment further mitigates physical noise in the data to achieve superior semantic consistency and physical plausibility. However, this “generate-parse-retrieve” workflow introduces high latency and irreversible error propagation. Conversely, end-to-end methods like MesaTask (Hao et al., 2025) model joint dis- tributions directly from data. naive training encour- ages models to replicate dataset artifacts, including physical collisions. Unlike PhyScene3D, these methods lack intrin- sic mechanisms to transcend imperfect demonstrations via physical self-correction. Prior methods rely on non-differentiable rejection sampling via rigid simula- tors (Todorov et al., 2012; Xiang et al., 2020) or heuristic filters (Paschalidou et al., 2021; Tang et al., 2024), which sever gradient flow and force expensive trial-and-error. Methodology We presentPhyScene3D, a unified framework designed to bridge the gap between abstract user instructions and physically plausible, hierarchically dense 3D desktop en- vironments. The framework consists of two organically integrated mod- ules: Problem Formulation Given a natural language instruction I, our objective is to generate a desktop scene layout S={e i}N i=1, where N is the number of objects. We define each entity ei as a tuple ei = (ci,p i,s i, θi), where ci denotes the semantic category, pi ∈R 3 is the absolute center position,s i ∈R 3 represents spatial dimensions, andθ i ∈Ris the yaw rotation. 3D AABB-BASEDPLACEMENTSCHEME Building upon the HSG, we introduce the 3D AABB-based placement scheme that enforces the graph’s structural con- straints. We define the pose of a child object ech strictly in the local frame of its parent epa. Crucially, we decouple the vertical dimension to encode physical affordance: Given a layout, for each object A, we derive its world-space rotation RA and translation tA. HIERARCHY-CONSTRAINEDTEST-TIME OPTIMIZATION(TTO) To utilize this engine without breaking semantics, we in- troduce a Hierarchy-Constrained TTO. PHYSICALLY-PROJECTEDSEMANTIC ANCHORING Finally, we address the challenge of training the VLM to satisfy physical constraints without a dense semantic re- ward model. Methods such asHolodeck(Yang et al., 2024) andI-Design( C ¸elen et al., 2024), which lever- age LLMs to generate symbolic constraints for external solvers. Training proceeds in two stages:i) Supervised Fine-Tuning (SFT):Full-parameter optimization on the training set.ii) Physically-Aware RL:GRPO with LoRA adapters (r= 16 ) to inject physical gradients. Our method achieves a QPR (τ= 7) of 46.5%, surpassing the Reference data itself (17.1%) by a wide margin. Beyond regression baselines, our method also demonstrates superior performance compared to the diffusion-based base- line. In comparison, our method demonstrates the unique bene- fits of the CTRC-PADA combination. Robotic Task Success Rate IID scene OOD scene MesaTask 4.6% 3.5% PhyScene3D (Ours) 50.4% 14.1% and train robotic agents for cluttered target retrieval. the collision-free stacks and precise container content relationships produced by our method are not merely visually convincing, but provide a stable physical foundation that enables reliable robotic interaction. Our proposedPADAframework shows the benefit of inter- nalizing these constraints during training. By combining theCTRCfor hierarchical plan- ning with(PADA)for geometric grounding, our method overcomes the limitations of noisy supervision, generating environments that are physically more plausible than the human-annotated data on which it is trained. Compute Relative Coordinate (Unified System) 10:b rel ←ComputeRelativeBBox(child.b abs, parent.babs, relati