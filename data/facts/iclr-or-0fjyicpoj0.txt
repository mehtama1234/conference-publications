REAL METHOD of "Gaussian certified unlearning in high dimensions: A hypothesis testing approach" (arXiv 2510.13094v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Then we theoretically analyze the performance of a widely used unlearning algorithm based on one step of the Newton method in the high-dimensional setting described above. Because full retraining is costly, machine unlearning develops efficient methods to remove data traces while preserving model generalization. Algorithms in theory:In addition to substantial progress in the practical development of machine unlearning algorithms, their theoretical understanding has also advanced significantly in recent years. (2025), we then theoretically investigate a class of unlearning algorithms that perform a few steps of Newton’s method, rather than fully re-optimizing the empirical risk. (2025), our studies explore the theoretical behavior of these methods in high-dimensionalproportional setting of p∼n , byrelaxing the standard optimization assumptions of Ω(1) strong convexity and O(1) smoothness of the chosen per-example loss. (2020) also concluded that asingle Newton step is sufficient for approximate removal of a single datapoint, but due totheir restrictive assumptions on the loss, their results fail completely in high-dimensions, even in the simplest setting of ridge regularized least squares. Comparison of conclusions and failure of assumptions in previous works:In the remaining part, first, we show why the standard optimization assumptions of the per-example loss f in Allouah et al. Theproblem of machine unlearningrequires a procedure ¯A to produce an update ¯A\M := ¯A(A(Dn),D M, T(Dn)) that closely approximates theidealmodel A(Dn \ DM) retrained from scratch. Exact recomputation is often computationally infeasible, and since ourfocus is on extending the theory of the proposed unlearning framework to a large class of loss functions in high-dimensional problems of interest, approximate removal methods offer a practical and scalable alternative. RERM training:The goal of a learning procedure A is to estimate the parameter β∗ from training data. Retraining from scratch:The objective of unlearning is to remove the influence of a subset M of observations, of size |M|=m , that have requested removal from the training data. ˆβ\M :=A(D \M) := arg min β∈Rp L\M(β) := arg min β∈Rp λr(β) + X i /∈M ℓ(yi|x⊤ i β).(2) The approach of machine unlearning:To process deletions efficiently, without full retraining, and obscure residual information, we update ˆβvia the randomized procedure below. (3) Assumptions on ¯A and motivations behind:Given a trained model, ˆβ, note that b is the only source of randomization during unlearning. In addition to the perturbation b, the unlearning procedure ¯A has access to the set DM corresponding to the data to be removed, and limited access to the outputs of the learning algorithm A on Dn. 3.1 GENERAL CERTIFIABILITY THROUGH THE LENS OF HYPOTHESIS TESTING A Hypothesis testing motivation:To motivate this, we take the perspective of an adversary who, given an output ˇβ from an unlearning algorithm ¯A, performs the following hypothesis testing problem: In Section 4, we will use the (ϕ, ε)-GPAR framework to theoretically evaluate the user privacy protection capabilities of machine unlearning algorithms that are based on the Newton method. Intuition behind:The above metric compares the generalization error of the unlearning algorithm ¯A given ˆβ and data removal requests M with that of the model A trained from scratch on D\M, on a fresh sample (x0, y0). 4 NEWTON-BASED MACHINE UNLEARNING:THEORETICAL GUARANTEES Having settled down on both the evaluation criteria, we study the performance of unlearning procedure ¯Abased on one Newton step and perturbing the result with Gaussian noise to achieve(ϕ, ε)-GPAR. 4.1 NEWTON-BASED UNLEARNING MECHANISM Newton method is an iterative method for solving optimization problems based on a second-order Taylor approximation of the given optimizing function Boyd & Vandenberghe (2004). Starting from an initial pointβ (0) ∈R p, the Newton method is the following iterative procedure: for stept≥1, β(t) :=β (t−1) −G −1(β(t−1))h(β(t−1)).(12) Using the Newton method Sekhari et al. Approximation step :starting from ˆβ, we run one Newton step withh=∇L \M to obtain: ˆβ(1) \M = ˆβ−G(L \M)−1( ˆβ)∇L\M( ˆβ),whereG(L \M)is the Hessian ofL \M in (2).(13) Randomization step:add a Gaussian noiseb∼N(0, σ 2Ip)to ˆβ(1) \M to have the unlearned output Noisy one step Newton output: Convexity of ℓ and ν-Strong convexity of r above actually means that per-example loss β→f(β,z) =l(y, x T β) + λ n r(β) is λν n strongly convex, which is significant relaxation of Ω(1) strong convexity of f, capturing the concrete case of the ridge regularized r(β) =∥β∥ 2 quadratic loss l(y, z) = (y−z) 2 . 4.3 THEORETICALGUARANTEES:ENSURING USER PRIVACY AS WELL AS MODEL ACCURACY One step Newton achieves (ϕ, ε)-GPAR:Our first result shows that for a suitable noise variance, the randomized one-step Newton unlearned estimator defined in (14) satisfies(ϕ, ε)-GPAR. Figure 2 and conclusions from it:In figure 2 we observe the same behavior as in figure 1 when comparing the performance on the unlearned dataset, where we compute the unlearned error divergence (UED) between the retrained and unlearned estimators (see appendix for more on UED). 6 CONCLUSION We introduce ε-Gaussian certifiability, and show that our noisy one Gaussian-Newton step ensures both privacy and accuracy in the high-dimensional proportional setting. A HYPOTHESISTESTING APPROACH In this supplement, we provide motivations B for some of the procedures in the paper, more background on Gaussian certifiability C, discussion and comparison of assumptions on the per- example loss functions D, proofs of our theoretical results G, H, implementation details for our experiments I, and results from additional simulations. First, we provide intuitions for our approach to describe the problem of machine unlearning. Condition on D:In considering ε− Gaussian certifiability, the following was the reason for compar- ing the output ¯A( ˆβ,D M, T(Dn),b) of ‘the unlearning algorithm on the full dataset with removed set M’ and ¯A( ˆβ\M,∅, T(D\M),b) ‘the result of the unlearning algorithm on the dataset without Mwith an empty removed set’. (2025) that such previous notions are not well-suited in high dimensions, which is based on the fact that we can allow unbounded Lipschitz bounds on the loss ℓ (indeed with low tolerance) compared to the worst-case bounds, which very much restricts the class of loss functions, particularly in high dimensions, inc