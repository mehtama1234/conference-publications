REAL METHOD of "GPT4Scene: Understand 3D Scenes from Videos with Vision-Language Models" (arXiv 2501.01428v4). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Text Encoder Visual Encoder Large Language Model BEV Image (Inference Optional) Video SequenceReconstruction ... (a) 3D Point LLMs (b) 3D Point LLMs with visual encoder (c) GPT4Scene (Ours) Figure 1. How- ever, we propose exploring a purely vision-based solution inspired by human perception, which merely relies on vi- sual cues for 3D spatial understanding. To address this, we intro- duce GPT4Scene, a novel visual prompting paradigm in VLM training and inference that helps build the global- local relationship, significantly improving the 3D spatial understanding of indoor scenes. The model then inputs the concatenated BEV image and video frames with markers. Surprisingly, af- 1 arXiv:2501.01428v4 [cs.CV] 11 Mar 2025 ter training with the GPT4Scene paradigm, VLMs consis- tently improve during inference, even without object marker prompting and BEV image as explicit correspondence. It demonstrates that the proposed paradigm helps VLMs de- velop an intrinsic ability to understand 3D scenes, which paves the way for a seamless approach to extending pre- trained VLMs for 3D scene understanding. In this work, we aim to leverage pre-trained VLMs with- out modifying their architecture, maximizing their vision perception capabilities. To address this, we propose GPT4Scene, a framework that enhances VLMs’ spatial understanding (see Figure 1). We first perform 3D reconstruction on input videos to gen- erate a Bird’s Eye View (BEV) image, offering a compre- hensive scene layout. Additionally, we introduce Spatial- Temporal Object markers (STO-markers) in both the BEV image and 2D frames. We first explored the effectiveness of GPT4Scene un- der a training-free approach. Experimental results revealed that it was notably effective for powerful large-scale VLMs such as Qwen2-VL-72B [96], as well as closed-source models like GPT-4o [72] and Gemini-1.5-Pro [88], signifi- cantly enhancing their 3D scene understanding capabilities, even reaching levels comparable to previous state-of-the- art point-based methods. For smaller open-source vision-language models (VLMs), we introduce ScanAlign, a multimodal dataset comprising 165K aligned data pairs featuring STO-marker-annotated video frames, BEV im- ages, and textual descriptions. • We introduce GPT4Scene, a framework that enhances Vision-Language Models (VLMs) to comprehend 3D scenes directly from pure vision input. • We introduce ScanAlign, a dataset comprising video frames, BEV images with STO-markers, and text anno- tations. 3D perception, as a founda- tional component, is typically trained on common indoor datasets [7, 10, 24, 27, 51, 69, 79, 85, 95, 124] using point clouds as input, supporting tasks like 3D object detection and instance segmentation [47, 70, 75, 83, 94, 102, 116]. 2D vision-language pretraining (2D- VLP) has driven progress in 3D visual-language learning (3D-VL) [34, 36, 73, 87, 105, 107, 118, 119], with recent 3D-VLP [28, 46, 48, 97, 108, 127, 128] methods demon- strating that combining 2D visual cues with 3D point clouds enables complementary cross-modal alignment. However, we aspire to go further by incorpo- rating 3D content into large language models (LLMs) to achieve more natural human-computer interaction. The origin of VLMs can be traced back to CLIP’s 2D image-text pair pretraining [11, 78, 82], which laid the foun- dation for incorporating LLMs. Early VLMs used attention mechanisms or Q-Former to fuse image and text modalities before inputting them to LLMs [3, 26, 44, 55, 56]. Currently, while some studies employ VLMs for indoor scene under- standing, most remain either benchmark-oriented [54, 109] or still rely on 3D-based methods [120, 123]. Methodology In this section, we introduce the GPT4Scene framework as shown in Section 3.1, which enhances VLMs’ 3D scene un- derstanding using video inputs. GPT4Scene Framework This section first presents an overview of the GPT4Scene framework, then details the 3D BEV Map (providing global scene layouts) and STO-markers (delivering localized ob- ject cues) as core components. , IN } captured by moving around an indoor scene, we first approximately uniformly sample n frames using in- dices: This strategy reduces token counts and computational overhead for Vision-Language Models (VLMs) while preserving scene context, without significant information loss. We then leverage the complete temporal sequence for 3D scene reconstruction, generating a holis- tic Bird’s-Eye View (BEV) map. Then, we gener- ate a BEV image of the scene from the global point cloud: 3 3D Point BEV Image with labels Visual Encoder & Large Language Model Reconstruct Partition Instance Seg & Detect Mark Img 1 Img 2 Img 3 Img 4 Img 5 Img 6 Img 7 Img 8 .. The resulting frames and BEV image, enhanced with STO-markers, are inputs for Large Language Model (VLM) training and inference. To help VLMs focus on specific objects, we introduce Spatial-Temporal Object markers (STO-markers), ensuring consistency be- tween 2D frames and the 3D BEV image. Then we extract the center coordinates of the resulting bounding box, de- noted as C xy = {C xy 1 , Cxy 2 , . For each frame i, we extract the centroid of the 2D mask of each object as its 2D marker, defined as C uv i = {C uv i,1 , Cuv i,2 , . This process is termed “unlocking”, which enables training-free 3D scene comprehension in VLMs via prompts. At this point, GPT-4o can still accept additional first-person per- spective frames, allowing it to understand the current posi- tion in the scene to plan the following action. We first construct an indoor scene dataset, ScanAlign, with egocentric, BEV images and text annotations based on the ScanNet [24]. The visual input consists of selected video frames with STO-markers and BEV im- ages, and T denotes text annotations derived from five Scan- Net annotations [24], as shown in Table 1. Since our method does not require additional modality alignment steps, we can directly perform single-stage in- struction fine-tuning on the ScanAlign dataset to enhance the model’s 3D spatial understanding capabilities. During the training phase, the training loss is the Cross-Entropy loss of the language model. Therefore, the loss function can be expressed as follows: The set of learnable parameters θ is the vision language projection layers. After fine-tuning with ScanAlign, we observed signifi- cant performance gains for smaller models, which proves that our approach effectively enhances the intrinsic 3D per- ception capabilities of visual languag