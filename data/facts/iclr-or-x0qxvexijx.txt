REAL METHOD of "Controlling Repetition in Protein Language Models" (arXiv 2602.00782v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We first propose quantitative metrics to characterize motif-level and homopolymer repeti- tion and then demonstrate their negative impact on folding reliability. To address this challenge, we propose UCCS (Utility-Controlled Contrastive Steering), which steers protein generation with a constrained dataset. Experiments with ESM-3 and ProtGPT2 in CATH, UniRef50, and SCOP show that our method outperforms de- coding penalties and other baselines, substantially lowering repetition while pre- serving AlphaFold confidence scores. The first aredecoding-based heuristics, including stochastic sampling strategies (Fan et al., 2018; *The first two authors contributed equally to this work. Since pathological repetition is a newly recognized issue in protein generation, the first step is to formally define this failure mode and establish evaluation metrics. This unified perspective not only provides principled evaluation, but also forms the basis of our method. • We introduceUtility-Controlled Contrastive Steering (UCCS), a dataset-guided steering method that disentangles repetition control from biological utility. A pretrained protein language model (PLM)Mcan be trained in either of two 3 Published as a conference paper at ICLR 2026 paradigms: Our methodUtility-Controlled Contrastive Steering (UCCS)con- structs paired datasets to disentangle repetition from utility, computes mean difference vectors from hidden activations, and injects them into selected layers to control generation. Given a pretrained PLMMand an optional prefixp, the model generates a sequencex= (x 1, . This formulation is model and method-agnostic, i.e., any intervention from decoding heuristics to latent-space steering can be assessed under the same constrained optimization framework. To weaken the intrinsic correlation between the two dimensions, we first filter out sequences withU(x) outside a tolerance band around the reference mean ¯U, defined as the average utility score across the candidate pool (computed separately within each length bin; see Appendix F.3). Contrastive selection.We then selectD + (low repetition) andD − (high repetition) by maximizing their separation inR(x), the repetition score, subject to a utility constraint. As a result, the steering vectors extracted in Sec- tion 4.3 capture latent directions of pathological repetition rather than foldability artifacts, providing a principled foundation for our method. This choice aligns with the training objectives of the two paradigms, ensuringϕ L(x)is faithful to how each PLM encodes global context: This procedure requires no retraining and can be applied plug-and-play, making it an efficient and interpretable mechanism for repetition control under the framework defined in Section 4.1. 5 EXPERIMENTS 5.1 SETUP Datasets.We evaluate our method on three widely used protein sequence databases: For each test sequence, the first 10% of residues are used as the prefix, and the remaining residues are generated with the total target length uniformly sampled between 50 and 512 residues. Results are averaged over 5 random seeds with standard deviations.For each method, hyperparameters are selected according to the harmonic mean ofR(x)andU(x)(Appendix G.4), ensuring a balanced trade-off between repetition reduction and biological plausibility. UCCS achieves the best trade-off.Our method consistently improvesR(x)while maintaining or enhancingU(x)across all settings. On PROTGPT2(see Table 2(a)), UCCS achieves the highest repetition control (up to +20%relative to temperature sampling) and is the only method that consistently satisfies the utility constraint across datasets and tasks. U(x) conditional unconditional (c) Layer ablation (ProtGPT2) Figure 5:Representative ablations.(a) dataset sampling, (b) steering strength, (c) steering layer. For layer ablations, ESM3 benefits most from steering at the final 2–3 layers, while ProtGPT2 shows a smoother optimum in the last third of its stack, consistent with their architectural differences. 6 CONCLUSION We conducted the first systematic study of pathological repetition in protein language models (PLMs) and introduced metrics framing it as a constrained optimization problem. F.4 PSEUDOALGORITHM OFDATASETCONSTRUCTION For completeness, we provide pseudocode for the dataset construction procedure described in Sec- tion 4.2. This algorithm implements the optimization objective (D+,D −) = arg max D+,D− ∆Rs.t.∆U≤ϵ, with multiple selection strategies (Pareto frontier, composite scoring, and random selection as base- line). parameter default value schedule (schedule)cosine unmasking strategy (strategy)random decoding steps (num steps)20 temperature (temperature)1.0 temperature annealing (temperature annealing)True top-p(top p)1.0 G.1 MODELCONFIGURATIONS We summarize the default configurations for ESM-3 (masked LM) and PROTGPT2 (autoregressive LM). G.2.2 EFFECT OFDECODINGSTEPS ONREPETITION To examine whether the observed repetition behavior of ESM-3 arises from an insufficient number of decoding iterations, we conducted an ablation varying the number of unmasking steps. Note that ESM-3 performsparallel unmaskingrather than stepwise autoregressive decoding; the default configuration of 20 steps follows the official recommendation balancing quality and efficiency. Specifically, we varied the number of iterative unmasking steps num steps∈ {10,20,40,100,200}, under unconditional generation (target length = 200 aa, 100 samples, 5 random seeds). Ablation on the number of decoding steps (num steps) for ESM-3 sequence generation. Overall, increasing the number of unmasking iterations does not meaningfully reduce repetition, suggesting that the observed degeneracy is an inherent property of the pretrained model rather than a consequence of limited decoding steps. For each sequencex, we compute the normalized entropyH norm(x)(Section 4.1; Appendix B) as a scalar supervision signal: Scoring.For every layerLand neuronj, we collect activations{h L j (x)}across sequences, align them with supervision values{H norm(x)}, and compute the Pearson correlation coefficient: G.3.2 PROBE-BASEDSTEERING Setup.Given contrastive setsD + (low repetition) andD − (high repetition), we train a binary probe at a chosen layerLto separate the two sets. Algorithm 3Probe-based Steering Require:Activations{h L(x)}, contrastive setsD +,D − 1:Build dataset(X, y)fromD + andD − 2:Train logistic regressor(w, b)on(X, y)▷epochs=200, lr=10 −2, weight decay=0 3:˜w←w/∥w∥ 2 if normal