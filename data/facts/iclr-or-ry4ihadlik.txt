REAL METHOD of "CLEAR: Calibrated Learning for Epistemic and Aleatoric Risk" (arXiv 2507.08150v4). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

We propose CLEAR, a calibration method with two distinct parameters, γ1 and γ2, to combine the two uncertainty components and improve the conditional coverage of predictive intervals for regression tasks. , n} can be split into train and calibration subsets Dtrain, Dcal. On the training data, a first estimate of C can be constructed, and then we can use data from Dcal to calibrate C such that (2) is satisfied. In case of CQR, we first estimate conditional quantiles ˆqα/2(x), ˆq1−α/2(x) using Dtrain, and then construct C(x) = [ˆqα/2(x) − γ, ˆq1−α/2(x) + γ], where the calibration parameter γ is chosen as the smallest value such that the prediction interval C(Xi) contains Yi for at least ⌈(1 − α)(|Dcal| + 1)⌉ points in the calibration set Dcal. 2.2 E PISTEMIC UNCERTAINTY The traditional machine learning approach trains a predictive algorithm on a single version of the cleaned/preprocessed dataset and uses the best-performing algorithm (compared using the validation set) for future predictions. Then, train models for all N1 × N2 combinations on Dtrain and pick the top-k based on their performance on Dval. ,Db train of Dtrain to obtain an ensemble of m = k × b estimators ˆf1, . (2025) extends this by using the combined data set Dtrain ∪ Dval for training and calibrating uncertainty based on 3 Published as a conference paper at ICLR 2026 out-of-bag data. Specifically, using a PCS-type estimator ˆqepi α and a quantile regression estimator ˆqale α trained on the residuals Yi − ˆf (Xi), we define the prediction interval: (3) Given a fixed ratio λ = γ2 γ1 , we compute γ1 on a held-out calibration set using the standard split conformal prediction procedure. We have chosen quantile loss (Koenker & Bassett, 1978) (defined in Algorithm 1, which is equivalent to the pinball loss or interval score loss, see Appendix B.3.4) as a simple metric to balance both coverage and width. A large ratio λ⋆ ˆqepi 1−α/2(x)+ˆqepi α/2(x) ˆqale 1−α/2(x)+ˆqale α/2(x) ≫ 1 indicates that epistemic uncertainty dominates at x (reducible with more training observations or stronger assumptions), while a small ratio ≪ 1 indicates aleatoric uncertainty dominates (not reducible by adding more training observations, though sometimes reducible by adding covariates). , n, split into training Dtrain, calibration Dcal, and validation Dval (we consider Dcal = Dval); grid of λ values Λ; significance level α. 3.3 M ETRICS To evaluate the quality of our prediction intervals, we employ interval coverage (PICP), normalized interval width (NIW), average interval score loss (AISL) and the quantile loss that are common in the interval prediction literature (Pearce et al., 2018; V’yugin & Trunov, 2019; Azizi et al., 2025). Quantile loss and NCIW performance of different methods over 10 seeds normalized relative to CLEAR (baseline = 1.0) with error bars are ±1 σ. Experiment Method NCIW Quantile Loss Average Width ($) Coverage 2 features PCS 0.214 3,818 107,880 0.87 CQR 0.186 3,448 104,741 0.90 CLEAR 0.171 3,131 95,177 0.89 All features PCS 0.105 1,922 57,594 0.89 CQR 0.117 2,194 62,398 0.88 CLEAR 0.103 1,923 55,910 0.88 Table 4: AISL Average Interval Score Loss Computed from (Rossellini et al., 2024) and as de- fined in Appendix B.3.4. For regression, they train each NN with 2 outputs estimating µ and σ via a Gaussian Maximum-Likelihood-loss (as in Nix & Weigend (1994)), where σ is responsible for the aleatoric uncertainty, which they refer to as “ambiguity in targets y for a given x”. However, to the best of our knowledge, many practitioners refer to “DEs” without implying adversarial attacks during training, and adversarial attacks during training 22 Published as a conference paper at ICLR 2026 are seen as an optional add-on to DEs, but not an important part of DEs. The main difference to DE is that MC Dropout only trains one NN with dropout and obtains an ensemble after training by randomly setting weights of the model to zero at inference time. Their paper suggests a different loss function, which they call β-NLL, for training to mitigate the underestimation of aleatoric uncertainty to some extent in their experimental setting without comparing this approach to calibration. In general, where one suspects that the aleatoric or the epistemic uncertainty might be too small or too large across the domain, we strongly recommend simply applying CLEAR on top of the already trained ensemble instead of retraining all the models with a new training pipeline. These methods only monotonically transform the uncertainty without changing the ranking of the uncertainties (i.e., if one was more uncertain on x than on ˜x before the calibration step, one will also be more uncertain on x than on ˜x afterwards; see Appendix I). Each of these methods could be incorporated into CLEAR as an alternative to our recommended ALEATORIC-R module. (2021) explores the limitations of the pinball loss (which we call Quantile Loss), criticizing that its direct minimization does not guarantee correct marginal coverage. we minimize the pinball loss on a calibration dataset Dcal (see Line 5 in Algorithm 1) while constraining the marginal coverage on Dcal (see Line 4 in Algorithm 1). Train and Optimize λ by running Algorithm 1 on Dtrain and Dval: The model ˆf and the uncertainty estimators are trained on Dtrain (Step 2 and 3 of Algorithm 1). The optimal value λ∗ is found by optimizing the QuantileLoss on Dval, without using any data from Dcal (Step 4 and 5 of Algorithm 1). The conformal marginal coverage guarantee P[Ynew ∈ C(Xnew)] = EDtrain,Dcal[P[Ynew ∈ C(Xnew)|Dtrain, Dcal]] ≥ 1 − α does not imply that P[Ynew ∈ C(Xnew)|Dtrain, Dcal] ≥ 1 − α for a fixed realization of the calibration set Dcal, as already discussed before. Even if the exchangeability assumption is satisfied, even methods with conformal guarantees often strongly undercover, i.e., PDtrain,Dcal [P[Ynew ∈ Cconformal(Xnew)|Dtrain, Dcal] ≪ 1 − α] ≫ 0. |Dtrain|, |Dval| → ∞ensures that model selection on Dval identifies k consistent base models among the candidates, since the validation loss converges to the population loss. Boosting methods trained on general loss functions can be made consistent under certain conditions, especially when regularized through early stopping (Zhang & Yu, 2005). Note that the majority of the literature defines the quantile loss as QLτ , whereas our QuantileLoss already aggregates QLτ over both the upper and 30 Published as a conference paper at ICLR 2026 the lower bound of the int