REAL METHOD of "The Illusion of Generalization in Tabular Language Models" (arXiv 2602.04031v2). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

First, binary and categorical classification achieve near-zero median lift over majority-class base- lines and strong aggregate performance is driven entirely by quartile classification tasks. Second, top-performing datasets exhibit pervasive contam- ination, including complete train-test overlap and task-level leakage that evades standard deduplica- tion. We conclude with recommenda- tions for strengthening TLM evaluation.1 1. Introduction Large Language Models (LLMs) have transformed natural language processing (Zhao et al., 2025; Minaee et al., 2025), and their success has inspired a new wave of methods for tabular data (Fang et al., 2024; Gardner et al., 2024; Hegsel- mann et al., 2023). Beyond naive approaches such as text serialization (Dinh et al., 2022; Lee et al., 2025), a new class of language models specifically (pre-)trained or fine-tuned for tabular domain have emerged (Fang et al., 2024; Hegsel- mann et al., 2023; Gardner et al., 2024; Wang et al., 2023; Sun et al., 2024). GBTs naturally handle irregular, discontinuous patterns without assuming feature relationships (Chen & Guestrin, 2016; Grinsztajn et al., 2022), while PFNs encode inductive biases through syn- thetic (heterogeneous) data priors and leverage transformer architectures without fixed positional encodings to respect permutation invariance (Hollmann et al., 2023; M¨uller et al., 2022). Rogers & Luccioni (2024) argue that LLM eval- uation is often confounded by benchmark contamination, inflated claims of emergent abilities, and insufficient atten- tion to what models actually learn from their training data. Before investigatingwhyTLMs might generalize, we must first verifythatthey do. • Data contamination:We present systematic evidence of training-test overlap and direct leakage in standard benchmarks. • Format over structure:Instruction-tuning alone achieves competitive performance, suggesting TLMs may leverage instruction-following capabilities rather than learning tabular-specific representations.3 Based on these findings, we conclude with 7 recommenda- tions for strengthening evaluation standards to differentiate evaluation artifacts from substantive advances in TLM de- velopment. Background Tabular Prediction Methods.Gradient-boosted trees (GBTs) have dominated tabular prediction for over two decades (Friedman, 2001; Chen & Guestrin, 2016; Ke et al., 2017; Prokhorenkova et al., 2018). Alternatively, TabPFN (Hollmann et al., 2023) pro- poses training a transformer on synthetic data to perform in-context learning on small tabular datasets (Hollmann et al., 2025). While PFNs achieve strong performance in small-data regimes, they are designed for small datasets and rely on synthetic rather than real-world pretraining data. Tabular Language Models.TLMs fine-tune or pretrain LLMs on serialized tabular data, converting rows into text sequences via templates or special tokens. These methods commonly report zero- and few-shot improvements over GBTs and TabPFN on aggregate accuracy metrics. Tabula-8B fine-tunes Llama 3-8B (Grattafiori et al., 2024) for classification and binned regression using a language modeling objective. It is trained on T4, a filtered subset of TabLib (Eggert et al., 2023) containing approx- imately 4 million tables ( ∼100B tokens) extracted from Common Crawl and GitHub. Contamination Concerns in Foundation Models.Data contamination, the presence of evaluation data in train- ing corpora, is a recognized concern in foundation model. (2020) with GPT-3 were among the first to systematically analyze contamination, using n-gram overlap detection and finding variable impact across bench- 2 The Illusion of Generalization in Tabular Language Models marks. (2024) precisely because of the au- thors’ significant commitment to open science; releasing the T4 training corpus, model weights, and evaluation suite enabled the kind of independent verification we conduct here. A literature review (Appendix G) confirms that the same methodological gaps recur across other major TLM papers. First, does TLM performance exceed naive baselines across task types? Second, can top performance be attributed to training data contamination? Third, is tabular pretraining necessary, or does general instruction-following suffice? We address each in turn through a majority-class baseline, targeted contami- nation probes of top-performing datasets, and instruction- tuned controls without tabular exposure. The majority-class baseline predicts the most frequent class in each test set, es- tablishing the minimum accuracy threshold and enabling us to computelift(improvement over naive prediction). To disentangle instruction-following capability from tabular knowledge, we fine-tuned Llama-3-8B on the 50K Alpaca dataset (Taori et al., 2023), a general-purpose instruction- following corpus containing no tabular data (see Appendix A for training examples and evaluation protocol), and refer to this model as Alpaca. Tabula-8B is trained on T4 tabu- lar data without general instruction-tuning, while Alpaca is trained on general instructions without any tabular ex- posure. Any performance gap between the two branches therefore isolates the contribution of tabular pretraining from that of instruction-tuning. Training details and hyperparameters are pro- vided in Appendix A. We use parameter-efficient LoRA fine-tuning (Hu et al., 2022) for compute reasons; prior work suggests LoRA matches full fine-tuning on instruction- tuning at this scale (Dettmers et al., 2023), and any resid- ual gap would, if anything, increase Alpaca’s recovery of Tabula-8B’s accuracy and strengthen our central finding. For Alpaca+Q evaluation, 20 quartile datasets were held out for training, leaving 26 quartile datasets for evaluation. Rep- resentative prompt examples for each task type are provided in Appendix A.2. To investigate data contamination, we searched the T4 train- ing corpus for evaluation examples from top-performing datasets (Table 2), where contamination would be most consequential for performance claims. Findings Our findings address three diagnostics, namely missing base- lines and task-type composition (Section 4.1), pervasive data contamination (Section 4.2), and the contribution of instruction-following relative to tabular pretraining (Sec- tion 4.3). As further validation, we computed Cohen’s Kappa (Co- hen, 1960) across all the datasets in Figure 5. We investigated this claim by search- ing the T4 (Gardner et al., 2024) training corpus fo