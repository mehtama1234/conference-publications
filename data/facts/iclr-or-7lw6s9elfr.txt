REAL METHOD of "MVR: Multi-view Video Reward Shaping for Reinforcement Learning" (arXiv 2603.01694v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

MVR leverages video-text similarity from a frozen pre-trained VLM to learn a state relevance function that mitigates the bias towards specific static poses inherent in image-based methods. Additionally, we introduce a state-dependent reward shaping formulation that integrates task-specific rewards and VLM-based guidance, automatically reducing the influence of VLM guidance once the desired motion pattern is achieved. 1 INTRODUCTION When using reinforcement learning (RL), agents learn to optimizetask rewardsencoding quantitative objectives. Firstly, as single-image similarity cannot characterize dynamic motions, the generated rewards are mis-specified—they incentivize agents to visit the very states with maximal similarity score (Fu et al., 2024). In addition, most methods linearly add single-view VLM scores to task or success rewards without explicit shaping, which can alter the optimal policy. 1 arXiv:2603.01694v1 [cs.CV] 2 Mar 2026 Published as a conference paper at ICLR 2026 Visual Guidance MVR VLM Figure 1:The proposed MVR computes visual guidance using a VLM and videos collected from multiple viewpoints.In this example, the task requires a humanoid robot to run forward. Being captured from different viewpoints, the image sequences encode complementary information and enable comprehensive evaluation of the agent’s behaviors. This learned relevance constructs astate- dependent reward shapingterm that provides dense guidance early on and automatically diminishes as behavior improves, avoiding persistent conflict with task objectives. MVR consistently outperforms VLM-RM (Rocamonde et al., 2024), and RoboCLIP (Sontakke et al., 2024), which are existing VLM-based methods. We propose MVR, a framework for generating feedback from multi-view videos. We introduce astate-dependent reward shapingformulation that integrates task rewards with VLM-based guidance and decays its influence as behaviour aligns with the target. These methods typically treat the VLM as a frozen scoring function and then improve reward quality via VLM fine-tuning (Fu et al., 2024), binarization (Huang et al., 2024a), or ranking-based objectives (Wang et al., 2024b). REDS (Kim et al., 2025) learns subtask-aware dense rewards from segmented demonstrations, and ReWiND (Zhang et al., 2025) trains a video-language transformer to produce dense language- conditioned rewards and demonstrates real-world RL fine-tuning. Our work specifically addresses the unique challenges of using pre-trained VLMs for continuous motion tasks, proposing practical solutions for video-based multi-view reward learning. The objective of RL is to find the optimal policy π∗ such that vπ∗ (s)≥v π(s) for any states. The Learning ProblemThe video-text similarity computed by models such as ViCLIP (Wang et al., 2024a) is denoted by ψVLM and characterizes how well a video matches a text description. During policy learning, we periodically sample state sequences s, render them into videos o from different viewpoints, and compute the video-text similarity scores ψVLM(o, ℓ), where ℓ is the task description. These steps result in a dataset D={(s,o, ψ VLM(o, ℓ))}. Throughout the paper, we use bold symbols (e.g., s) for sequences and plain symbols (e.g., s) for single timesteps. 4.1 LEARNINGSTATERELEVANCE FROMMULTI-VIEWVIDEOS ChallengesOperating in state space S enables efficient reward computation without per-step video rendering, which is crucial for million-step training. First, there is a semantic gap between states (proprioception, joint angles) and visual 3 Published as a conference paper at ICLR 2026 features (textures, colors), which prohibits directly regressing video-text similarity from states. It then queries a VLM for the similarity scores and video embeddings of the videos and augments its datasetD with state sequences, video embeddings, and similarity scores (step 3). Lastly, using the latest state relevance model and the reference set, MVR computes visual feedback rVLM for the online RL agent (step 1), which is combined with task rewards rtask through state-dependent reward shaping that automatically decays as the agent’s behavior aligns with the reference set. Evaluation MetricsFor HumanoidBench tasks, agents are trained for ten million environment steps, and we report the mean and standard deviation of the episodic returns over three random seeds. For MetaWorld tasks, agents are trained for one million environment steps, and we report the mean and standard deviation of the task success rates over five random seeds. Alternative MethodsFor methods only using rtask, we select two recent RL algorithms: For each rendered episode we draw one viewpoint uniformly at random; across training this rotates through all cameras without exceeding the rendering/encoding budget of a single-view pipeline. 8 Published as a conference paper at ICLR 2026 Task Rewards Only MVR Figure 4:MVR identifies suboptimal states through reward shaping.We visualize states generated by a TQC agent for the Sit_Hard task and annotate them with rtask (left) and the shaped reward rMVR =r task +wr VLM (right) computed by a trained MVR agent. First, MetaWorld episodes are short (500 steps) and focus on single tabletop objects, where sparse trajectory-level feedback already provides useful exploration hints. We annotate the states with rtask and rMVR computed by a trained MVR agent in Fig. MVR has the best average rank when compared to existing methods that use VLMs and methods that only use task rewards, confirming its superiority for soliciting visual guidance for RL agents. A1 specify the training objectives, optimization steps, and update schedule for MVR. 9:ift > 0then 10:ift %T render= 0then 11:Render the last episode into videos. This requires the robot to adapt its gait and step height for each stair while preserving balance and forward momentum, simulating real-world obstacle navigation. Samples were collected from 8 parallel environments, with the learning rate decaying from 6×10 −4 at the start to 5×10 −5 at the final step, ensuring training stability. Compute costFor each rendered trajectory we sample only one viewpoint, so the wall-clock cost matches that of a single-view implementation. A3, the backbone adopts a fully-connected architecture with two hidden layers of dimension 512, activated by ReLU nonlinearity. Formally, given an input state s∈R d, the backbone first projects it into a latent embedding space through sequential transformations: Models Used in MVR, 