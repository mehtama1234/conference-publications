REAL METHOD of "Directed Semi-Simplicial Learning with Applications to Brain Activity Decoding" (arXiv 2505.17939v3). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

To fill this gap, we introduce Semi- Simplicial Neural Networks (SSNs), a principled class of TDL models that operate on semi-simplicial sets, combinatorial structures that encode directed higher-order motifs and their directional relationships. To enhance scalability, we propose Routing-SSNs, which dynamically select the most informative relations in a learn- able manner. Building on previous evidence that such descriptors are critical for characterizing brain activity, we then introduce a new rigorous frame- work for brain dynamics representation learning centered on SSNs. The success of GNNs is mainly due to their ability to synergize the flexibility of deep learning models with the inductive bias encoded in the graph structure (Bruna et al., 2014; Gori et al., 2005). We then select a set of higher- order directed relations R induced by the topology of K, and process X l withSemi-Simplicial Neural Networks(SSNs), where the relations in R define how information is propagated and updated. extending GNNs into architectures that operate directly on CTSs. Here, directionalityrefers to structural asymmetry; for example, a directed edge has a source and a target, and its reverse is a different edge, with one not implying the other—unlike orientation, which merely assigns a sign to the same undirected edge (e.g., positive vs. C1.We introduceSemi-simplicial Neural Networks(SSNs), the first TDL models explicitly designed for semi-simplicial sets. A binary dynamics map B assigns to each simplex a binary activation vector xσ ∈ {0,1}T indicating whether it is active (1) or inactive (0) at each ofTdiscrete time steps. To address these limitations, we propose SSNs, a principled class of TDL architectures that operate directly on attributed semi-simplicial sets. if ωR = MPNN-D (Rossi et al., 2024), the SSN is a message passing architecture; if ωR is masked self-attention, the SSN is a transformer-like architecture. An SSN layer, as defined in (1), is equivariant to simplex reindexing if, for each relation R∈ R, the message function ωR and the aggregation operator N are permuta- tion equivariant. 4 TOPOLOGICALDEEPREPRESENTATIONLEARNING FORBRAINDYNAMICS We introduce a principled framework for representation learning on dynamical brain activity, com- patible with graph-based and TDL architectures. 6 CONCLUSION We introduced Semi-Simplicial Neural Networks (SSNs), a new class of topological deep learning (TDL) architectures for data structured as semi-simplicial sets. Both convolutional (Ebli et al., 2020; Yang et al., 2022; Hajij et al., 2020; Yang & Isufi, 2023; Roddenberry et al., 2021; Hajij et al., 2022a; Yan & Kuruoglu, 2025) and attentional (Battiloro et al., 2024b; Giusti et al., 2022; Goh et al., 2022; Giusti et al., 2023a) architectures over simplicial and cell complexes have been introduced. Given a directed simplicial complex K—with directed graphs as its 1-skeleton—we define itssymmetrization map π:K → Ksym by sending each n-simplex (v0, . Transitivization.Conversely, given a simplicial complex K= (V,Σ) endowed with an arbitrary total order < on its vertex set V , one naturally obtains a unique directed simplicial complex Kdir by assigning to each simplex σ∈Σ the directed simplex σ< = (v0, v1, . Aggregated Lower and Upper Adjacencies.For each dimension N≥n >0 , we define the collection of all possiblen-lower adjacencyrelations as R↓n ={R ↓n,i,j |(i, j)∈[n] 2}.(13) Accordingly, we define R↓n := [ R↓n,(14) which relates a simplex σ to all simplices τ sharing a common facet with σ. Then, for dimensions N−1≥n≥0, we define the collection of all possiblen-thupper adjacencyrelations by R↑n ={R ↑n,i,j |(i, j)∈[n] 2 andi̸=j}..(15) Finally, we define the aggregated upper adjacency relation as R↑n := [ R↑n,(16) which relates a simplex σ to all simplices τ that share a common higher-dimensional simplex containing both as facets. Assume that the image of c consists of k distinct colors (which, without loss of generality, can be labeled {1, . This algorithmic procedure, yielding incomplete graph invariants discriminating some non-isomorphic pairs, is the Weisfeiler–Leman test (Weisfeiler & Leman, 1968). If for each relation R∈ R, the mapping ωR and the aggregator N are permutation equivariant, then the SSN layer is permutation equivariant with respect to simplex reindexing. X l+1 t =ϕ  X l t, dim(S)M s=0 O R∈Rs t ωR(X l s)   .(19) This formulation involves a two-step aggregation procedure: For computational efficiency and practical implementation, we adapt the HeteroConv framework to express the R-SSN update at layerlfor then-dimensional feature representation: To address this, we adopt a soft constraint from (Shazeer et al., 2017), incorporating a regularization term in the training loss to encourage equitable distribution of samples among experts. Specifically, for each training sample x, we compute the probability P(x, i)that the gating function G(x)i remains active upon independently re-sampling noise for the i-th expert, holding other noises constant. We now define the load vector Load(X), i.e., an estimator of the number of samples assigned to each expert given a batchX, whose components are Load(X) i = X x∈X P(x, i).(26) Finally, the additional loss termL load is: Lload =λ loadCV(Load(X)) 2 (27) where CV(·) computes the coefficient of variation and λload is a hyperparameter that weights the contribution of this term in the total loss. Let H be an SSN layer of X l+1 = H(X l,B) where X l+1 ∈N×D l+1 with ωR an MPNN-D module (Rossi et al., 2024). G.2 DYNAMICALACTIVITYCOMPLEXES In Section 4, we introduced a lifting procedure that maps dynamic binary digraphs to dynamic binary directed simplicial complexes. a Dynamical Activity Complex (DAC) must encode the time series offunctional complexes—that is, the directed flag complexes derived from the subgraphs induced by active neurons at each time step. Concretely, for each simplexσ, we assign the activation pattern ˜B(σ) = h min v∈σ B1(v), . Given a face-map-induced relation R on GB or KB, we define itsk-hop composition restricted to the active simplices at timetas R1,t,k ={(σ, τ)∈R ◦k |τ∈Σ 1,t}, where Σ1,t denotes the set of simplices active at time t. Lemma 6.Let KB be a DAC with a labeled set of simplices S and corresponding binary feature matrix X∈B |S|×T , where each row encodes the activation pattern of a simplex over T discrete time steps. In other words, (ARX) i t =| {σj ∈R(σ i) :X j t = 1} |=|R1,t(σi)| , where we define R1,t(σi) as the