REAL METHOD of "Label Smoothing Improves Machine Unlearning" (arXiv 2406.07698v1). Extracted method-dense text follows; base the explainer on THIS actual approach, not the abstract.

Taking inspiration from the influence of label smoothing on model confidence and differential privacy, we propose a simple gradient-based MU approach that uses an inverse process of label smoothing. We conducted extensive experiments on six datasets of various sizes and different modalities, demonstrating the effectiveness and robustness of our proposed method. Retraining from scratch (Retrain) is a straightforward method when we want to remove the data from the model; yet it incurs prohibitive computation costs for large models due to computing resource constraints. The most straightforward MU approach should be retraining-based method [3], meaning that we retrain the model from scratch without using the data to be forgotten. One of the primary reasons is that once the model completes training, the gradient of well-memorized data that was learned during the process is diminishing (close to 0 loss) and therefore the effect of GA is rather limited. Our approach is a plug-and-play method that can improve the gradient-based MU performance consistently and does not hurt the performance of the remaining dataset and the testing dataset in a gradient-mixed way. ● Extensive experiments in six datasets in different modalities and several unlearning paradigms regarding different MU metrics show the robustness and generalization of our method. One direct unlearning method involves retraining the model from scratch after removing the forgetting data from the original training set. Influence Function is a classic statistical method to track the impact of one training sample. 1) θ∗ tr, the optimal parameters trained from Dtr ∼ Dtr , 2) θ∗ r , the optimal parameters trained from Dr ∼ Dr, such that Dr = Dtr∖ Df and 3) θ∗ f , the optimal parameters unlearnt using gradient ascent (GA) on Df ∼ Df . To alleviate the possible undesired effect of GA, we propose to use label smoothing as a plug-in module. A Plug-and-Play and Gradient-Mixed MU Method Compared with retraining, Fine-Tune (FT) and GA are much more efficient as illustrated in the Experiment part in Section 4 with comparable or better MU performance. Due to the flexibility of label smoothing, our method is suitable for the gradient-based methods including FT and GA, making our method a plug-and-play algorithm. We calculate the loss using a gradient-mixed method as: Thus, our method does not incur substantially more computation but improves the MU performance on a large scale. 4 Experiments and Results 4.1 Experiment Setup Dataset and Model Selection We validate our method using various datasets in different scales and modality, including CIFAR-10 [48], CIFAR-100 [48], SVHN [49], CelebA [50], ImageNet [51] and 20 Newsgroup [52] datasets. Gap [61] to compare the MU performance with the retraining [ 11], where a smaller performance gap between the unlearning and retraining indicates a better MU method. Moreover, in terms of average gap, the proposed method shows its similarity to the retrained model. When the optimization goal is complete unlearning, our methods are much better in UA and MIAScore than the baseline methods, even retrain. When the optimization goal is to make the approximate MU close to the retrained model, our methods still outperform other baseline methods. We train a model to classify 20 super-classes using CIFAR-100 training set. Compared with baseline methods, our method can forget the identity information better without forgetting too much remaining information in the dataset. This paradigm provides a practical usage of MU and our methods provide a faster and more reliable way to improve the MU performance. Even when the metrics is based on the retrained model, the proposed method still can make good balance among all the metrics. A plot of loss landscape of the retrained model θr on CIFAR-10 dataset is attached to Figure 6 in the Appendix. We report the UGradSL and UGradSL+ with the optimization objective based on retraining which are tagged with (R). For a fair comparison, we also try our best to make the baseline methods as close as to retraining. Our method can be further validated and tested in other tasks, such as unlearning recommendation systems, etc. Specifically targeted at the training data, model attacks are often used as a proxy metric for privacy leakage in pretrained models. Data extraction attacks aim to reverse engineer individual samples used during training [ 67], while property inference attacks focus on inferring properties of the training data [68]. 8 The Framework of Our Method Our framework is shown in given Figure 1. The proposed method with a following (R) means the method is optimized according to the retrained model while the method without a (R) means the method is optimized according to the completely unlearning [63]. Average Gap is to evaluate how close the approximate MU method is to the retrained model. When we target to make the approximate MU method close to the retrained model, UGradSL (★), UGradSL(R,★) and UGradSL+(R, ★) outperform the other methods. Moreover, in both plots, our proposed methods are always the top methods. Our methods show the robustness and generalization capability in both MU metrics. 10 The details of Algorithm We provide a more detailed explanation of UGradSL and UGradSL+ in Algorithm 1 here. We compute the distance d(zr i , zf i )∈ [0, 1] for each (zr i , zf i ) pair where zr i ∈ Br and zf i ∈ Bf (Line 6). We also study the difference between PLS and NLS by replacing NLS with PLS in our methods. The differences between FT and retrain are the model initialization θo and much smaller training epochs. For the retraining-based method, we use the validation set to select the hyper-parameters. Our method can improve the unlearning accuracy (UA) without significant drop of testing accuracy (TA). Our methods can improve the unlearning accuracy (UA) without significant drop of testing accuracy (TA). It can be found that our method will not lead to the extra hint of Df . The confusion matrix of testing set and forgetting set Df using our method on CIFAR-10 with random forgetting across all the classes.