# When to Retrain after Drift: A Data-Only Test of Post-Drift Data Size Sufficiency

## Metadata

- Conference: iclr-2026
- Status: Poster
- OpenReview ID: 05PqjBzN6S
- Authors: Ren Fujiwara; Yasuko Matsubara; Yasushi Sakurai
- Primary area: learning on time series and dynamical systems
- Keywords: Concept drift;Stream learning;Data sufficiency;Time series
- Source URL: https://openreview.net/forum?id=05PqjBzN6S
- PDF URL: https://openreview.net/pdf?id=05PqjBzN6S

## Abstract

Sudden concept drift makes previously trained predictors unreliable, yet deciding when to retrain and what post-drift data size is sufficient is rarely addressed. We propose CALIPER —a detector- and model-agnostic, data-only test that estimates the post-drift data size required for stable retraining. CALIPER exploits state dependence in streams generated by dynamical systems: we run a single-pass weighted local regression over the post-drift window and track a one-step proxy error as a function of a locality parameter $\theta$. When an effective sample size gate is satisfied, a monotonically non-increasing trend in this error with increasing a locality parameter indicates that the data size is sufficiently informative for retraining.
We also provide a theoretical analysis of our method, and we show that the algorithm has a low per-update time and memory. Across datasets from four heterogeneous domains, three learner families, and two detectors, CALIPER consistently matches or exceeds the best fixed data size for retraining while incurring negligible overhead and often outperforming incremental updates. CALIPER closes the gap between drift detection and data-sufficient adaptation in streaming learning.

## One-Sentence Claim

CALIPER estimates when enough post-drift stream data has accumulated for stable retraining, using a detector- and model-agnostic data-only test based on local regression behavior.

## Problem

After sudden concept drift, models may become unreliable, but retraining too early can use too little post-drift data and retraining too late wastes degraded predictions. Existing drift workflows often detect the drift but do not decide how much new data is sufficient for retraining.

## Core Contribution

The paper contributes CALIPER, a data-only sufficiency test for post-drift retraining. It bridges drift detection and adaptation by estimating whether the post-drift window contains enough informative data, independent of a particular detector or downstream learner.

## Method

CALIPER uses state dependence in streams from dynamical systems. It performs single-pass weighted local regression over the post-drift window, tracks a one-step proxy error as locality parameter `theta` varies, and uses an effective sample-size gate. A monotonically non-increasing proxy-error trend with increasing locality indicates sufficient post-drift data for retraining.

## Experiments and Evidence

The abstract reports theoretical analysis plus low per-update time and memory. Across four heterogeneous domains, three learner families, and two detectors, CALIPER matches or exceeds the best fixed retraining data size, has negligible overhead, and often outperforms incremental updates.

## Limits and Failure Modes

This note is abstract/metadata-only. Full-text review should inspect the exact sufficiency test, effective sample-size threshold, assumptions about state dependence, behavior under gradual or recurring drift, and robustness when post-drift windows are noisy, sparse, or nonstationary. The proxy error may not match downstream retraining utility for all model families.

## Deep Themes

- Data sufficiency after concept drift.
- Stream learning adaptation.
- Model-agnostic retraining decisions.
- Local regression as diagnostic evidence.

## Subthemes

- CALIPER.
- Post-drift window sizing.
- Effective sample-size gate.
- Dynamical-system streams.
- Incremental updates versus retraining.

## Connections to Other Papers

Connects to Track-and-Stop through stopping/sufficiency decisions under uncertainty, to T3 through determining which trajectory prefix is informative, and to time-series foundation-model notes where data regime and temporal dependence shape adaptation.

## Notes for Cross-Paper Synthesis

CALIPER strengthens the broader selection theme: the question is not only whether a distribution changed, but whether the new evidence is sufficient to act. It turns retraining from a schedule into an evidence-threshold decision.
